<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
        <!--
            DAMA Doctrine Test Bundle is INSTALLED as a dev-dep but the
            PHPUnit extension is intentionally DISABLED.

            Symfony BP audit item #11 added the bundle so it is ready to
            use, but the existing test suite (~6900 tests) is built around
            manual fixture setUp/tearDown + `disableReboot()`, which
            conflicts with DAMA's per-test transactional rollback (85/97
            sample tests fail when activated). A future sprint must
            migrate the fixture-heavy tests to DAMA conventions before
            this extension is re-enabled.

            To re-enable per-test later:
              <bootstrap class="DAMA\DoctrineTestBundle\PHPUnit\PHPUnitExtension" />
        -->
    </extensions>
</phpunit>
