{#
 # Management KPIs Widget
 # Displays module-aware KPIs for management reports
 # Only shows KPIs for active modules
 # Sections are collapsible - danger sections auto-expand
 #}
{% import '_components/_fa_progress.html.twig' as _fa_progress %}

{% if management_kpis is defined and management_kpis|length > 0 %}
<div class="widget-card mb-4"
     data-widget-id="management-kpis"
     data-dashboard-customizer-target="widget">
    <div class="widget-header">
        <h3 class="widget-title">
            <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
            {{ 'dashboard.management_kpis.title'|trans({}, 'dashboard') }}
        </h3>
        <div class="widget-actions">
            {% include '_components/_badge.html.twig' with {
                variant: 'secondary',
                title: 'dashboard.management_kpis.subtitle'|trans({}, 'dashboard'),
                content: management_kpis.active_modules|default([])|length ~ ' ' ~ 'common.modules'|trans({}, 'messages')
            } %}
        </div>
    </div>
    <div class="widget-body p-3">
        {# KPI sections with Aurora mask-icon names (see assets/icons/*.svg). #}
        {% set kpi_sections = [
            { key: 'core', icon: 'nav-shield-check', title_key: 'dashboard.management_kpis.core.title', route: 'app_soa_index', col_class: 'col-6' },
            { key: 'risk_management', icon: 'status-warning', title_key: 'dashboard.management_kpis.risk_management.title', route: 'app_risk_index', col_class: 'col-6 col-md-4' },
            { key: 'asset_management', icon: 'asset-server', title_key: 'dashboard.management_kpis.asset_management.title', route: 'app_asset_index', col_class: 'col-6 col-md-4' },
            { key: 'incident_management', icon: 'status-fire', title_key: 'dashboard.management_kpis.incident_management.title', route: 'app_incident_index', col_class: 'col-6 col-md-3' },
            { key: 'business_continuity', icon: 'nav-workflow', title_key: 'dashboard.management_kpis.business_continuity.title', route: 'app_bcm_index', col_class: 'col-6 col-md-3' },
            { key: 'training', icon: 'nav-mortarboard', title_key: 'dashboard.management_kpis.training.title', route: 'app_training_index', col_class: 'col-6' },
            { key: 'audits', icon: 'nav-clipboard-check', title_key: 'dashboard.management_kpis.audits.title', route: 'app_audit_index', col_class: 'col-6 col-md-4' },
            { key: 'supplier_management', icon: 'nav-building', title_key: 'dashboard.management_kpis.supplier_management.title', route: 'app_supplier_index', col_class: 'col-6 col-md-3' },
            { key: 'documentation', icon: 'nav-file-earmark-text', title_key: 'dashboard.management_kpis.documentation.title', route: 'app_document_index', col_class: 'col-6 col-md-4' },
            { key: 'bsi_stufen', icon: 'nav-shield', title_key: 'dashboard.management_kpis.bsi_stufen.title', route: 'app_bsi_grundschutz_check', col_class: 'col-6 col-md-4' },
        ] %}

        <div class="accordion" id="kpiAccordion">
            {% for section in kpi_sections %}
                {% if attribute(management_kpis, section.key) is defined %}
                    {% set sectionKpis = attribute(management_kpis, section.key) %}

                    {# Check if any KPI in this section has danger status #}
                    {% set hasDanger = false %}
                    {% for key, kpi in sectionKpis %}
                        {% if kpi.status|default('') == 'danger' %}
                            {% set hasDanger = true %}
                        {% endif %}
                    {% endfor %}

                    <div class="accordion-item">
                        <h2 class="accordion-header" id="kpiHeading-{{ section.key }}">
                            <button class="accordion-button {{ hasDanger ? '' : 'collapsed' }} py-2"
                                    type="button"
                                    data-bs-toggle="collapse"
                                    data-bs-target="#kpi-{{ section.key }}"
                                    aria-expanded="{{ hasDanger ? 'true' : 'false' }}"
                                    aria-controls="kpi-{{ section.key }}">
                                <i class="fa-icon fa-icon--{{ section.icon }} me-2" aria-hidden="true"></i>
                                {{ section.title_key|trans({}, 'dashboard') }}
                                {% if hasDanger %}
                                    <span class="fa-status-pill fa-status-pill--danger ms-2">!</span>
                                {% endif %}
                            </button>
                        </h2>
                        <div id="kpi-{{ section.key }}"
                             class="accordion-collapse collapse {{ hasDanger ? 'show' : '' }}"
                             aria-labelledby="kpiHeading-{{ section.key }}"
                             data-bs-parent="#kpiAccordion">
                            <div class="accordion-body py-2 px-3">
                                <div class="d-flex justify-content-end mb-2">
                                    <a href="{{ path(section.route) }}" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm p-0" title="{{ 'common.view_all'|trans({}, 'messages') }}">
                                        <i class="fa-icon fa-icon--util-arrow-right" aria-hidden="true"></i>
                                    </a>
                                </div>
                                <div class="row g-2">
                                    {% for key, kpi in sectionKpis %}
                                    <div class="{{ section.col_class }}">
                                        {% if section.key == 'core' %}
                                        {# Core section uses the richer card style with progress bars #}
                                        <div class="p-2 rounded border-start border-4 border-{{ kpi.status == 'good' ? 'success' : (kpi.status == 'warning' ? 'warning' : (kpi.status == 'danger' ? 'danger' : 'info')) }} bg-body">
                                            <div class="d-flex justify-content-between align-items-center">
                                                <span class="small text-muted">{{ ('dashboard.kpi.' ~ key)|trans({}, 'dashboard') }}</span>
                                                <span>
                                                    {% set kpiVariant = kpi.status == 'good' ? 'success' : (kpi.status == 'warning' ? 'warning' : (kpi.status == 'danger' ? 'danger' : 'secondary')) %}
                                                    {% include '_components/_badge.html.twig' with {
                                                        variant: kpiVariant,
                                                        content: kpi.value ~ kpi.unit
                                                    } %}
                                                    {% if kpi.trend is defined and kpi.trend != 'stable' %}
                                                    <span class="small ms-1 text-{{ kpi.trend_sentiment|default('neutral') == 'good' ? 'success' : (kpi.trend_sentiment|default('neutral') == 'bad' ? 'danger' : 'muted') }}">
                                                        {% if kpi.trend == 'up' %}
                                                            <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                                                        {% elseif kpi.trend == 'down' %}
                                                            <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                                                        {% endif %}
                                                        {% if kpi.trend_delta is defined %}
                                                            <small>{{ kpi.trend_delta > 0 ? '+' : '' }}{{ kpi.trend_delta }}{{ kpi.unit|default('') }}</small>
                                                        {% endif %}
                                                    </span>
                                                    {% endif %}
                                                </span>
                                            </div>
                                            {% if kpi.details is defined and kpi.details.total is defined %}
                                            {{ _fa_progress.render({ value: kpi.value, tone: kpi.status == 'good' ? 'success' : (kpi.status == 'warning' ? 'warning' : 'danger'), class: 'mt-1' }) }}
                                            {% endif %}
                                        </div>
                                        {% else %}
                                        {# Other sections use the compact mini card style #}
                                        <div class="kpi-mini p-2 rounded bg-body-secondary">
                                            <div class="small text-muted">{{ ('dashboard.kpi.' ~ key)|trans({}, 'dashboard') }}</div>
                                            <div class="fw-bold text-{{ kpi.status == 'good' ? 'success' : (kpi.status == 'warning' ? 'warning' : (kpi.status == 'danger' ? 'danger' : 'body')) }}">
                                                {{ kpi.value }}{{ kpi.unit }}
                                                {% if kpi.trend is defined and kpi.trend != 'stable' %}
                                                <span class="small ms-1 text-{{ kpi.trend_sentiment|default('neutral') == 'good' ? 'success' : (kpi.trend_sentiment|default('neutral') == 'bad' ? 'danger' : 'muted') }}">
                                                    {% if kpi.trend == 'up' %}
                                                        <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                                                    {% elseif kpi.trend == 'down' %}
                                                        <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                                                    {% endif %}
                                                    {% if kpi.trend_delta is defined %}
                                                        <small>{{ kpi.trend_delta > 0 ? '+' : '' }}{{ kpi.trend_delta }}{{ kpi.unit|default('') }}</small>
                                                    {% endif %}
                                                </span>
                                                {% endif %}
                                            </div>
                                        </div>
                                        {% endif %}
                                    </div>
                                    {% endfor %}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</div>
{% endif %}
