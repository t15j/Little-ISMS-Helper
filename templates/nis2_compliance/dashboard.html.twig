{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% block title %}{{ 'nis2.dashboard.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="mb-3">
                <i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i>
                {{ 'nis2.dashboard.title'|trans({}, 'messages') }}
            </h1>
            <p class="text-muted">{{ 'nis2.dashboard.description'|trans({}, 'messages') }}</p>
        </div>
    </div>

    {# Overall Compliance Score #}
    <div class="row mb-4">
        <div class="col-12">
            {% embed '_components/_card.html.twig' with { 'class': (overall_compliance_score >= 95 ? 'border-success' : (overall_compliance_score >= 75 ? 'border-warning' : 'border-danger')) } %}
                {% block card_body %}
                    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                    <div class="text-center">
                        <div class="h2 mb-3">{{ 'nis2.dashboard.overall_compliance'|trans({}, 'messages') }}</div>
                        <div class="display-1 {% if overall_compliance_score >= 95 %}text-success{% elseif overall_compliance_score >= 75 %}text-warning{% else %}text-danger{% endif %}">
                            {{ overall_compliance_score }}%
                        </div>
                        {{ _fa_progress.render({ value: overall_compliance_score, tone: overall_compliance_score >= 95 ? 'success' : (overall_compliance_score >= 75 ? 'warning' : 'danger'), showText: true, class: 'mt-3' }) }}
                        <p class="mt-3 mb-0">
                            {% if overall_compliance_score >= 95 %}
                                {% include '_components/_badge.html.twig' with { variant: 'success', icon: 'status-ok', content: 'nis2.dashboard.status.compliant'|trans({}, 'messages') } %}
                            {% elseif overall_compliance_score >= 75 %}
                                {% include '_components/_badge.html.twig' with { variant: 'warning', icon: 'status-warning', content: 'nis2.dashboard.status.partial'|trans({}, 'messages') } %}
                            {% else %}
                                {% include '_components/_badge.html.twig' with { variant: 'danger', icon: 'status-critical', content: 'nis2.dashboard.status.non_compliant'|trans({}, 'messages') } %}
                            {% endif %}
                        </p>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Art. 21.2 letter grid — all 11 risk-management measures #}
    {% if letters|length > 0 %}
        {% set status_variant = {
            'good': 'success',
            'warning': 'warning',
            'danger': 'danger',
            'info': 'info',
            'na': 'secondary',
        } %}
        {% set status_text_class = {
            'good': 'text-success',
            'warning': 'text-warning',
            'danger': 'text-danger',
            'info': 'text-info',
            'na': 'text-muted',
        } %}
        <div class="row mb-2">
            <div class="col-12">
                <h2 class="h4">
                    <i class="fa-icon fa-icon--nav-grid" aria-hidden="true"></i>
                    {{ 'nis2.dashboard.letters.title'|trans({}, 'messages') }}
                </h2>
                <p class="text-muted small mb-3">{{ 'nis2.dashboard.letters.description'|trans({}, 'messages') }}</p>
            </div>
        </div>
        <div class="row g-3 mb-4" data-testid="nis2-letters-grid">
            {% for code, letter in letters %}
                {% set key = code|replace({'21.2.': ''}) %}
                {% set color = status_text_class[letter.status] ?? 'text-muted' %}
                {% set badge_variant = status_variant[letter.status] ?? 'secondary' %}
                <div class="col-md-6 col-lg-4 col-xl-3">
                    {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                        {% block card_body %}
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <div>
                                    <span class="border fa-status-pill fa-status-pill--neutral">{{ code }}</span>
                                </div>
                                {% include '_components/_badge.html.twig' with {
                                    variant: badge_variant,
                                    content: ('nis2.dashboard.status.' ~ letter.status)|trans({}, 'messages')
                                } %}
                            </div>
                            <div class="h6 card-title">
                                {{ ('nis2.dashboard.letter.' ~ key ~ '.title')|trans({}, 'messages') }}
                            </div>
                            <div class="display-6 {{ color }}" data-testid="letter-{{ key }}-value">
                                {% if letter.value is null %}
                                    <span class="text-muted">—</span>
                                {% else %}
                                    {{ letter.value }}{{ letter.unit }}
                                {% endif %}
                            </div>
                            {% if letter.details is defined and letter.details|length > 0 %}
                                <ul class="list-unstyled small text-muted mt-2 mb-0">
                                    {% for k, v in letter.details %}
                                        <li>
                                            <span class="text-capitalize">{{ k|replace({'_': ' '}) }}:</span>
                                            <strong>{{ v is iterable ? v|length : v }}</strong>
                                        </li>
                                    {% endfor %}
                                </ul>
                            {% endif %}
                        {% endblock %}
                    {% endembed %}
                </div>
            {% endfor %}
        </div>

        {# Art. 23 reporting timeline #}
        {% if article23 is not null %}
            {% set art23_variant = status_variant[article23.status] ?? 'secondary' %}
            <div class="row mb-4">
                <div class="col-12">
                    {% embed '_components/_card.html.twig' with { 'class': 'border-' ~ art23_variant } %}
                        {% block card_header %}
                            <div class="card-header d-flex justify-content-between align-items-center">
                                <h2 class="h5 mb-0">
                                    <i class="fa-icon fa-icon--ui-hourglass" aria-hidden="true"></i>
                                    {{ 'nis2.dashboard.article23.title'|trans({}, 'messages') }}
                                </h2>
                                {% include '_components/_badge.html.twig' with {
                                    variant: art23_variant,
                                    content: ('nis2.dashboard.status.' ~ article23.status)|trans({}, 'messages')
                                } %}
                            </div>
                        {% endblock %}
                        {% block card_body %}
                            <div class="row text-center">
                                <div class="col-md-3">
                                    <div class="text-muted small">{{ 'nis2.dashboard.article23.tracked'|trans({}, 'messages') }}</div>
                                    <div class="h4 mb-0">{{ article23.tracked ?? 0 }}</div>
                                </div>
                                <div class="col-md-3">
                                    <div class="text-muted small">{{ 'nis2.dashboard.article23.early_warning'|trans({}, 'messages') }} (24h)</div>
                                    <div class="h4 mb-0">{{ article23.early_warning_rate ?? 0 }}%</div>
                                </div>
                                <div class="col-md-3">
                                    <div class="text-muted small">{{ 'nis2.dashboard.article23.detailed'|trans({}, 'messages') }} (72h)</div>
                                    <div class="h4 mb-0">{{ article23.detailed_notification_rate ?? 0 }}%</div>
                                </div>
                                <div class="col-md-3">
                                    <div class="text-muted small">{{ 'nis2.dashboard.article23.final'|trans({}, 'messages') }} (1m)</div>
                                    <div class="h4 mb-0">{{ article23.final_report_rate ?? 0 }}%</div>
                                </div>
                            </div>
                            {% if article23.overdue_count is defined and article23.overdue_count > 0 %}
                                {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'danger', icon: 'status-warning', class: 'mt-3 mb-0' } } %}
                                    {% block alert_body %}{{ 'nis2.dashboard.article23.overdue'|trans({'%count%': article23.overdue_count}, 'messages') }}{% endblock %}
                                {% endembed %}
                            {% endif %}
                        {% endblock %}
                    {% endembed %}
                </div>
            </div>
        {% endif %}
    {% endif %}

    {# Key Metrics #}
    <div class="row g-3 mb-4">
        {# MFA Adoption #}
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                {% block card_body %}
                    <div class="small fw-semibold text-muted mb-1">
                        <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
                        {{ 'nis2.dashboard.mfa.title'|trans({}, 'messages') }}
                    </div>
                    <div class="display-6 {% if mfa_adoption_rate >= 95 %}text-success{% elseif mfa_adoption_rate >= 75 %}text-warning{% else %}text-danger{% endif %}">
                        {{ mfa_adoption_rate }}%
                    </div>
                    <p class="small text-muted mb-0">
                        {{ users_with_mfa }} / {{ total_users }} {{ 'nis2.dashboard.mfa.users'|trans({}, 'messages') }}
                    </p>
                    <small class="text-muted">{{ 'nis2.article'|trans({'%article%': '21.2.b'}, 'compliance') }}</small>
                {% endblock %}
            {% endembed %}
        </div>

        {# Incident Reporting #}
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                {% block card_body %}
                    <div class="small fw-semibold text-muted mb-1">
                        <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i>
                        {{ 'nis2.dashboard.incident_reporting.title'|trans({}, 'messages') }}
                    </div>
                    <div class="display-6 {% if reporting_compliance_rate >= 95 %}text-success{% elseif reporting_compliance_rate >= 75 %}text-warning{% else %}text-danger{% endif %}">
                        {{ reporting_compliance_rate }}%
                    </div>
                    <p class="small text-muted mb-0">
                        {{ total_nis2_incidents }} {{ 'nis2.dashboard.incident_reporting.incidents'|trans({}, 'messages') }}
                    </p>
                    {%- set articleNumber = '23' -%}
                    <small class="text-muted">{{ 'nis2.article'|trans({'%article%': articleNumber}, 'compliance') }}</small>
                {% endblock %}
            {% endembed %}
        </div>

        {# Vulnerability Management #}
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                {% block card_body %}
                    <div class="small fw-semibold text-muted mb-1">
                        <i class="fa-icon fa-icon--incident" aria-hidden="true"></i>
                        {{ 'nis2.dashboard.vulnerability.title'|trans({}, 'messages') }}
                    </div>
                    <div class="display-6 {% if critical_vulnerabilities + high_vulnerabilities == 0 %}text-success{% elseif critical_vulnerabilities > 0 %}text-danger{% else %}text-warning{% endif %}">
                        {{ critical_vulnerabilities + high_vulnerabilities }}
                    </div>
                    <p class="small text-muted mb-0">
                        {{ 'nis2.dashboard.vulnerability.critical_high'|trans({}, 'messages') }}
                    </p>
                    <small class="text-muted">{{ 'nis2.article'|trans({'%article%': '21.2.d'}, 'compliance') }}</small>
                {% endblock %}
            {% endembed %}
        </div>

        {# Patch Deployment #}
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                {% block card_body %}
                    <div class="small fw-semibold text-muted mb-1">
                        <i class="fa-icon fa-icon--nav-download" aria-hidden="true"></i>
                        {{ 'nis2.dashboard.patch.title'|trans({}, 'messages') }}
                    </div>
                    <div class="display-6 {% if patch_deployment_rate >= 95 %}text-success{% elseif patch_deployment_rate >= 75 %}text-warning{% else %}text-danger{% endif %}">
                        {{ patch_deployment_rate }}%
                    </div>
                    <p class="small text-muted mb-0">
                        {{ deployed_patches }} / {{ total_patches }} {{ 'nis2.dashboard.patch.patches'|trans({}, 'messages') }}
                    </p>
                    <small class="text-muted">{{ 'nis2.article'|trans({'%article%': '21.2.d'}, 'compliance') }}</small>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Overdue Incidents Alert #}
    {% if overdue_incidents|length > 0 %}
        <div class="row mb-4">
            <div class="col-12">
                <div class="fa-alert fa-alert--danger" role="alert">
                    <i class="fa-icon fa-icon--status-warning" fa-alert__icon" aria-hidden="true"></i>
                    <div class="fa-alert__body"><div class="h4 alert-heading">
                        <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i>
                        {{ 'nis2.dashboard.overdue.title'|trans({}, 'messages') }}
                    </div>
                    <p>{{ 'nis2.dashboard.overdue.description'|trans({'%count%': overdue_incidents|length}, 'messages') }}</p>
                    <hr>
                    <ul class="mb-0">
                        {% for incident in overdue_incidents %}
                            <li>
                                <a href="{{ path('app_incident_show', {id: incident.id}) }}" class="alert-link">
                                    {{ incident.title }}
                                </a>
                                -
                                {% if incident.isEarlyWarningOverdue %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'nis2.dashboard.overdue.early_warning'|trans({}, 'messages') } %}
                                {% endif %}
                                {% if incident.isDetailedNotificationOverdue %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'nis2.dashboard.overdue.detailed'|trans({}, 'messages') } %}
                                {% endif %}
                                {% if incident.isFinalReportOverdue %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'nis2.dashboard.overdue.final'|trans({}, 'messages') } %}
                                {% endif %}
                            </li>
                        {% endfor %}
                    </ul>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}

    {# Detailed Metrics #}
    <div class="row g-3">
        {# Incident Reporting Details #}
        <div class="col-md-6">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">{{ 'nis2.dashboard.incident_details.title'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_table.html.twig' with {
                        'class': 'table-sm',
                        'noMargin': true,
                        'responsive': true
                    } %}
                        {% block table_head %}
                            <tr>
                                <th scope="col">{{ 'nis2.dashboard.incident_details.stage'|trans({}, 'messages') }}</th>
                                <th scope="col" class="text-end">{{ 'nis2.dashboard.incident_details.compliant'|trans({}, 'messages') }}</th>
                                <th scope="col" class="text-end">{{ 'nis2.dashboard.incident_details.total'|trans({}, 'messages') }}</th>
                                <th scope="col" class="text-end">{{ 'nis2.dashboard.incident_details.rate'|trans({}, 'messages') }}</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            <tr>
                                <td>{{ 'nis2.dashboard.incident_details.early_warning'|trans({}, 'messages') }} (24h)</td>
                                <td class="text-end">{{ early_warning_compliant }}</td>
                                <td class="text-end">{{ total_nis2_incidents }}</td>
                                <td class="text-end">
                                    {% set rate = total_nis2_incidents > 0 ? (early_warning_compliant / total_nis2_incidents * 100)|round(1) : 100 %}
                                    {% include '_components/_badge.html.twig' with { variant: rate >= 95 ? 'success' : (rate >= 75 ? 'warning' : 'danger'), content: rate ~ '%' } %}
                                </td>
                            </tr>
                            <tr>
                                <td>{{ 'nis2.dashboard.incident_details.detailed'|trans({}, 'messages') }} (72h)</td>
                                <td class="text-end">{{ detailed_notification_compliant }}</td>
                                <td class="text-end">{{ total_nis2_incidents }}</td>
                                <td class="text-end">
                                    {% set rate = total_nis2_incidents > 0 ? (detailed_notification_compliant / total_nis2_incidents * 100)|round(1) : 100 %}
                                    {% include '_components/_badge.html.twig' with { variant: rate >= 95 ? 'success' : (rate >= 75 ? 'warning' : 'danger'), content: rate ~ '%' } %}
                                </td>
                            </tr>
                            <tr>
                                <td>{{ 'nis2.dashboard.incident_details.final'|trans({}, 'messages') }} (30d)</td>
                                <td class="text-end">{{ final_report_compliant }}</td>
                                <td class="text-end">{{ total_nis2_incidents }}</td>
                                <td class="text-end">
                                    {% set rate = total_nis2_incidents > 0 ? (final_report_compliant / total_nis2_incidents * 100)|round(1) : 100 %}
                                    {% include '_components/_badge.html.twig' with { variant: rate >= 95 ? 'success' : (rate >= 75 ? 'warning' : 'danger'), content: rate ~ '%' } %}
                                </td>
                            </tr>
                        {% endblock %}
                    {% endembed %}
                {% endblock %}
            {% endembed %}
        </div>

        {# Quick Actions #}
        <div class="col-md-6">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">{{ 'nis2.dashboard.actions.title'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="d-grid gap-2">
                        <a href="{{ path('app_nis2_art21_requirements') }}" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i>
                            {{ 'nis2.dashboard.actions.view_art21_requirements'|trans({}, 'nis2') }}
                        </a>
                        <a href="{{ path('app_incident_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--ui-list" aria-hidden="true"></i>
                            {{ 'nis2.dashboard.actions.view_incidents'|trans({}, 'messages') }}
                        </a>
                        <a href="{{ path('app_vulnerability_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--incident" aria-hidden="true"></i>
                            {{ 'nis2.dashboard.actions.view_vulnerabilities'|trans({}, 'messages') }}
                        </a>
                        <a href="{{ path('app_patch_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--nav-download" aria-hidden="true"></i>
                            {{ 'nis2.dashboard.actions.view_patches'|trans({}, 'messages') }}
                        </a>
                        <a href="{{ path('admin_mfa_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
                            {{ 'nis2.dashboard.actions.manage_mfa'|trans({}, 'messages') }}
                        </a>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}
