{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}Lizenzinformationen{% endblock %}

{% block body %}
<div class="container-fluid">
    {# Breadcrumb Navigation #}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'Reports', url: path('app_reports_overview') },
            { label: 'Lizenzen' }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'nav-file-earmark-text', label: 'Lizenzen' },
        title: 'Lizenzinformationen',
        subtitle: 'Third-Party Software Attributions & Compliance'
    }) }}

    <main class="fa-aurora-surface">

    <div class="row">
        <div class="col-lg-3 mb-4">
            {# Navigation Sidebar #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-list me-2" aria-hidden="true"></i>Navigation
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                <div class="list-group list-group-flush">
                    <a href="{{ path('app_licenses') }}" class="list-group-item list-group-item-action active">
                        <i class="fa-icon fa-icon--policy me-2" aria-hidden="true"></i>NOTICE
                    </a>
                    <a href="{{ path('app_licenses_report') }}" class="list-group-item list-group-item-action">
                        <i class="fa-icon fa-icon--nav-reports me-2" aria-hidden="true"></i>Detaillierter Bericht
                    </a>
                    <a href="{{ path('app_licenses_summary') }}" class="list-group-item list-group-item-action">
                        <i class="fa-icon fa-icon--nav-bar-chart me-2" aria-hidden="true"></i>Zusammenfassung
                    </a>
                </div>
                <div class="h6 mb-2 fw-semibold mt-3">Projekt</div>
                <p class="small mb-2"><strong>{{ project_name }}</strong></p>
                <p class="small text-muted mb-0">Lizenz: {{ project_license }}</p>
                {% endblock %}
            {% endembed %}

            {# Quick Actions #}
            {% embed '_components/_card.html.twig' with {class: 'mt-3'} %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--nav-tools me-2" aria-hidden="true"></i>Aktionen
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="d-grid gap-2">
                        <button data-controller="ui-actions" data-action="ui-actions#print" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
                            <i class="fa-icon fa-icon--ui-printer me-1" aria-hidden="true"></i>Drucken
                        </button>
                        <a href="{{ asset('NOTICE.md') }}" download class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
                            <i class="fa-icon fa-icon--nav-download me-1" aria-hidden="true"></i>NOTICE.md
                        </a>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-lg-9">
            {# Compliance Status Alert #}
            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'success', class: 'mb-4' } } %}
                {% block alert_body %}
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <i class="fa-icon fa-icon--ui-check fs-3 me-3" aria-hidden="true"></i>
                    </div>
                    <div>
                        <div class="fa-alert__title"><i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i> Lizenzkonform für kommerzielle Nutzung</div>
                        <p class="mb-0">
                            Alle verwendeten Third-Party-Komponenten sind für kommerzielle Nutzung freigegeben.
                            Bitte beachten Sie die Attributions- und Lizenzvorgaben.
                        </p>
                    </div>
                </div>
                {% endblock %}
            {% endembed %}

            {# NOTICE Content #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <div class="d-flex justify-content-between align-items-center">
                            <h2 class="h5 mb-0">
                                <i class="fa-icon fa-icon--policy me-2" aria-hidden="true"></i>NOTICE - Third-Party Software Attributions
                            </h2>
                            <div class="btn-group btn-group-sm" role="group" data-controller="ui-actions">
                                <button type="button" class="fa-cyber-btn fa-cyber-btn--ghost" data-action="ui-actions#setFontSize" data-ui-actions-size-param="14px" data-ui-actions-target-param="#license-content">
                                    <i class="fa-icon fa-icon--util-zoom-out" aria-hidden="true"></i>
                                </button>
                                <button type="button" class="fa-cyber-btn fa-cyber-btn--ghost" data-action="ui-actions#setFontSize" data-ui-actions-size-param="16px" data-ui-actions-target-param="#license-content">
                                    <i class="fa-icon fa-icon--util-zoom-in" aria-hidden="true"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div id="license-content" class="markdown-content" style="font-size: 14px;">
                        {{ notice_content|raw|nl2br }}
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
    </main>
</div>

<style>
.markdown-content {
    line-height: 1.6;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.markdown-content h1 {
    font-size: 2rem;
    font-weight: 700;
    margin-top: 2rem;
    margin-bottom: 1rem;
    border-bottom: 2px solid var(--border);
    padding-bottom: 0.5rem;
}

.markdown-content h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
    border-bottom: 1px solid var(--border);
    padding-bottom: 0.25rem;
}

.markdown-content h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-top: 1rem;
    margin-bottom: 0.5rem;
}

.markdown-content h4 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-top: 0.75rem;
    margin-bottom: 0.5rem;
}

.markdown-content p {
    margin-bottom: 1rem;
}

.markdown-content code {
    background-color: var(--surface-2);
    padding: 0.2rem 0.4rem;
    border-radius: 3px;
    font-size: 0.9em;
}

.markdown-content pre {
    background-color: var(--surface-2);
    padding: 1rem;
    border-radius: 5px;
    overflow-x: auto;
}

.markdown-content table {
    width: 100%;
    margin-bottom: 1rem;
    border-collapse: collapse;
}

.markdown-content table th,
.markdown-content table td {
    padding: 0.75rem;
    border: 1px solid var(--border);
}

.markdown-content table th {
    background-color: var(--surface-2);
    font-weight: 600;
}

.markdown-content ul,
.markdown-content ol {
    margin-bottom: 1rem;
    padding-left: 2rem;
}

.markdown-content blockquote {
    border-left: 4px solid var(--primary);
    padding-left: 1rem;
    margin-left: 0;
    color: var(--fg-3);
    font-style: italic;
}

.markdown-content hr {
    margin: 2rem 0;
    border: 0;
    border-top: 2px solid var(--border);
}

@media print {
    .breadcrumb,
    .page-header-actions,
    .card:not(.card:has(.markdown-content)),
    .btn,
    .alert {
        display: none !important;
    }
}
</style>
{% endblock %}
