{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% block title %}Lizenz-Zusammenfassung{% endblock %}

{% block body %}
<div class="container-fluid">
    {# Breadcrumb Navigation #}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'Reports', url: path('app_reports_overview') },
            { label: 'Lizenzen', url: path('app_licenses') },
            { label: 'Zusammenfassung' }
        ]
    } %}

    {# Page Header #}
    {% include '_components/_page_header.html.twig' with {
        title: 'Lizenz-Compliance Zusammenfassung',
        subtitle: 'Schnellübersicht über die Lizenz-Compliance',
        icon: 'nav-bar-chart'
    } %}

    <div class="row">
        <div class="col-lg-3 mb-4">
            {# Navigation Sidebar #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-list me-2" aria-hidden="true"></i>Navigation
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="list-group list-group-flush">
                        <a href="{{ path('app_licenses') }}" class="list-group-item list-group-item-action">
                            <i class="fa-icon fa-icon--policy me-2" aria-hidden="true"></i>NOTICE
                        </a>
                        <a href="{{ path('app_licenses_report') }}" class="list-group-item list-group-item-action">
                            <i class="fa-icon fa-icon--nav-reports me-2" aria-hidden="true"></i>Detaillierter Bericht
                        </a>
                        <a href="{{ path('app_licenses_summary') }}" class="list-group-item list-group-item-action active">
                            <i class="fa-icon fa-icon--nav-bar-chart me-2" aria-hidden="true"></i>Zusammenfassung
                        </a>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-lg-9">
            {% if stats.total > 0 %}
                {# Compliance Status #}
                <div class="row mb-4">
                    <div class="col-12">
                        {% set compliance_percentage = (stats.allowed / stats.total * 100)|round(1) %}
                        {% if compliance_percentage >= 95 %}
                            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'success' } } %}
                                {% block alert_body %}
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <i class="fa-icon fa-icon--ui-check fs-3 me-3" aria-hidden="true"></i>
                                    </div>
                                    <div>
                                        <div class="fa-alert__title"><i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i> Hervorragend Lizenzkonform</div>
                                        <p class="mb-0">
                                            {{ compliance_percentage }}% der Pakete haben permissive Lizenzen.
                                            Das Projekt ist für kommerzielle Nutzung freigegeben.
                                        </p>
                                    </div>
                                </div>
                                {% endblock %}
                            {% endembed %}
                        {% elseif compliance_percentage >= 80 %}
                            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
                                {% block alert_body %}
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <i class="fa-icon fa-icon--ui-info fs-3 me-3" aria-hidden="true"></i>
                                    </div>
                                    <div>
                                        <div class="fa-alert__title">✓ Überwiegend Lizenzkonform</div>
                                        <p class="mb-0">
                                            {{ compliance_percentage }}% der Pakete haben permissive Lizenzen.
                                            Einzelne Pakete erfordern besondere Aufmerksamkeit.
                                        </p>
                                    </div>
                                </div>
                                {% endblock %}
                            {% endembed %}
                        {% else %}
                            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'warning' } } %}
                                {% block alert_body %}
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <i class="fa-icon fa-icon--status-warning fs-3 me-3" aria-hidden="true"></i>
                                    </div>
                                    <div>
                                        <div class="fa-alert__title"><i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> Prüfung erforderlich</div>
                                        <p class="mb-0">
                                            {{ compliance_percentage }}% der Pakete haben permissive Lizenzen.
                                            Bitte prüfen Sie problematische Lizenzen im Detail.
                                        </p>
                                    </div>
                                </div>
                                {% endblock %}
                            {% endembed %}
                        {% endif %}
                    </div>
                </div>

                {# Statistics Cards #}
                <div class="row mb-4">
                    <div class="col-md-4 mb-3">
                        {% embed '_components/_card.html.twig' with {class: 'h-100'} %}
                            {% block card_body %}
                                <div class="text-center">
                                    <div class="display-4 mb-2 text-primary">{{ stats.total }}</div>
                                    <div class="h6 mb-0 fw-semibold">Pakete Gesamt</div>
                                    <small class="text-muted">
                                        {% if stats.generated_at %}
                                            Stand: {{ stats.generated_at }}
                                        {% endif %}
                                    </small>
                                </div>
                            {% endblock %}
                        {% endembed %}
                    </div>

                    <div class="col-md-4 mb-3">
                        {% embed '_components/_card.html.twig' with {class: 'h-100', borderColor: 'success'} %}
                            {% block card_body %}
                                <div class="text-center">
                                    <div class="display-4 mb-2 text-success">{{ stats.allowed }}</div>
                                    <div class="h6 mb-0 fw-semibold">Erlaubt</div>
                                    <small class="text-muted">{{ (stats.allowed / stats.total * 100)|round(1) }}%</small>
                                </div>
                            {% endblock %}
                        {% endembed %}
                    </div>

                    <div class="col-md-4 mb-3">
                        {% embed '_components/_card.html.twig' with {class: 'h-100', borderColor: (stats.unknown > 0 or stats.not_allowed > 0) ? 'warning' : ''} %}
                            {% block card_body %}
                                <div class="text-center">
                                    <div class="display-4 mb-2 {% if stats.unknown > 0 or stats.not_allowed > 0 %}text-warning{% else %}text-muted{% endif %}">
                                        {{ stats.restricted + stats.copyleft + stats.not_allowed + stats.unknown }}
                                    </div>
                                    <div class="h6 mb-0 fw-semibold">Prüfung erforderlich</div>
                                    <small class="text-muted">
                                        {{ ((stats.restricted + stats.copyleft + stats.not_allowed + stats.unknown) / stats.total * 100)|round(1) }}%
                                    </small>
                                </div>
                            {% endblock %}
                        {% endembed %}
                    </div>
                </div>

                {# Detailed Breakdown #}
                {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
                    {% block card_header %}
                        <div class="card-header">
                            <h2 class="h5 mb-0">
                                <i class="fa-icon fa-icon--nav-pie-chart me-2" aria-hidden="true"></i>Lizenzverteilung
                            </h2>
                        </div>
                    {% endblock %}
                    {% block card_body %}
                        {% embed '_components/_table.html.twig' with {
                            'class': 'table-sm',
                            'responsive': true,
                            'noMargin': true
                        } %}
                            {% block table_head %}
                                <tr>
                                    <th scope="col">Status</th>
                                    <th scope="col">Beschreibung</th>
                                    <th scope="col" class="text-end">Anzahl</th>
                                    <th scope="col" class="text-end">Anteil</th>
                                    <th scope="col" class="w-200px">Verteilung</th>
                                </tr>
                            {% endblock %}
                            {% block table_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                                <tr>
                                    <td>{% include '_components/_badge.html.twig' with { variant: 'success', icon: 'status-ok', content: 'Erlaubt' } %}</td>
                                    <td><small>Permissive Lizenzen (MIT, BSD, Apache)</small></td>
                                    <td class="text-end"><strong>{{ stats.allowed }}</strong></td>
                                    <td class="text-end">{{ (stats.allowed / stats.total * 100)|round(1) }}%</td>
                                    <td>
                                        {{ _fa_progress.render({ value: stats.allowed, max: stats.total, tone: 'success' }) }}
                                    </td>
                                </tr>
                                <tr>
                                    <td>{% include '_components/_badge.html.twig' with { variant: 'warning', icon: 'status-warning', content: 'Eingeschränkt' } %}</td>
                                    <td><small>Weak copyleft (MPL, EPL, CC-BY)</small></td>
                                    <td class="text-end"><strong>{{ stats.restricted }}</strong></td>
                                    <td class="text-end">{{ (stats.restricted / stats.total * 100)|round(1) }}%</td>
                                    <td>
                                        {{ _fa_progress.render({ value: stats.restricted, max: stats.total, tone: 'warning' }) }}
                                    </td>
                                </tr>
                                <tr>
                                    <td>{% include '_components/_badge.html.twig' with { variant: 'info', icon: 'util-refresh', content: 'Copyleft' } %}</td>
                                    <td><small>Strong copyleft (GPL, LGPL, AGPL)</small></td>
                                    <td class="text-end"><strong>{{ stats.copyleft }}</strong></td>
                                    <td class="text-end">{{ (stats.copyleft / stats.total * 100)|round(1) }}%</td>
                                    <td>
                                        {{ _fa_progress.render({ value: stats.copyleft, max: stats.total, tone: 'info' }) }}
                                    </td>
                                </tr>
                                <tr>
                                    <td>{% include '_components/_badge.html.twig' with { variant: 'danger', icon: 'status-critical', content: 'Nicht erlaubt' } %}</td>
                                    <td><small>Non-commercial Lizenzen</small></td>
                                    <td class="text-end"><strong>{{ stats.not_allowed }}</strong></td>
                                    <td class="text-end">{{ (stats.not_allowed / stats.total * 100)|round(1) }}%</td>
                                    <td>
                                        {{ _fa_progress.render({ value: stats.not_allowed, max: stats.total, tone: 'danger' }) }}
                                    </td>
                                </tr>
                                <tr>
                                    <td>{% include '_components/_badge.html.twig' with { variant: 'secondary', content: 'Unbekannt' } %}</td>
                                    <td><small>Manuelle Prüfung erforderlich</small></td>
                                    <td class="text-end"><strong>{{ stats.unknown }}</strong></td>
                                    <td class="text-end">{{ (stats.unknown / stats.total * 100)|round(1) }}%</td>
                                    <td>
                                        {{ _fa_progress.render({ value: stats.unknown, max: stats.total, tone: 'neutral' }) }}
                                    </td>
                                </tr>
                            {% endblock %}
                        {% endembed %}
                    {% endblock %}
                {% endembed %}

                {# Actions Required #}
                {% if stats.unknown > 0 or stats.not_allowed > 0 %}
                    {% embed '_components/_card.html.twig' with {class: 'mb-4', borderColor: 'warning'} %}
                        {% block card_header %}
                            <div class="card-header">
                                <h2 class="h5 mb-0">
                                    <i class="fa-icon fa-icon--status-warning text-warning me-2" aria-hidden="true"></i>Handlungsbedarf
                                </h2>
                            </div>
                        {% endblock %}
                        {% block card_body %}
                            <ul class="mb-0">
                                {% if stats.unknown > 0 %}
                                    <li><strong>{{ stats.unknown }}</strong> Pakete mit unbekannter Lizenz müssen manuell geprüft werden</li>
                                {% endif %}
                                {% if stats.not_allowed > 0 %}
                                    <li><strong>{{ stats.not_allowed }}</strong> Pakete mit nicht-kommerziellen Lizenzen müssen ersetzt werden</li>
                                {% endif %}
                                {% if stats.copyleft > 0 %}
                                    <li><strong>{{ stats.copyleft }}</strong> Copyleft-Pakete erfordern Quelloffenlegung</li>
                                {% endif %}
                            </ul>
                            <div class="mt-3">
                                <a href="{{ path('app_licenses_report') }}" class="fa-cyber-btn fa-cyber-btn--warning">
                                    <i class="fa-icon fa-icon--nav-reports me-1" aria-hidden="true"></i>Detailbericht anzeigen
                                </a>
                            </div>
                        {% endblock %}
                    {% endembed %}
                {% endif %}

                {# Next Steps #}
                {% embed '_components/_card.html.twig' %}
                    {% block card_header %}
                        <div class="card-header">
                            <h2 class="h5 mb-0">
                                <i class="fa-icon fa-icon--nav-list-check me-2" aria-hidden="true"></i>Nächste Schritte
                            </h2>
                        </div>
                    {% endblock %}
                    {% block card_body %}
                        <ol class="mb-0">
                            <li>Vollständigen Bericht prüfen: <a href="{{ path('app_licenses_report') }}">Detailbericht öffnen</a></li>
                            <li>Problematische Lizenzen identifizieren und dokumentieren</li>
                            <li>NOTICE-Datei prüfen: <a href="{{ path('app_licenses') }}">NOTICE anzeigen</a></li>
                            <li>{{ 'admin.licensing.attributions_provided'|trans({}, 'admin') }}</li>
                            <li>Regelmäßig aktualisieren: <code>./license-report.sh</code> ausführen</li>
                        </ol>
                    {% endblock %}
                {% endembed %}

            {% else %}
                {# No Report Found #}
                {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'warning' } } %}
                    {% block alert_body %}
                    <div class="d-flex align-items-center">
                        <div class="flex-shrink-0">
                            <i class="fa-icon fa-icon--status-warning fs-3 me-3" aria-hidden="true"></i>
                        </div>
                        <div>
                            <div class="fa-alert__title">Kein Bericht gefunden</div>
                            <p class="mb-3">
                                Der Lizenzbericht wurde noch nicht generiert. Bitte führen Sie das folgende Kommando aus:
                            </p>
                            <code>./license-report.sh</code>
                            <p class="mt-3 mb-0">
                                <a href="{{ path('app_licenses_report') }}" class="fa-cyber-btn fa-cyber-btn--warning">
                                    <i class="fa-icon fa-icon--ui-refresh me-1" aria-hidden="true"></i>Bericht generieren
                                </a>
                            </p>
                        </div>
                    </div>
                    {% endblock %}
                {% endembed %}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
