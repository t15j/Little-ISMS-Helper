{% extends 'admin/layout.html.twig' %}
{% trans_default_domain 'user' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}{{ 'user.import.title'|trans({}, 'user') }}{% endblock %}

{% block admin_content %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="fa-icon fa-icon--nav-upload" aria-hidden="true"></i> {{ 'user.import.title'|trans({}, 'user') }}</h1>
        <a href="{{ path('user_management_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'common.back'|trans({}, 'messages') }}
        </a>
    </div>

    {# Instructions #}
    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', class: 'mb-4' } } %}
        {% block alert_body %}
        <div class="fa-alert__title">{{ 'user.import.description'|trans({}, 'user') }}</div>
        <p class="mb-2">{{ 'user.import.format'|trans({}, 'user') }}</p>
        <code>email, first_name, last_name, password, is_active, roles</code>
        <hr>
        <strong>{{ 'user.import.example_csv'|trans({}, 'user') }}</strong>
        <pre class="mb-0">email,first_name,last_name,password,is_active,roles
john.doe@example.com,John,Doe,SecurePass123,yes,"Admin,User"
jane.smith@example.com,Jane,Smith,SecurePass456,yes,"User"</pre>
        {% endblock %}
    {% endembed %}

    {# Import Form #}
    {% embed '_components/_card.html.twig' %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--nav-upload" aria-hidden="true"></i> {{ 'user.import.upload_title'|trans({}, 'user') }}
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <form method="post" enctype="multipart/form-data">
                <input type="hidden" name="_token" value="{{ csrf_token('user_import') }}">
                <div class="mb-3">
                    <label for="import_file" class="form-label">{{ 'user.import.file_label'|trans({}, 'user') }}</label>
                    <input type="file"
                           class="form-control fa-cyber-input__field"
                           id="import_file"
                           name="import_file"
                           accept=".csv"
                           required>
                    <div class="form-text">
                        {{ 'user.import.file_help'|trans({}, 'user') }}
                    </div>
                </div>

                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                    <a href="{{ path('user_management_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                        <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> {{ 'common.cancel'|trans({}, 'messages') }}
                    </a>
                    <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                        <i class="fa-icon fa-icon--nav-upload" aria-hidden="true"></i> {{ 'user.import.submit'|trans({}, 'user') }}
                    </button>
                </div>
            </form>
        {% endblock %}
    {% endembed %}

    {# CSV Template Download #}
    {% embed '_components/_card.html.twig' with { 'class': 'mt-4' } %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--nav-download" aria-hidden="true"></i> {{ 'user.import.template_title'|trans({}, 'user') }}
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <p>{{ 'user.import.template_description'|trans({}, 'user') }}</p>
            <a href="data:text/csv;charset=utf-8,email%2Cfirst_name%2Clast_name%2Cpassword%2Cis_active%2Croles%0A"
               download="user_import_template.csv"
               class="fa-cyber-btn fa-cyber-btn--ghost">
                <i class="fa-icon fa-icon--nav-file-check" aria-hidden="true"></i> {{ 'user.import.template_download'|trans({}, 'user') }}
            </a>
        {% endblock %}
    {% endembed %}

    {# Important Notes #}
    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'warning', class: 'mt-4' } } %}
        {% block alert_body %}
        <div class="fa-alert__title">{{ 'user.import.notes_title'|trans({}, 'user') }}</div>
        <ul class="mb-0">
            <li>{{ 'user.import.note_existing_email'|trans({}, 'user') }}</li>
            <li>{{ 'user.import.note_random_password'|trans({}, 'user') }}</li>
            <li>{{ 'user.import.note_role_case'|trans({}, 'user') }}</li>
            <li>{{ 'user_management.import_irreversible'|trans({}, 'user') }}</li>
            <li>{{ 'user.import.note_default_role'|trans({}, 'user') }}</li>
        </ul>
        {% endblock %}
    {% endembed %}
</div>
{% endblock admin_content %}
