{% extends 'admin/layout.html.twig' %}
{% trans_default_domain 'user' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_tabs.html.twig' as _fa_tabs %}

{% block title %}{{ 'user.new'|trans({}, 'user') }}{% endblock %}

{% block admin_content %}
<div class="container-fluid">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.admin'|trans({}, 'nav'), url: path('admin_dashboard') },
            { label: 'nav.users'|trans({}, 'nav'), url: path('user_management_index') },
            { label: 'user.new'|trans({}, 'user') }
        ]
    } %}

    {{ _fa_page_header.render({
        title: ('user.new'|trans({}, 'user')),
        badge: { icon: 'plus', label: '' },
        actions: [
            { label: 'common.back'|trans({}, 'messages'), href: path('user_management_index'), variant: 'ghost', icon: 'util-arrow-left' }
        ]
    }) }}

    <div class="row">
        <div class="col-md-10 offset-md-1">
            {# Role Help Component - explains system vs custom roles #}
            {{ include('_components/_role_help.html.twig') }}

            <section class="fa-section">
                <div class="fa-section__header">
                    <h2 class="fa-section__title">{{ 'user.create'|trans({}, 'user') }}</h2>
                </div>
                <div class="fa-section__body">
                    {{ form_start(form) }}

                    {{ _fa_tabs.render({
                        id: 'user-new-settings',
                        urlHashSync: true,
                        initialTab: 'tab-profile',
                        tabs: [
                            {
                                id: 'tab-profile',
                                label: 'user.tab.profile'|trans({}, 'user'),
                                icon: 'ui-person',
                                body: include('user_management/tabs/_tab_profile.html.twig', { form: form })
                            },
                            {
                                id: 'tab-access',
                                label: 'user.tab.access'|trans({}, 'user'),
                                icon: 'nav-shield-lock',
                                body: include('user_management/tabs/_tab_access.html.twig', { form: form })
                            },
                            {
                                id: 'tab-security',
                                label: 'user.tab.security'|trans({}, 'user'),
                                icon: 'ui-lock',
                                body: include('user_management/tabs/_tab_security.html.twig', { form: form })
                            }
                        ]
                    }) }}

                    {# CSRF token — outside tab panels so it is always submitted #}
                    {% if form._token is defined %}
                        {{ form_row(form._token) }}
                    {% endif %}

                    {# Form Actions #}
                    <div class="form-actions d-flex gap-2 justify-content-between mt-4">
                        <a href="{{ path('user_management_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i>
                            {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--ui-save" aria-hidden="true"></i>
                            {{ 'user.create'|trans({}, 'user') }}
                        </button>
                    </div>

                    {{ form_end(form, {render_rest: false}) }}
                </div>
            </section>
        </div>
    </div>
</div>
{% endblock admin_content %}

{% block javascripts %}
{{ parent() }}
<script>
    // Turbo re-eval guard: wrap in IIFE so top-level declarations are not re-declared on body-swap
    (function () {
    // Initialize Bootstrap tooltips for role checkboxes
    function initUserNewTooltips() {
        const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]');
        tooltipTriggerList.forEach(function(el) {
            new bootstrap.Tooltip(el);
        });
    }
    document.addEventListener('DOMContentLoaded', initUserNewTooltips);
    document.addEventListener('turbo:load', initUserNewTooltips);
    })();
</script>
{% endblock %}
