{% extends 'base.html.twig' %}
{% import '_components/_fa_alva_hint.html.twig' as _fa_alva_hint %}
{% trans_default_domain 'privacy' %}

{% block title %}{{ 'privacy.data_breach.management'|trans({}, 'privacy') }}{% endblock %}

{% block body %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
<div class="container-fluid mt-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'privacy.breadcrumb.privacy'|trans({}, 'privacy'), icon: 'nav-shield-lock' },
            { label: 'privacy.breadcrumb.data_breaches'|trans({}, 'privacy') }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'status-warning', label: 'privacy.data_breach.management'|trans({}, 'privacy') },
        title: 'privacy.data_breach.management'|trans({}, 'privacy'),
        subtitle: 'privacy.data_breach.subtitle'|trans({}, 'privacy'),
        actions: [
            { label: 'privacy.data_breach.actions.dashboard'|trans({}, 'privacy'), href: path('app_data_breach_dashboard'), variant: 'ghost', icon: 'nav-dashboard' },
            { label: 'privacy.data_breach.actions.new'|trans({}, 'privacy'), href: path('app_data_breach_new'), variant: 'danger', icon: 'plus' }
        ]
    }) }}
{% set _global_hints = alva_hints('data_breach_index') %}
{% if _global_hints|length > 0 %}
    {% for _h in _global_hints %}
        {{ _fa_alva_hint.render(_h) }}
    {% endfor %}
{% endif %}

    <main class="fa-aurora-surface">

    {# CRITICAL: Overdue Notifications Alert #}
    {% if compliance_score.overdue_notifications > 0 %}
        {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'danger', dismissible: true } } %}
            {% block alert_body %}
                <div class="fa-alert__title">{{ 'privacy.data_breach.alert.overdue_title'|trans({}, 'privacy') }}</div>
                <p class="fa-alert__message">{{ 'privacy.data_breach.alert.overdue_text'|trans({'%count%': compliance_score.overdue_notifications}, 'privacy')|raw }}</p>
                <hr>
                <p class="mb-0">
                    <a href="{{ path('app_data_breach_index', {filter: 'overdue'}) }}" class="fa-alert__link">
                        {{ 'privacy.data_breach.alert.view_overdue'|trans({}, 'privacy') }}
                    </a>
                </p>
            {% endblock %}
        {% endembed %}
    {% endif %}

    {# Overall Compliance Score #}
    {% embed '_components/_card.html.twig' with {class: 'mb-4 text-center'} %}
        {% block card_body %}
            <div class="small fw-semibold text-muted mb-2">{{ 'privacy.data_breach.score.heading'|trans({}, 'privacy') }}</div>
            {% set score = compliance_score.overall_score %}
            <h1 class="display-1 mb-0
                {% if score >= 80 %}text-success
                {% elseif score >= 60 %}text-warning
                {% else %}text-danger{% endif %}">
                {{ score }}%
            </h1>
            <div class="row mt-4">
                <div class="col-md-4">
                    <small class="text-muted">{{ 'privacy.data_breach.score.notification_compliance'|trans({}, 'privacy') }}</small>
                    <h4>{{ compliance_score.notification_compliance }}%</h4>
                </div>
                <div class="col-md-4">
                    <small class="text-muted">{{ 'privacy.data_breach.score.timeliness'|trans({}, 'privacy') }}</small>
                    <h4 class="{% if compliance_score.timeliness_score < 80 %}text-danger{% else %}text-success{% endif %}">
                        {{ compliance_score.timeliness_score }}%
                    </h4>
                </div>
                <div class="col-md-4">
                    <small class="text-muted">{{ 'privacy.data_breach.score.completeness'|trans({}, 'privacy') }}</small>
                    <h4>{{ compliance_score.completeness_score }}%</h4>
                </div>
            </div>
            <p class="text-muted mt-3">
                {{ 'privacy.data_breach.score.summary'|trans({'%total%': statistics.total, '%subjects%': compliance_score.total_affected_subjects}, 'privacy') }}
            </p>
        {% endblock %}
    {% endembed %}

    {# Key Metrics #}
    <div class="row mb-4">
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    <div class="text-muted small">{{ 'privacy.data_breach.kpi.total_breaches'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold">{{ statistics.total }}</div>
                    <div class="mt-2">
                        <small class="text-danger"><i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ statistics.high_risk + statistics.critical_risk }} {{ 'privacy.data_breach.kpi.high_critical_risk'|trans({}, 'privacy') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' with {variant: 'bordered', borderColor: 'warning'} %}
                {% block card_body %}
                    <div class="text-muted small">{{ 'privacy.data_breach.kpi.pending_authority'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold text-warning">{{ statistics.requires_authority_notification - statistics.authority_notified }}</div>
                    <div class="mt-2">
                        <small class="text-muted"><i class="fa-icon fa-icon--ui-clock" aria-hidden="true"></i> {{ 'privacy.data_breach.kpi.deadline_72h'|trans({}, 'privacy') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    <div class="text-muted small">{{ 'privacy.data_breach.kpi.special_categories'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold text-info">{{ statistics.special_categories_affected }}</div>
                    <div class="mt-2">
                        <small class="text-muted"><i class="fa-icon fa-icon--control" aria-hidden="true"></i> {{ 'privacy.data_breach.kpi.art9_data'|trans({}, 'privacy') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    <div class="text-muted small">{{ 'data_breach.index.kpi_affected_subjects'|trans({}, 'messages') }}</div>
                    <div class="fs-2 fw-bold">{{ compliance_score.total_affected_subjects }}</div>
                    <div class="mt-2">
                        <small class="text-muted"><i class="fa-icon fa-icon--group" aria-hidden="true"></i> {{ 'data_breach.index.kpi_total_count'|trans({}, 'messages') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Filters #}
    {% embed '_components/_search_filter_form.html.twig' with {
        action: path('app_data_breach_index'),
        filter_keys: ['filter'],
        show_search: false,
        form_id: 'data-breach-filter-form'
    } %}
        {% block filters %}
            <div class="col-md-4">
                <label for="filter-status" class="form-label">{{ 'data_breach.index.filter_title'|trans({}, 'messages') }}</label>
                <select class="form-select fa-cyber-input__field" id="filter-status" name="filter">
                    <option value="">{{ 'data_breach.index.filter_all'|trans({}, 'messages') }} ({{ statistics.total }})</option>
                    <option value="overdue" {{ current_filter == 'overdue' ? 'selected' : '' }}>{{ 'data_breach.index.filter_overdue'|trans({}, 'messages') }} ({{ compliance_score.overdue_notifications }})</option>
                    <option value="pending_authority" {{ current_filter == 'pending_authority' ? 'selected' : '' }}>{{ 'data_breach.index.filter_pending_authority'|trans({}, 'messages') }}</option>
                    <option value="pending_subjects" {{ current_filter == 'pending_subjects' ? 'selected' : '' }}>{{ 'data_breach.index.filter_pending_subjects'|trans({}, 'messages') }}</option>
                    <option value="draft" {{ current_filter == 'draft' ? 'selected' : '' }}>{{ 'data_breach.index.filter_draft'|trans({}, 'messages') }} ({{ statistics.draft }})</option>
                    <option value="under_assessment" {{ current_filter == 'under_assessment' ? 'selected' : '' }}>{{ 'data_breach.index.filter_under_assessment'|trans({}, 'messages') }} ({{ statistics.under_assessment }})</option>
                    <option value="authority_notified" {{ current_filter == 'authority_notified' ? 'selected' : '' }}>{{ 'data_breach.index.filter_authority_notified'|trans({}, 'messages') }} ({{ statistics.authority_notified }})</option>
                    <option value="closed" {{ current_filter == 'closed' ? 'selected' : '' }}>{{ 'data_breach.index.filter_closed'|trans({}, 'messages') }} ({{ statistics.closed }})</option>
                </select>
            </div>
        {% endblock %}
    {% endembed %}

    {# Breaches Table #}
    {% embed '_components/_card.html.twig' with {noPadding: true} %}
        {% block card_attrs %}data-controller="bulk-actions" data-bulk-actions-endpoint-value="/{{ app.request.locale }}/data-breach"{% endblock %}
        {% block card_header %}
            <div class="card-header"><h2 class="mb-0"><i class="fa-icon fa-icon--nav-list-check" aria-hidden="true"></i> {{ 'data_breach.index.table_title'|trans({}, 'messages') }} ({{ breaches|length }})</h2></div>
        {% endblock %}
        {% block card_body %}
            {% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
            {% if breaches|length > 0 %}
                {% embed '_components/_table.html.twig' with {
                    'noMargin': true
                } %}
                    {% block table_head %}
                        <tr>
                            <th scope="col" class="bulk-select-column">
                                <input type="checkbox"
                                       class="form-check-input"
                                       data-action="bulk-actions#selectAll"
                                       data-bulk-actions-target="selectAllCheckbox"
                                       aria-label="{{ 'bulk_actions.select_all'|trans({}, 'messages') }}">
                            </th>
                            <th scope="col">{{ 'data_breach.index.col_reference'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_title'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_status'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_risk'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_affected_subjects'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_deadline'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_authority'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_subjects'|trans({}, 'messages') }}</th>
                            <th scope="col">{{ 'data_breach.index.col_actions'|trans({}, 'messages') }}</th>
                        </tr>
                    {% endblock %}
                    {% block table_body %}
                        {% for breach in breaches %}
                            <tr class="{% if breach.isAuthorityNotificationOverdue %}table-danger{% endif %}">
                                <td class="bulk-select-column">
                                    <input type="checkbox"
                                           class="form-check-input"
                                           data-bulk-actions-target="item"
                                           data-action="bulk-actions#selectItem"
                                           value="{{ breach.id }}"
                                           aria-label="{{ 'common.select'|trans({}, 'messages') }} {{ breach.referenceNumber }}">
                                </td>
                                <td>
                                    <a href="{{ path('app_data_breach_show', {id: breach.id}) }}">
                                        {{ breach.referenceNumber }}
                                    </a>
                                </td>
                                <td>{{ breach.title }}</td>
                                <td>
                                    {% set status_class = {
                                        'draft': 'secondary',
                                        'under_assessment': 'info',
                                        'authority_notified': 'success',
                                        'subjects_notified': 'success',
                                        'closed': 'dark'
                                    }[breach.status] %}
                                    {% include '_components/_badge.html.twig' with { variant: status_class, content: ('privacy.data_breach.status.' ~ breach.status)|trans({}, 'privacy') } %}
                                </td>
                                <td>
                                    {% if breach.riskLevel %}
                                        {% set risk_class = {
                                            'low': 'success',
                                            'medium': 'info',
                                            'high': 'warning',
                                            'critical': 'danger'
                                        }[breach.riskLevel] %}
                                        {% include '_components/_badge.html.twig' with { variant: risk_class, content: ('privacy.data_breach.risk.' ~ breach.riskLevel)|trans({}, 'privacy') } %}
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if breach.affectedDataSubjects %}
                                        {{ breach.affectedDataSubjects|number_format }}
                                    {% else %}
                                        <span class="text-muted">{{ 'data_breach.index.unknown'|trans({}, 'messages') }}</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if breach.requiresAuthorityNotification and not breach.supervisoryAuthorityNotifiedAt %}
                                        {% set hours = breach.hoursUntilAuthorityDeadline %}
                                        {% if hours < 0 %}
                                            {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'data_breach.index.overdue'|trans({}, 'messages') ~ ' ' ~ (hours * -1)|number_format ~ 'h' } %}
                                        {% elseif hours < 24 %}
                                            {% include '_components/_badge.html.twig' with { variant: 'warning', content: 'data_breach.index.hours_left'|trans({'%hours%': hours|number_format}, 'messages') } %}
                                        {% else %}
                                            {% include '_components/_badge.html.twig' with { variant: 'info', content: 'data_breach.index.hours_left'|trans({'%hours%': hours|number_format}, 'messages') } %}
                                        {% endif %}
                                    {% elseif breach.supervisoryAuthorityNotifiedAt %}
                                        <i class="fa-icon fa-icon--status-ok" aria-hidden="true" title="{{ 'data_breach.notified_at'|trans({'%date%': breach.supervisoryAuthorityNotifiedAt|date('Y-m-d H:i')}, 'privacy') }}"></i>
                                    {% else %}
                                        <span class="text-muted">{{ 'data_breach.not_required'|trans({}, 'privacy') }}</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if breach.supervisoryAuthorityNotifiedAt %}
                                        <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                                    {% elseif breach.requiresAuthorityNotification %}
                                        <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i>
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if breach.dataSubjectsNotifiedAt %}
                                        <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                                    {% elseif breach.requiresSubjectNotification %}
                                        <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i>
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <div class="btn-group btn-group-sm" role="group" aria-label="{{ 'common.actions'|trans({}, 'messages') }}">
                                        <a href="{{ path('app_data_breach_show', {id: breach.id}) }}"
                                           class="fa-cyber-btn fa-cyber-btn--ghost"
                                           title="{{ 'common.view'|trans({}, 'messages') }}" aria-label="{{ 'common.view'|trans({}, 'messages') }}">
                                            <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i>
                                        </a>
                                        {% if breach.status in ['draft', 'under_assessment'] %}
                                            <a href="{{ path('app_data_breach_edit', {id: breach.id}) }}"
                                               class="fa-cyber-btn fa-cyber-btn--ghost"
                                               title="{{ 'common.edit'|trans({}, 'messages') }}" aria-label="{{ 'common.edit'|trans({}, 'messages') }}">
                                                <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i>
                                            </a>
                                        {% endif %}
                                        <a href="{{ path('app_data_breach_export_pdf', {id: breach.id}) }}"
                                           class="fa-cyber-btn fa-cyber-btn--success"
                                           title="{{ 'common.export_pdf'|trans({}, 'messages') }}" aria-label="{{ 'common.export_pdf'|trans({}, 'messages') }}">
                                            <i class="fa-icon fa-icon--nav-file-pdf" aria-hidden="true"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                    {% endblock %}
                {% endembed %}

                {# Bulk Action Bar #}
                {% include '_components/_bulk_action_bar.html.twig' with {
                    actions: ['export', 'delete']
                } %}
            {% else %}
                {{ _fa_empty_state.render({
                    mood: current_filter != 'all' ? 'curious' : 'celebrating',
                    title: 'data_breach.index.no_breaches_found'|trans({}, 'privacy'),
                    actions: current_filter != 'all' ? [
                        { label: 'data_breach.index.view_all_breaches'|trans({}, 'privacy'), variant: 'ghost', icon: 'ui-list', href: path('app_data_breach_index') }
                    ] : []
                }) }}
            {% endif %}
        {% endblock %}
    {% endembed %}
    </main>
</div>
{% endblock %}
