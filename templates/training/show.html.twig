{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% trans_default_domain 'training' %}

{% block title %}{{ training.title }} - {{ parent() }}{% endblock %}

{% block body %}
{% import '_components/_fa_alva_hint.html.twig' as _fa_alva_hint %}
<div class="container-fluid py-4">
    {# M-4 — module-gating: Training & Awareness (ISO 27001 A.6.3) #}
    {% if not is_module_active('training') %}
        {{ _fa_alert.render({
            variant: 'info',
            icon: 'nav-shield-alert',
            body: 'common.module_not_active'|trans({'%module%': 'Training'}, 'messages')
        }) }}
    {% else %}
    {# Lifecycle Y.5 — status transition dropdown (training_lifecycle) #}
    {% include '_components/_lifecycle_toolbar.html.twig' with {
        entity: training,
        entity_type: 'training',
        workflow_name: 'training_lifecycle'
    } only %}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.training'|trans({}, 'nav'), url: path('app_training_index') },
            { label: training.title }
        ]
    } %}

    {% set hint = alva_hint(training) %}
    {% if hint %}{{ _fa_alva_hint.render(hint) }}{% endif %}

    {{ _fa_page_header.render({
        badge: { icon: 'nav-mortarboard', label: 'nav.training'|trans({}, 'nav') },
        title: training.title,
        subtitle: ('training.status.' ~ training.status)|trans({}, 'training')
    }) }}

    <div class="row">
        <div class="col-lg-8">
            {# Main Training Information #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--training" aria-hidden="true"></i> {{ 'training.section.details'|trans({}, 'training') }}
                        </h2>
                        <span class="{{ badge_status(training.status) }} fs-6">
                            {{ ('training.status.' ~ training.status)|trans({}, 'training') }}
                        </span>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {# Mandatory Warning Banner #}
                    {% if training.mandatory %}
                    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'warning', icon: 'status-warning', class: 'mb-4' } } %}
                        {% block alert_body %}<div class="alert-heading fw-bold"> <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ 'training.mandatory_training'|trans({}, 'training')}} </div> <p class="mb-0">{{ 'training.mandatory_info'|trans({}, 'training')}}</p>{% endblock %}
                    {% endembed %}
                    {% endif %}

                    {# Description #}
                    {% if training.description %}
                    <div class="mb-4">
                        <h2><i class="fa-icon fa-icon--policy" aria-hidden="true"></i> {{ 'training.description'|trans({}, 'training')}}</h2>
                        <p class="text-muted">{{ training.description|nl2br }}</p>
                    </div>
                    <hr>
                    {% endif %}

                    {# Training Details #}
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <div class="text-muted mb-2 fw-semibold">{{ 'training.type_label'|trans({}, 'training')}}</div>
                            <p>
                                {% include '_components/_badge.html.twig' with { variant: 'secondary', content: ('training.type.' ~ training.trainingType)|trans({}, 'training') } %}
                            </p>
                        </div>
                        <div class="col-md-6">
                            <div class="text-muted mb-2 fw-semibold">{{ 'training.delivery_method'|trans({}, 'training')}}</div>
                            <p>
                                {% if training.deliveryMethod %}
                                {% include '_components/_badge.html.twig' with { variant: 'info', content: ('training.delivery.' ~ training.deliveryMethod)|trans({}, 'training') } %}
                                {% else %}
                                <span class="text-muted">-</span>
                                {% endif %}
                            </p>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <div class="text-muted mb-2 fw-semibold">
                                <i class="fa-icon fa-icon--ui-calendar-event" aria-hidden="true"></i> {{ 'training.scheduled_date'|trans({}, 'training')}}
                            </div>
                            <p class="fs-5">{{ training.scheduledDate|date('d.m.Y') }}</p>
                        </div>
                        <div class="col-md-6">
                            <div class="text-muted mb-2 fw-semibold">
                                <i class="fa-icon fa-icon--status-pending" aria-hidden="true"></i> {{ 'training.duration'|trans({}, 'training')}}
                            </div>
                            <p class="fs-5">{{ training.durationMinutes ?? '-' }} {{ 'training.minutes'|trans({}, 'training')}}</p>
                        </div>
                    </div>

                    {% if training.targetAudience %}
                    <div class="mb-3">
                        <div class="text-muted mb-2 fw-semibold">
                            <i class="fa-icon fa-icon--nav-target" aria-hidden="true"></i> {{ 'training.target_audience'|trans({}, 'training')}}
                        </div>
                        <p>{{ training.targetAudience }}</p>
                    </div>
                    {% endif %}

                    {% if training.trainer %}
                    <div class="mb-3">
                        <div class="text-muted mb-2 fw-semibold">
                            <i class="fa-icon fa-icon--role" aria-hidden="true"></i> {{ 'training.trainer'|trans({}, 'training')}}
                        </div>
                        <p>{{ training.effectiveTrainer }}</p>
                    </div>
                    {% endif %}
                {% endblock %}
            {% endembed %}

            {# Participants Section #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--group" aria-hidden="true"></i> {{ 'training.participants'|trans({}, 'training')}}
                            {% include '_components/_badge.html.twig' with { variant: 'primary', content: training.attendeeCount ?? 0 } %}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% if training.participants %}
                    <p>{{ training.participants|nl2br }}</p>
                    {% else %}
                    <p class="text-muted mb-0">
                        <i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i> {{ 'training.no_participants'|trans({}, 'training')}}
                    </p>
                    {% endif %}
                {% endblock %}
            {% endembed %}

            {# Materials Section — Junior-ISB-Audit-2026-05-22 9.5: structured file uploads + legacy free-text #}
            {% if training.materialFiles or training.materials %}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--policy" aria-hidden="true"></i> {{ 'training.materials'|trans({}, 'training')}}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% if training.materialFiles %}
                        <ul class="list-unstyled mb-0">
                            {% for file in training.materialFiles %}
                                <li class="mb-2">
                                    <a href="{{ asset('uploads/training-materials/' ~ file.filename) }}" target="_blank" rel="noopener">
                                        <i class="fa-icon fa-icon--nav-document" aria-hidden="true"></i>
                                        {{ file.originalName }}
                                    </a>
                                    <span class="text-muted small">
                                        ({{ (file.size / 1024)|round(1) }} KB
                                        {% if file.uploadedAt %} · {{ file.uploadedAt|slice(0, 10) }}{% endif %})
                                    </span>
                                </li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                    {% if training.materials %}
                        <details {% if training.materialFiles %}class="mt-3"{% endif %}>
                            <summary class="text-muted small">{{ 'training.field.materials_legacy'|trans({}, 'training') }}</summary>
                            <p class="mt-2 mb-0">{{ training.materials|nl2br }}</p>
                        </details>
                    {% endif %}
                {% endblock %}
            {% endembed %}
            {% endif %}

            {# Feedback Section #}
            {% if training.feedback %}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--review" aria-hidden="true"></i> {{ 'training.feedback'|trans({}, 'training')}}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <p>{{ training.feedback|nl2br }}</p>
                {% endblock %}
            {% endembed %}
            {% endif %}
        </div>

        <div class="col-lg-4">
            {# Action Buttons #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0"><i class="fa-icon fa-icon--nav-tools" aria-hidden="true"></i> {{ 'common.actions'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="d-grid gap-2">
                        {% if is_granted('ROLE_USER') %}
                        <a href="{{ path('app_training_edit', {id: training.id}) }}"
                           class="fa-cyber-btn fa-cyber-btn--primary"
                           data-turbo-action="advance">
                            <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i> {{ 'common.edit'|trans({}, 'messages') }}
                        </a>
                        <form method="post" action="{{ path('app_training_clone', {id: training.id}) }}" data-turbo="false">
                            <input type="hidden" name="_token" value="{{ csrf_token('clone_training_' ~ training.id) }}">
                            <button type="submit" class="fa-cyber-btn fa-cyber-btn--success w-100" title="{{ 'common.clone.help'|trans({}, 'messages') }}">
                                <i class="fa-icon fa-icon--ui-copy" aria-hidden="true"></i> {{ 'common.clone.label'|trans({}, 'messages') }}
                            </button>
                        </form>
                        {% endif %}

                        <a href="{{ path('app_training_index') }}"
                           class="fa-cyber-btn fa-cyber-btn--ghost"
                           data-turbo-action="advance">
                            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'common.back_to_list'|trans({}, 'messages') }}
                        </a>

                        {% if is_granted('ROLE_ADMIN') %}
                        <hr>
                        <button type="button"
                                class="fa-cyber-btn fa-cyber-btn--danger" data-controller="fa-modal-dispatcher" data-action="click->fa-modal-dispatcher#open" data-fa-modal-id="deleteModal">
                            <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                        </button>
                        {% endif %}
                    </div>
                {% endblock %}
            {% endembed %}

            {# Data Reuse: Covered Controls #}
            {% if training.coveredControls is defined and training.coveredControls|length > 0 %}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i> {{ 'training.covered_controls'|trans({}, 'training')}}
                            {% include '_components/_badge.html.twig' with { variant: 'primary', content: training.coveredControls|length } %}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="list-group list-group-flush">
                        {% for control in training.coveredControls %}
                        <div class="list-group-item px-0">
                            <div class="d-flex w-100 justify-content-between align-items-start">
                                <div>
                                    <div class="mb-1">{% include '_components/_iso_reference_label.html.twig' with { control: control, show_name: false, code_class: 'fw-bold' } only %}</div>
                                    <p class="mb-1 small text-muted">{{ control.name }}</p>
                                </div>
                                <span class="{{ badge_status(control.implementationStatus) }} rounded-pill">
                                    {{ control.implementationStatus|upper }}
                                </span>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                {% endblock %}
            {% endembed %}
            {% endif %}

            {# Data Reuse: Compliance Coverage #}
            {% if training.complianceRequirements is defined and training.complianceRequirements|length > 0 %}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--audit-internal" aria-hidden="true"></i> {{ 'training.compliance_coverage'|trans({}, 'training') }}
                            {% include '_components/_badge.html.twig' with { variant: 'success', content: training.complianceRequirements|length } %}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <p class="small text-muted mb-2">{{ 'training.compliance_coverage_info'|trans({}, 'training') }}</p>
                    <div class="d-flex flex-wrap gap-2">
                        {% for requirement in training.complianceRequirements %}
                            {% include '_components/_badge.html.twig' with { variant: 'info', content: (requirement.framework ? requirement.framework.name : 'N/A') ~ ' ' ~ requirement.requirementId } %}
                        {% endfor %}
                    </div>
                {% endblock %}
            {% endembed %}
            {% endif %}

            {# Data Reuse: Training Statistics #}
            {% if training.status == 'completed' %}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i> {{ 'training.statistics'|trans({}, 'training') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            <span class="small text-muted">{{ 'training.completion_rate'|trans({}, 'training') }}</span>
                            <span class="small fw-bold">100%</span>
                        </div>
                        {{ _fa_progress.render({ value: 100, tone: 'success' }) }}
                    </div>
                    <div class="d-flex justify-content-between small">
                        <span class="text-muted">{{ 'training.attendee_count'|trans({}, 'training') }}:</span>
                        <span class="fw-bold">{{ training.attendeeCount ?? 0 }}</span>
                    </div>
                    {% if training.completionDate %}
                    <div class="d-flex justify-content-between small mt-2">
                        <span class="text-muted">{{ 'training.completed_on'|trans({}, 'training') }}:</span>
                        <span class="fw-bold">{{ training.completionDate|date('d.m.Y') }}</span>
                    </div>
                    {% endif %}
                {% endblock %}
            {% endembed %}
            {% endif %}

            {# Metadata #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0"><i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i> {{ 'common.metadata'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="row mb-0 small">
                        <dt class="col-sm-5 text-muted">{{ 'common.created_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-7">{{ training.createdAt|date('d.m.Y H:i') }}</dd>

                        {% if training.updatedAt %}
                        <dt class="col-sm-5 text-muted">{{ 'common.updated_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-7">{{ training.updatedAt|date('d.m.Y H:i') }}</dd>
                        {% endif %}
                    </dl>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
    {% endif %} {# /M-4 training module gate #}
</div>

{# Delete Confirmation Modal #}
{% if is_granted('ROLE_ADMIN') %}
<div class="fa-modal fa-modal--confirm fa-modal--danger" id="deleteModal"
     data-controller="fa-modal" data-fa-modal-mode-value="confirm" data-fa-modal-tone-value="danger"
     role="alertdialog" aria-modal="true" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="fa-modal__backdrop" data-fa-modal-target="backdrop" data-action="click->fa-modal#backdropClick" aria-hidden="true"></div>
    <div class="fa-modal__container" data-fa-modal-target="dialog"><header class="fa-modal__header"><span class="fa-modal__icon" aria-hidden="true"><i class="fa-icon fa-icon--ui-trash"></i></span><div class="fa-modal__heading"><h2 class="fa-modal__title" id="deleteModalLabel">
                    {{ 'training.confirm_delete'|trans({}, 'training')}}
                </h2></div><button type="button" class="fa-modal__close" data-action="fa-modal#cancel" aria-label="Close"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i></button></header>
            <div class="fa-modal__body">
                <p>{{ 'training.delete_warning'|trans({'%title%': training.title}, 'training') }}</p>
                <p class="text-muted"><small>{{ 'common.action_irreversible'|trans({}, 'messages') }}</small></p>
            </div>
            <footer class="fa-modal__footer">
                <button type="button" class="fa-cyber-btn fa-cyber-btn--ghost" data-action="fa-modal#cancel">{{ 'common.cancel'|trans({}, 'messages') }}</button>
                <form method="post" action="{{ path('app_training_delete', {id: training.id}) }}" class="d-inline" data-fa-modal-target="form">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.id) }}">
                    <button type="submit" class="fa-cyber-btn fa-cyber-btn--danger">
                        <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                    </button>
                </form></div>
</div>
</div>
{% endif %}
{% endblock %}
