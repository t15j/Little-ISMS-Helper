{% trans_default_domain 'policy_wizard' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{# W4-C — empty-state for the Bestandsaufnahme step.

   Shown when the controller's `inventory_rows` is empty (greenfield
   tenant) — the step's isApplicable() guard would normally prevent this
   include, but if a brownfield tenant cleans up between page loads we
   render a graceful "you're greenfield, let's start fresh" panel and
   the user simply continues to Step 1. #}

{{ _fa_alert.render({
    variant: 'success',
    title: 'policy_wizard.step.bestandsaufnahme.empty.title'|trans,
    body: 'policy_wizard.step.bestandsaufnahme.empty.message'|trans
}) }}

<p class="small text-muted mt-2 mb-0">
    <i class="fa-icon fa-icon--util-arrow-right" aria-hidden="true"></i>
    {{ 'policy_wizard.step.bestandsaufnahme.empty.action_continue'|trans }}
</p>
