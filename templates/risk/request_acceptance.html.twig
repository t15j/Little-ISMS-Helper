{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}{{ 'risk.acceptance.title'|trans({}, 'risk') }} - {{ risk.title }} - {{ parent() }}{% endblock %}

{% block body %}
{# Breadcrumb Navigation #}
{% include '_components/_breadcrumb.html.twig' with {
    breadcrumbs: [
        { label: 'nav.risks'|trans({}, 'nav'), url: path('app_risk_index'), icon: 'status-warning' },
        { label: risk.title, url: path('app_risk_show', {id: risk.id}) },
        { label: 'risk.acceptance.title'|trans({}, 'risk') }
    ]
} %}

{# Page Header #}
{% include '_components/_page_header.html.twig' with {
    title: 'risk.acceptance.title'|trans({}, 'risk'),
    icon: 'status-ok',
    actions: [
        { label: 'common.back'|trans({}, 'messages'), url: path('app_risk_show', {id: risk.id}), variant: 'secondary', icon: 'util-arrow-left' }
    ]
} %}

{# ISO 27005:2022 Section 8.4.4 - Risk Acceptance #}
<div class="row">
    <div class="col-lg-8">
        {% embed '_components/_card.html.twig' %}
            {% block card_header %}
                <div class="card-header">
                    <h2 class="mb-0">
                        <i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i>
                        {{ 'risk.acceptance.request'|trans({}, 'risk') }}
                    </h2>
                </div>
            {% endblock %}
            {% block card_body %}
                {# Risk Summary #}
                <div class="fa-alert fa-alert--info mb-4" role="alert">
                    <i class="fa-icon fa-icon--status-info" fa-alert__icon" aria-hidden="true"></i>
                    <div class="fa-alert__body"><div class="alert-heading fw-bold">
                        <i class="fa-icon fa-icon--status-info" aria-hidden="true"></i>
                        {{ risk.title }}
                    </div>
                    <hr>
                    <div class="row">
                        <div class="col-md-6">
                            <strong>{{ 'risk.field.inherent_risk'|trans({}, 'risk') }}:</strong>
                            {{ risk.inherentRiskLevel }}
                            ({{ risk.probability }} × {{ risk.impact }})
                        </div>
                        <div class="col-md-6">
                            <strong>{{ 'risk.residual_assessment'|trans({}, 'risk') }}:</strong>
                            {{ risk.residualRiskLevel }}
                            ({{ risk.residualProbability }} × {{ risk.residualImpact }})
                        </div>
                    </div>
                    </div>
                </div>

                {# Acceptance Request Form #}
                <form method="post" action="{{ path('app_risk_request_acceptance', {id: risk.id}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('request-acceptance' ~ risk.id) }}">

                    <div class="mb-4">
                        <label for="justification" class="form-label required">
                            {{ 'risk.acceptance.justification_label'|trans({}, 'risk') }}
                        </label>
                        <textarea
                            class="form-control fa-cyber-input__field"
                            id="justification"
                            name="justification"
                            rows="6"
                            required
                            placeholder="{{ 'risk.acceptance.justification_help'|trans({}, 'risk') }}"
                        ></textarea>
                        <div class="form-text">
                            {{ 'risk.acceptance.justification_help'|trans({}, 'risk') }}
                        </div>
                    </div>

                    <div class="d-flex justify-content-between align-items-center">
                        <a href="{{ path('app_risk_show', {id: risk.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i>
                            {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--ui-check" aria-hidden="true"></i>
                            {{ 'risk.acceptance.submit_request'|trans({}, 'risk') }}
                        </button>
                    </div>
                </form>
            {% endblock %}
        {% endembed %}
    </div>

    {# Sidebar: Approval Level Information #}
    <div class="col-lg-4">
        {% set approval_variant = required_level == 'automatic' ? 'success' : (required_level == 'manager' ? 'warning' : 'danger') %}
        {% embed '_components/_card.html.twig' with { 'class': 'border-' ~ approval_variant } %}
            {% block card_header %}
                <div class="card-header">
                    <h2 class="mb-0">
                        <i class="fa-icon fa-icon--compliance-shield text-{{ approval_variant }}" aria-hidden="true"></i>
                        {{ 'risk.acceptance.required_approval_level'|trans({}, 'risk') }}
                    </h2>
                </div>
            {% endblock %}
            {% block card_body %}
                {% embed '_components/_fa_alert.html.twig' with { props: { variant: approval_variant, class: 'mb-3' } } %}
                    {% block alert_body %}
                        <strong class="d-block">{{ ('risk.acceptance.approval_levels.' ~ required_level)|trans({}, 'risk') }}</strong>
                        <small>{{ 'risk.acceptance.approval_threshold_info'|trans({'%score%': risk.residualRiskLevel}, 'risk') }}</small>
                    {% endblock %}
                {% endembed %}

                <h3>{{ 'risk.guidance.title'|trans({}, 'risk') }}</h3>
                <div class="list-group list-group-flush">
                    {% for level, config in thresholds %}
                        <div class="list-group-item {{ required_level == level ? 'active' : '' }}">
                            <div class="d-flex align-items-center justify-content-between">
                                <div>
                                    <div class="mb-1 fw-bold">{{ config.label }}</div>
                                    <small class="{{ required_level == level ? 'text-white' : 'text-muted' }}">
                                        {{ config.description }}
                                    </small>
                                </div>
                                {% if required_level == level %}
                                    <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                                {% endif %}
                            </div>
                        </div>
                    {% endfor %}
                </div>

                <div class="mt-3">
                    <div class="fw-bold">{{ 'common.info'|trans({}, 'messages') }}</div>
                    <p class="small text-muted mb-0">
                        <i class="fa-icon fa-icon--status-info" aria-hidden="true"></i>
                        ISO 27005:2022 Section 8.4.4 requires formal approval for risk acceptance based on residual risk level.
                    </p>
                </div>
            {% endblock %}
        {% endembed %}
    </div>
</div>
{% endblock %}
