{% extends 'base.html.twig' %}
{% trans_default_domain 'workflows' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'workflow.title.definitions'|trans({}, 'workflows') }} - Little ISMS Helper{% endblock %}

{% block body %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}

{{ _fa_page_header.render({
    badge: { icon: 'nav-workflow', label: 'nav.workflows'|trans({}, 'nav') },
    title: 'workflow.title.definitions'|trans({}, 'workflows'),
    subtitle: 'workflow.description.definitions'|trans({}, 'workflows'),
    actions: [
        { label: 'common.back'|trans({}, 'messages'), variant: 'ghost', icon: 'util-arrow-left', href: path('app_workflow_index') }
    ]
}) }}

{# Statistics Cards #}
<div class="kpi-grid">
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'workflow.stats.total_definitions'|trans({}, 'workflows'), value: workflows|length, variant: 'primary' }) }}
    </div>
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'workflow.stats.active_definitions'|trans({}, 'workflows'), value: workflows|filter(w => w.isActive)|length, variant: 'success' }) }}
    </div>
    <div class="col-auto">
        {% set totalSteps = 0 %}
        {% for workflow in workflows %}
            {% set totalSteps = totalSteps + workflow.steps|length %}
        {% endfor %}
        {{ _fa_feature_card.render({ label: 'workflow.stats.avg_steps'|trans({}, 'workflows'), value: workflows|length > 0 ? (totalSteps / workflows|length)|round : 0, variant: 'info' }) }}
    </div>
    <div class="col-auto">
        {% set entityTypes = [] %}
        {% for w in workflows %}
            {% if w.entityType not in entityTypes %}
                {% set entityTypes = entityTypes|merge([w.entityType]) %}
            {% endif %}
        {% endfor %}
        {{ _fa_feature_card.render({ label: 'workflow.stats.entity_types'|trans({}, 'workflows'), value: entityTypes|length, variant: 'secondary' }) }}
    </div>
</div>

{# Definitions Table #}
{% embed '_components/_table.html.twig' with {
    'class': 'table-container',
    'noMargin': true
} %}
    {% block table_head %}
        <tr>
            <th scope="col">{{ 'workflow.field.definition_name'|trans({}, 'workflows') }}</th>
            <th scope="col">{{ 'workflow.field.description'|trans({}, 'workflows') }}</th>
            <th scope="col" class="text-center">{{ 'workflow.field.steps'|trans({}, 'workflows') }}</th>
            <th scope="col" class="text-center">{{ 'workflow.field.instances'|trans({}, 'workflows') }}</th>
            <th scope="col" class="text-center">{{ 'workflow.field.active'|trans({}, 'workflows') }}</th>
            <th scope="col" class="text-center">{{ 'common.actions'|trans({}, 'messages') }}</th>
        </tr>
    {% endblock %}
    {% block table_body %}
        {% for workflow in workflows %}
        <tr>
            <td>
                <strong>{{ workflow.name }}</strong>
                <br><small class="text-muted-color">{{ workflow.entityType }}</small>
            </td>
            <td>
                <small class="text-muted-color">
                    {{ workflow.description|default('-') }}
                </small>
            </td>
            <td class="text-center">
                {% include '_components/_badge.html.twig' with { variant: 'info', content: workflow.steps|length } %}
            </td>
            <td class="text-center">
                {% include '_components/_badge.html.twig' with { variant: 'secondary', content: '—' } %}
            </td>
            <td class="text-center">
                {% if workflow.isActive %}
                    {% include '_components/_badge.html.twig' with { variant: 'success', content: '✓' } %}
                {% else %}
                    {% include '_components/_badge.html.twig' with { variant: 'secondary', content: '✗' } %}
                {% endif %}
            </td>
            <td class="text-center">
                <a href="{{ path('app_workflow_definition_show', {id: workflow.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
                    👁 {{ 'common.view'|trans({}, 'messages') }}
                </a>
                <a href="{{ path('app_workflow_definition_show', {id: workflow.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
                    <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i> {{ 'common.edit'|trans({}, 'messages') }}
                </a>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="6" class="text-center-p-xl-muted">
                {{ 'workflow.message.no_definitions'|trans({}, 'workflows') }}
            </td>
        </tr>
        {% endfor %}
    {% endblock %}
{% endembed %}
{% endblock %}
