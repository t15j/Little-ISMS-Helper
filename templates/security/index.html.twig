{% extends 'base.html.twig' %}
{% trans_default_domain 'security_reports' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}{{ 'security_reports.page.title'|trans({}, 'reports') }} - {{ 'app.title'|trans({}, 'security_reports') }}{% endblock %}

{% block body %}
<div class="container-fluid">
    {# Breadcrumb Navigation #}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'reports.overview'|trans({}, 'reports'), url: path('app_reports_overview') },
            { label: 'security_reports.page.title'|trans({}, 'security_reports') }
        ]
    } %}

    {# Page Header #}
    {% include '_components/_page_header.html.twig' with {
        title: 'security_reports.page.title'|trans({}, 'security_reports'),
        subtitle: 'security_reports.page.subtitle'|trans({}, 'security_reports'),
        icon: 'nav-shield-lock'
    } %}

    <div class="row">
        <div class="col-lg-3 mb-4">
            {# Navigation Sidebar #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="card-title mb-0">
                            <i class="fa-icon me-2 fa-icon--ui-list" aria-hidden="true"></i>{{ 'security_reports.nav.title'|trans({}, 'security_reports') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                <div class="list-group list-group-flush">
                    <a href="{{ path('app_security_report') }}" class="list-group-item list-group-item-action active">
                        <i class="fa-icon fa-icon--compliance-shield me-2" aria-hidden="true"></i>{{ 'security_reports.nav.overview'|trans({}, 'security_reports') }}
                    </a>
                    <a href="{{ path('app_security_report_2025') }}" class="list-group-item list-group-item-action {% if not stats.owasp_2025.exists %}disabled{% endif %}">
                        <i class="fa-icon me-2 fa-icon--nav-bar-chart" aria-hidden="true"></i>{{ 'security_reports.nav.owasp_2025'|trans({}, 'security_reports') }}
                    </a>
                    <a href="{{ path('app_security_report_2021') }}" class="list-group-item list-group-item-action {% if not stats.owasp_2021.exists %}disabled{% endif %}">
                        <i class="fa-icon me-2 fa-icon--nav-bar-chart" aria-hidden="true"></i>{{ 'security_reports.nav.owasp_2021'|trans({}, 'security_reports') }}
                    </a>
                    <a href="{{ path('app_reports_overview') }}" class="list-group-item list-group-item-action">
                        <i class="fa-icon me-2 fa-icon--util-arrow-left" aria-hidden="true"></i>{{ 'security_reports.nav.all_reports'|trans({}, 'security_reports') }}
                    </a>
                </div>
                {% endblock %}
            {% endembed %}

            {# Quick Actions #}
            {% embed '_components/_card.html.twig' with {class: 'mt-3'} %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="card-title mb-0">
                            <i class="fa-icon me-2 fa-icon--nav-tools" aria-hidden="true"></i>{{ 'security_reports.action.title'|trans({}, 'security_reports') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="d-grid gap-2">
                        <button class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm"
                                data-controller="report-regenerate"
                                data-action="click->report-regenerate#regenerate"
                                data-report-regenerate-url-value="{{ path('app_security_report_generate') }}"
                                data-report-regenerate-loading-text-value="{{ 'common.generating_reports'|trans({}, 'messages') }}"
                                data-report-regenerate-success-text-value="{{ 'common.report_regenerated'|trans({}, 'messages') }}">
                            <i class="fa-icon me-1 fa-icon--ui-refresh" aria-hidden="true"></i>{{ 'security_reports.action.regenerate'|trans({}, 'security_reports') }}
                        </button>
                    </div>
                    <div class="mt-3">
                        <small class="text-muted">
                            <i class="fa-icon me-1 fa-icon--status-info" aria-hidden="true"></i>
                            {{ 'security_reports.info.regenerate_hint'|trans({}, 'security_reports')|raw }}
                        </small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-lg-9">
            {# OWASP 2025 RC1 Report Card #}
            {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="card-title mb-0">
                            <i class="fa-icon fa-icon--control me-2" aria-hidden="true"></i>{{ 'security_reports.card.owasp_2025_title'|trans({}, 'security_reports') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    {% if stats.owasp_2025.exists %}
                        <div class="row">
                            <div class="col-md-3">
                                <div class="text-center">
                                    <div class="h2 mb-0 {% if stats.owasp_2025.score >= 9.0 %}text-success{% elseif stats.owasp_2025.score >= 8.0 %}text-primary{% elseif stats.owasp_2025.score >= 7.0 %}text-warning{% else %}text-danger{% endif %}">
                                        {{ stats.owasp_2025.score }}/10
                                    </div>
                                    <small class="text-muted">{{ 'security_reports.label.overall_score'|trans({}, 'security_reports') }}</small>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="text-center">
                                    <div class="h3 mb-0">
                                        <span class="fa-status-pill {% if stats.owasp_2025.status == 'EXCELLENT' %}fa-status-pill--success{% elseif stats.owasp_2025.status == 'GUT' %}fa-status-pill--primary{% elseif stats.owasp_2025.status == 'BEFRIEDIGEND' %}fa-status-pill--warning{% else %}fa-status-pill--danger{% endif %}">
                                            {{ stats.owasp_2025.status }}
                                        </span>
                                    </div>
                                    <small class="text-muted">{{ 'security_reports.label.status'|trans({}, 'security_reports') }}</small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="d-flex justify-content-around">
                                    <div class="text-center">
                                        <div class="h4 mb-0 {% if stats.owasp_2025.critical_count > 0 %}text-danger{% else %}text-success{% endif %}">
                                            {{ stats.owasp_2025.critical_count }}
                                        </div>
                                        <small class="text-muted">{{ 'security_reports.severity.p0'|trans({}, 'security_reports') }}</small>
                                    </div>
                                    <div class="text-center">
                                        <div class="h4 mb-0 {% if stats.owasp_2025.high_count > 0 %}text-warning{% else %}text-success{% endif %}">
                                            {{ stats.owasp_2025.high_count }}
                                        </div>
                                        <small class="text-muted">{{ 'security_reports.severity.p1'|trans({}, 'security_reports') }}</small>
                                    </div>
                                    <div class="text-center">
                                        <div class="h4 mb-0 text-info">
                                            {{ stats.owasp_2025.medium_count }}
                                        </div>
                                        <small class="text-muted">{{ 'security_reports.severity.p2'|trans({}, 'security_reports') }}</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between align-items-center">
                            <small class="text-muted">
                                <i class="fa-icon me-1 fa-icon--ui-calendar" aria-hidden="true"></i>{{ 'security_reports.label.generated_at'|trans({}, 'security_reports') }}: {{ stats.owasp_2025.generated_at }}
                            </small>
                            <a href="{{ path('app_security_report_2025') }}" class="fa-cyber-btn fa-cyber-btn--primary fa-cyber-btn--sm">
                                <i class="fa-icon fa-icon--policy me-1" aria-hidden="true"></i>{{ 'security_reports.label.view_report'|trans({}, 'security_reports') }}
                            </a>
                            </div>
                    {% else %}
                        {{ _fa_alert.render({ variant: 'warning', class: 'mb-0', body: 'security_reports.info.regenerate_hint'|trans({}, 'security_reports') }) }}
                    {% endif %}
                {% endblock %}
            {% endembed %}

            {# OWASP 2021 Report Card #}
            {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="card-title mb-0">
                            <i class="fa-icon fa-icon--compliance-shield me-2" aria-hidden="true"></i>{{ 'security_reports.nav.owasp_2021'|trans({}, 'security_reports') }} (Legacy)
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    {% if stats.owasp_2021.exists %}
                        <div class="row">
                            <div class="col-md-3">
                                <div class="text-center">
                                    <div class="h2 mb-0 {% if stats.owasp_2021.score >= 9.0 %}text-success{% elseif stats.owasp_2021.score >= 8.0 %}text-primary{% elseif stats.owasp_2021.score >= 7.0 %}text-warning{% else %}text-danger{% endif %}">
                                        {{ stats.owasp_2021.score }}/10
                                    </div>
                                    <small class="text-muted">{{ 'security_reports.label.overall_score'|trans({}, 'security_reports') }}</small>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="text-center">
                                    <div class="h3 mb-0">
                                        <span class="fa-status-pill {% if stats.owasp_2021.status == 'EXCELLENT' %}fa-status-pill--success{% elseif stats.owasp_2021.status == 'GUT' %}fa-status-pill--primary{% elseif stats.owasp_2021.status == 'BEFRIEDIGEND' %}fa-status-pill--warning{% else %}fa-status-pill--danger{% endif %}">
                                            {{ stats.owasp_2021.status }}
                                        </span>
                                    </div>
                                    <small class="text-muted">{{ 'security_reports.label.status'|trans({}, 'security_reports') }}</small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="d-flex justify-content-around">
                                    <div class="text-center">
                                        <div class="h4 mb-0 {% if stats.owasp_2021.critical_count > 0 %}text-danger{% else %}text-success{% endif %}">
                                            {{ stats.owasp_2021.critical_count }}
                                        </div>
                                        <small class="text-muted">{{ 'security_reports.severity.p0'|trans({}, 'security_reports') }}</small>
                                    </div>
                                    <div class="text-center">
                                        <div class="h4 mb-0 {% if stats.owasp_2021.high_count > 0 %}text-warning{% else %}text-success{% endif %}">
                                            {{ stats.owasp_2021.high_count }}
                                        </div>
                                        <small class="text-muted">{{ 'security_reports.severity.p1'|trans({}, 'security_reports') }}</small>
                                    </div>
                                    <div class="text-center">
                                        <div class="h4 mb-0 text-info">
                                            {{ stats.owasp_2021.medium_count }}
                                        </div>
                                        <small class="text-muted">{{ 'security_reports.severity.p2'|trans({}, 'security_reports') }}</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between align-items-center">
                            <small class="text-muted">
                                <i class="fa-icon me-1 fa-icon--ui-calendar" aria-hidden="true"></i>{{ 'security_reports.label.generated_at'|trans({}, 'security_reports') }}: {{ stats.owasp_2021.generated_at }}
                            </small>
                            <a href="{{ path('app_security_report_2021') }}" class="fa-cyber-btn fa-cyber-btn--primary fa-cyber-btn--sm">
                                <i class="fa-icon fa-icon--policy me-1" aria-hidden="true"></i>{{ 'security_reports.label.view_report'|trans({}, 'security_reports') }}
                            </a>
                        </div>
                    {% else %}
                        {{ _fa_alert.render({ variant: 'warning', class: 'mb-0', body: 'security_reports.info.regenerate_hint'|trans({}, 'security_reports') }) }}
                    {% endif %}
                {% endblock %}
            {% endembed %}

            {# Information Card #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header"><h2 class="card-title mb-0">
                            <i class="fa-icon me-2 fa-icon--status-info" aria-hidden="true"></i>Über diese Reports
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <p class="mb-2">
                        Die Security Audit Reports basieren auf den <strong>OWASP Top 10</strong> Security Risks und analysieren die Anwendung automatisch auf potenzielle Sicherheitslücken.
                    </p>
                    <ul class="mb-0">
                        <li><strong>OWASP 2025 RC1:</strong> Neueste Version mit aktualisierten Kategorien (z.B. "Mishandling of Exceptional Conditions")</li>
                        <li><strong>OWASP 2021:</strong> Stabile Version für Compliance-Nachweise</li>
                    </ul>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}
