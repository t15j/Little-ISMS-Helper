{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}Import Preview - Data Management{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i> Import Preview</h1>
        <a href="{{ path('data_import_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> Cancel
        </a>
    </div>

    {# Statistics #}
    <div class="row mb-4">
        <div class="col-md-4">
            {% embed '_components/_card.html.twig' with {borderColor: 'primary'} %}
                {% block card_body %}
                    <div class="text-center">
                        <div class="card-title text-muted fw-semibold">Total Entities</div>
                        <p class="display-6 text-primary">{{ stats|length }}</p>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-4">
            {% embed '_components/_card.html.twig' with {borderColor: 'success'} %}
                {% block card_body %}
                    <div class="text-center">
                        <div class="card-title text-muted fw-semibold">Total Records</div>
                        <p class="display-6 text-success">
                            {% set total = 0 %}
                            {% for entity, count in stats %}
                                {% set total = total + count %}
                            {% endfor %}
                            {{ total }}
                        </p>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-4">
            {% embed '_components/_card.html.twig' with {borderColor: 'info'} %}
                {% block card_body %}
                    <div class="text-center">
                        <div class="card-title text-muted fw-semibold">Status</div>
                        <p class="display-6 text-info small">Preview</p>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Entity Breakdown #}
    {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--ui-list" aria-hidden="true"></i> Import Summary
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            {% embed '_components/_table.html.twig' with {
                'class': 'table-hover',
                'noMargin': true,
                'responsive': true
            } %}
                {% block table_head %}
                    <tr>
                        <th scope="col">Entity</th>
                        <th scope="col" class="text-end">Record Count</th>
                    </tr>
                {% endblock %}
                {% block table_body %}
                    {% for entity, count in stats %}
                        <tr>
                            <td>
                                <i class="fa-icon fa-icon--nav-collection" aria-hidden="true"></i>
                                <strong>{{ entity }}</strong>
                            </td>
                            <td class="text-end">
                                {% include '_components/_badge.html.twig' with { variant: 'primary', content: count } %}
                            </td>
                        </tr>
                    {% endfor %}
                {% endblock %}
            {% endembed %}
        {% endblock %}
    {% endembed %}

    {# Preview Data (first few records) #}
    {% for entity, records in data %}
        {% if records|length > 0 %}
            {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
                {% block card_header %}
                    <div class="card-header"><h3 class="mb-0 h5">
                            <i class="fa-icon fa-icon--nav-grid" aria-hidden="true"></i> {{ entity }} - Preview (showing first {{ records|slice(0, 3)|length }} of {{ records|length }})
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_table.html.twig' with {
                        'class': 'table-sm table-bordered',
                        'theadClass': 'table-light',
                        'noMargin': true,
                        'responsive': true
                    } %}
                        {% block table_head %}
                            <tr>
                                {% for field in records[0]|keys %}
                                    <th scope="col">{{ field }}</th>
                                {% endfor %}
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for record in records|slice(0, 3) %}
                                <tr>
                                    {% for value in record %}
                                        <td>
                                            <small>{{ value|slice(0, 50) }}{% if value|length > 50 %}...{% endif %}</small>
                                        </td>
                                    {% endfor %}
                                </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                    {% if records|length > 3 %}
                        <small class="text-muted">... and {{ records|length - 3 }} more records</small>
                    {% endif %}
                {% endblock %}
            {% endembed %}
        {% endif %}
    {% endfor %}

    {# Action Buttons #}
    {% embed '_components/_card.html.twig' %}
        {% block card_body %}
            {% import '_components/_fa_alert.html.twig' as _fa_alert %}
            {{ _fa_alert.render({ variant: 'warning', class: 'mb-3', body: '<strong>Import functionality is currently in preview mode.</strong> The actual import execution is not yet implemented. This preview shows what would be imported.' }) }}

            <form method="post" action="{{ path('data_import_execute') }}"
                  data-controller="wizard-busy"
                  data-wizard-busy-message-value="{{ 'data.alva.busy.import_execute'|trans({}, 'data_management') }}">
                <input type="hidden" name="_token" value="{{ csrf_token('data_import_execute') }}">

                <div class="d-grid gap-2">
                    <button type="submit" class="fa-cyber-btn fa-cyber-btn--warning fa-cyber-btn--lg" disabled>
                        <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> Execute Import (Not Yet Available)
                    </button>
                    <a href="{{ path('data_import_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                        <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> Back to Import
                    </a>
                </div>
            </form>
        {% endblock %}
    {% endembed %}
</div>
{% endblock %}
