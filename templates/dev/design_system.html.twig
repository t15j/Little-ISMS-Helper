{# Living Styleguide — live macro demos PLUS the canonical spec sections
   shipped under docs/design_system/sections/*.html (loaded by
   DevDesignSystemController::loadSpecSections()). Single source of truth
   per AURORA_v4_FULL_MIGRATION_PLAN.md § 11.3 Living-Doc-Page. #}
{% extends 'base.html.twig' %}

{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_section.html.twig' as _fa_section %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}
{% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
{% import '_components/_fa_hero.html.twig' as _fa_hero %}
{% import '_components/_fa_filter_chip.html.twig' as _fa_filter_chip %}
{% import '_components/_fa_entity_card.html.twig' as _fa_entity_card %}
{% import '_components/_fa_entity_badge.html.twig' as _fa_entity_badge %}
{% import '_components/_alva.html.twig' as alva %}
{% import '_components/_fa_form_layout.html.twig' as _fa_form %}
{% import '_components/_fa_modal_wizard.html.twig' as _fa_modal_wizard %}
{% import '_components/_fa_modal.html.twig' as _fa_modal %}
{% import '_components/_fa_tabs.html.twig' as _fa_tabs %}

{% block title %}Aurora v4 Living Styleguide{% endblock %}

{% block body %}
<meta name="turbo-cache-control" content="no-cache">

<main class="fa-aurora-surface fa-aurora-surface--dots" style="min-height:100vh; padding-bottom: 4rem;">
    <div class="container-xl py-4">

        {# ── Top navigation ────────────────────────────────────────────── #}
        <nav class="d-flex flex-wrap gap-2 mb-4 p-3" style="background:var(--surface-2); border-radius:var(--radius-lg,8px); border:1px solid var(--border);">
            <span style="color:var(--fg-2); font-size:.75rem; align-self:center; margin-right:.5rem;">Jump to:</span>
            <a href="#sec-spec-sections" class="fa-filter-chip">Living Spec</a>
            <a href="#sec-surface"      class="fa-filter-chip">Aurora Surfaces</a>
            <a href="#sec-page-header"  class="fa-filter-chip">fa-page-header</a>
            <a href="#sec-section"      class="fa-filter-chip">fa-section</a>
            <a href="#sec-feature-card" class="fa-filter-chip">fa-feature-card</a>
            <a href="#sec-empty-state"  class="fa-filter-chip">fa-empty-state</a>
            <a href="#sec-hero"         class="fa-filter-chip">fa-hero</a>
            <a href="#sec-filter-chip"  class="fa-filter-chip">fa-filter-chip</a>
            <a href="#sec-entity-card"  class="fa-filter-chip">fa-entity-card</a>
            <a href="#sec-entity-badge" class="fa-filter-chip">fa-entity-badge</a>
            <a href="#sec-alva"         class="fa-filter-chip">Alva moods</a>
            <a href="#sec-cyber-btn"    class="fa-filter-chip">fa-cyber-btn</a>
            <a href="#sec-tokens"       class="fa-filter-chip">Aurora tokens</a>
            <a href="#sec-bulk-bar"     class="fa-filter-chip">fa-bulk-bar</a>
            <a href="#sec-kbd"          class="fa-filter-chip">fa-kbd / fa-cheatsheet</a>
            <a href="#sec-pager"        class="fa-filter-chip">fa-pager</a>
            <a href="#sec-fa-menu"      class="fa-filter-chip">fa-menu</a>
            <a href="#fa-form-layout"       class="fa-filter-chip">fa-form-layout</a>
            <a href="#fa-modal-wizard"     class="fa-filter-chip">fa-modal--wizard</a>
            <a href="#fa-tabs"             class="fa-filter-chip">fa-tabs</a>
            <a href="#fa-modal-confirm"    class="fa-filter-chip">fa-modal--confirm</a>
            <a href="#fa-modal-settings"   class="fa-filter-chip">fa-modal--settings</a>
        </nav>

        {# ── Page header ───────────────────────────────────────────────── #}
        {{ _fa_page_header.render({
            badge:    { icon: 'nav-palette', label: 'Dev only' },
            title:    'Aurora v4 Living Styleguide',
            subtitle: 'Every FairyAurora component with live preview and copy-paste Twig snippet.',
            actions: [
                { label: 'Source',   variant: 'ghost',   icon: 'ui-external-link',    href: '#' },
                { label: 'Phase 5',  variant: 'primary',  icon: 'ui-stars',     href: '#' }
            ]
        }) }}

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 0. LIVING SPEC SECTIONS                                          #}
        {#    Canonical HTML fragments from docs/design_system/sections/.   #}
        {#    Each section is collapsed by default — live demos below       #}
        {#    remain the primary navigation, spec is reference material.    #}
        {# ═══════════════════════════════════════════════════════════════ #}
        {% if spec_sections|default([])|length > 0 %}
        <section class="fa-section mb-5" id="sec-spec-sections">
            <div class="fa-section__header">
                <h2 class="fa-section__title">
                    <i class="fa-icon fa-icon--nav-book me-2" aria-hidden="true"></i>
                    Living Spec Sections
                </h2>
                <p class="fa-section__subtitle" style="margin:.25rem 0 0; color:var(--fg-2); font-size:.8rem;">
                    Canonical Aurora v4 reference material — collapsed by default.
                    Source: <code>docs/design_system/sections/*.html</code>.
                </p>
            </div>
            <div class="fa-section__body">
                <div class="d-flex flex-wrap gap-2 mb-3">
                    {% for sec in spec_sections %}
                        <a href="#spec-{{ sec.key }}" class="fa-filter-chip">{{ sec.title }}</a>
                    {% endfor %}
                </div>
                {% for sec in spec_sections %}
                    <details id="spec-{{ sec.key }}" class="mb-2" style="background:var(--surface-2); border:1px solid var(--border); border-radius:var(--radius-md,6px); padding:.75rem 1rem;">
                        <summary style="cursor:pointer; color:var(--primary); font-weight:600;">
                            {{ sec.title }}
                            <span style="color:var(--fg-3); font-weight:400; font-size:.8rem; margin-left:.5rem;">
                                ({{ sec.key }}.html)
                            </span>
                        </summary>
                        <div class="spec-section-body mt-3">
                            {{ sec.html|raw }}
                        </div>
                    </details>
                {% endfor %}
            </div>
        </section>
        {% endif %}

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 1. AURORA SURFACE VARIANTS                                      #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-surface">
            <div class="fa-section__header">
                <h2 class="fa-section__title">Aurora Surface Variants</h2>
            </div>
            <div class="fa-section__body">

                <div class="row g-3 mb-3">
                    <div class="col-sm-6 col-lg-3">
                        <div class="fa-aurora-surface d-flex align-items-center justify-content-center fw-semibold"
                             style="height:200px; border-radius:var(--radius-lg,8px); border:1px solid var(--border); color:var(--fg);">
                            .fa-aurora-surface
                        </div>
                    </div>
                    <div class="col-sm-6 col-lg-3">
                        <div class="fa-aurora-surface--subtle d-flex align-items-center justify-content-center fw-semibold"
                             style="height:200px; border-radius:var(--radius-lg,8px); border:1px solid var(--border); color:var(--fg);">
                            .fa-aurora-surface--subtle
                        </div>
                    </div>
                    <div class="col-sm-6 col-lg-3">
                        <div class="fa-aurora-surface--hero d-flex align-items-center justify-content-center fw-semibold"
                             style="height:200px; border-radius:var(--radius-lg,8px); border:1px solid var(--border); color:var(--fg);">
                            .fa-aurora-surface--hero
                        </div>
                    </div>
                    <div class="col-sm-6 col-lg-3">
                        <div class="fa-aurora-surface--dots d-flex align-items-center justify-content-center fw-semibold"
                             style="height:200px; border-radius:var(--radius-lg,8px); border:1px solid var(--border); color:var(--fg);">
                            .fa-aurora-surface--dots
                        </div>
                    </div>
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}<div class="fa-aurora-surface">…default aurora…</div>
<div class="fa-aurora-surface--subtle">…subtle variant…</div>
<div class="fa-aurora-surface--hero">…hero variant…</div>
<div class="fa-aurora-surface--dots">…dots variant…</div>{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 2. FA-PAGE-HEADER                                               #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-page-header">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-page-header</h2>
            </div>
            <div class="fa-section__body">

                <div class="mb-3" style="background:var(--surface-2); border:1px solid var(--border); border-radius:var(--radius-lg,8px); padding:1.5rem;">
                    {{ _fa_page_header.render({
                        badge:    { icon: 'nav-shield-lock', label: 'ISMS' },
                        title:    'Risikoübersicht',
                        subtitle: 'Alle offenen Risiken der aktuellen Periode.',
                        actions: [
                            { label: 'Filter',    variant: 'ghost',   icon: 'ui-funnel',  href: '#' },
                            { label: 'Neu',       variant: 'primary', icon: 'plus',    href: '#' }
                        ]
                    }) }}
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{{ _fa_page_header.render({
    badge:    { icon: 'nav-shield-lock', label: 'ISMS' },
    title:    'Risikoübersicht',
    subtitle: 'Alle offenen Risiken der aktuellen Periode.',
    actions: [
        { label: 'Filter', variant: 'ghost',   icon: 'ui-funnel', href: path('risk_index') },
        { label: 'Neu',    variant: 'primary',  icon: 'plus',   href: path('risk_new') }
    ]
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 3. FA-SECTION                                                   #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-section">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-section</h2>
            </div>
            <div class="fa-section__body">

                <div class="mb-3">
                    {{ _fa_section.render({
                        title:  'Neueste Aktivität',
                        tools:  '<button class="fa-filter-chip fa-filter-chip--active" type="button">Heute</button>
                                 <button class="fa-filter-chip" type="button">Woche</button>
                                 <button class="fa-filter-chip" type="button">Monat</button>',
                        body:   '<p style="color:var(--fg-2);">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Quisque vehicula mauris in erat accumsan, vel varius odio lacinia. Suspendisse potenti.</p>',
                        footer: '<a href="#" class="fa-link">Alle anzeigen &rarr;</a>'
                    }) }}
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_section.html.twig' as _fa_section %}

{{ _fa_section.render({
    title:  'Neueste Aktivität',
    tools:  '<button class="fa-filter-chip fa-filter-chip--active" type="button">Heute</button>
             <button class="fa-filter-chip" type="button">Woche</button>',
    body:   '<p>Lorem ipsum dolor sit amet…</p>',
    footer: '<a href="#" class="fa-link">Alle anzeigen →</a>'
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 4. FA-FEATURE-CARD                                              #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-feature-card">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-feature-card</h2>
            </div>
            <div class="fa-section__body">

                <div class="row g-3 mb-3">
                    <div class="col-sm-6 col-lg-3">
                        {{ _fa_feature_card.render({
                            label:   'SLA erfüllt',
                            value:   '98.4',
                            unit:    '%',
                            variant: 'success',
                            icon:    'check-circle-fill',
                            hint:    '+2.1% seit letzter Woche'
                        }) }}
                    </div>
                    <div class="col-sm-6 col-lg-3">
                        {{ _fa_feature_card.render({
                            label:   'Offene Risiken',
                            value:   '12',
                            unit:    '',
                            variant: 'warning',
                            icon:    'exclamation-triangle-fill',
                            hint:    '3 neu diese Woche'
                        }) }}
                    </div>
                    <div class="col-sm-6 col-lg-3">
                        {{ _fa_feature_card.render({
                            label:   'Kritische Vorfälle',
                            value:   '2',
                            unit:    '',
                            variant: 'danger',
                            icon:    'shield-exclamation',
                            hint:    'Eskalation erforderlich'
                        }) }}
                    </div>
                    <div class="col-sm-6 col-lg-3">
                        {{ _fa_feature_card.render({
                            label:   'Controls aktiv',
                            value:   '87',
                            unit:    '/ 93',
                            variant: 'primary',
                            icon:    'toggles-on',
                            hint:    '6 ausstehend'
                        }) }}
                    </div>
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}

{{ _fa_feature_card.render({
    label:   'SLA erfüllt',
    value:   '98.4',
    unit:    '%',
    variant: 'success',    {# success | warning | danger | primary #}
    icon:    'check-circle-fill',
    hint:    '+2.1% seit letzter Woche'
}) }}

{# Clickable tile — add href #}
{{ _fa_feature_card.render({
    label:   'Offene Risiken',
    value:   '12',
    variant: 'warning',
    icon:    'exclamation-triangle-fill',
    href:    path('risk_index')
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 5. FA-EMPTY-STATE                                               #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-empty-state">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-empty-state</h2>
            </div>
            <div class="fa-section__body">

                <div class="mb-3">
                    {{ _fa_empty_state.render({
                        mood:    'curious',
                        title:   'Noch keine Dokumente vorhanden',
                        text:    'Laden Sie das erste Dokument hoch oder wählen Sie eine Vorlage, um loszulegen.',
                        actions: [
                            { label: 'Dokument hochladen', variant: 'primary', icon: 'ui-upload',        href: '#' },
                            { label: 'Vorlage wählen',     variant: 'ghost',   icon: 'nav-file-earmark-text',  href: '#' }
                        ]
                    }) }}
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}

{{ _fa_empty_state.render({
    mood:    'curious',   {# idle|happy|thinking|focused|working|scanning|warning|celebrating|sleeping #}
    title:   'Noch keine Dokumente vorhanden',
    text:    'Laden Sie das erste Dokument hoch oder wählen Sie eine Vorlage.',
    actions: [
        { label: 'Hochladen',     variant: 'primary', icon: 'ui-upload',       href: path('document_new') },
        { label: 'Vorlage wählen', variant: 'ghost',  icon: 'nav-file-earmark-text', href: '#' }
    ]
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 6. FA-HERO                                                      #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-hero">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-hero</h2>
            </div>
            <div class="fa-section__body">

                <div class="mb-3">
                    {{ _fa_hero.render({
                        mood:     'happy',
                        title:    'Willkommen zurück, Max',
                        subtitle: 'Heute warten 3 Aufgaben in der Inbox auf Sie. Ihr ISMS-Status ist aktuell.',
                        actions:  [
                            { label: "Los geht's", variant: 'primary', icon: 'ui-arrow-right', href: '#' }
                        ]
                    }) }}
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_hero.html.twig' as _fa_hero %}

{{ _fa_hero.render({
    mood:     'happy',    {# idle|happy|thinking|focused|working|scanning|warning|celebrating|sleeping #}
    title:    'Willkommen zurück, {{ app.user.displayName }}',
    subtitle: 'Heute warten 3 Aufgaben in der Inbox auf Sie.',
    actions:  [
        { label: "Los geht's", variant: 'primary', icon: 'ui-arrow-right', href: path('dashboard') }
    ]
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 7. FA-FILTER-CHIP                                               #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-filter-chip">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-filter-chip</h2>
            </div>
            <div class="fa-section__body">

                {# Single chips #}
                <p class="mb-2" style="color:var(--fg-2); font-size:.875rem;">Single chips:</p>
                <div class="d-flex flex-wrap gap-2 mb-4">
                    {{ _fa_filter_chip.render('Alle',  { active: false }) }}
                    {{ _fa_filter_chip.render('Heute', { active: true  }) }}
                    {{ _fa_filter_chip.render('Mit Icon', { active: false, icon: 'ui-funnel' }) }}
                </div>

                {# Group #}
                <p class="mb-2" style="color:var(--fg-2); font-size:.875rem;">Chip group (activeIndex=1):</p>
                <div class="mb-4">
                    {{ _fa_filter_chip.group(['Heute', 'Woche', 'Monat', 'Quartal'], 1) }}
                </div>

                {# Group with hrefs #}
                <p class="mb-2" style="color:var(--fg-2); font-size:.875rem;">Chip group with hrefs:</p>
                <div class="mb-3">
                    {{ _fa_filter_chip.group([
                        { label: 'Offen',       href: '?status=open'   },
                        { label: 'In Bearbeitung', href: '?status=wip' },
                        { label: 'Geschlossen', href: '?status=closed' }
                    ], 0) }}
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_filter_chip.html.twig' as _fa_filter_chip %}

{# Single chip — as <button> #}
{{ _fa_filter_chip.render('Heute', { active: true }) }}

{# Single chip — as <a> with icon #}
{{ _fa_filter_chip.render('Filter', { active: false, icon: 'ui-funnel', href: '?filter=1' }) }}

{# Group — string labels, second chip active #}
{{ _fa_filter_chip.group(['Heute', 'Woche', 'Monat'], 1) }}

{# Group — objects with hrefs #}
{{ _fa_filter_chip.group([
    { label: 'Offen',       href: '?status=open'   },
    { label: 'In Bearbeitung', href: '?status=wip' },
    { label: 'Geschlossen', href: '?status=closed' }
], 0) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 7b. fa-entity-card — Listen-Item-Card mit Entity-Icon          #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-entity-card">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-entity-card</h2>
            </div>
            <div class="fa-section__body">
                <p class="text-muted mb-3">Listen-Item-Card für Findings, Risks, Controls, Incidents, Audits, NCs. 7 Entity-Type-Variants mit Akzent-Leiste + Icon-Färbung. Compact-Variante für dichte Listen.</p>

                <div class="d-flex flex-column gap-2 mb-3">
                    {{ _fa_entity_card.render({
                        type:    'finding',
                        icon:    'finding',
                        title:   'F-2026-014 Lückenhafte Logging-Konfig',
                        meta:    ['ISO 27001 A.8.15', 'fällig 12.05.'],
                        href:    '#',
                        status:  { variant: 'warning', label: 'Offen' }
                    }) }}
                    {{ _fa_entity_card.render({
                        type:    'risk',
                        icon:    'risk-register',
                        title:   'R-2026-007 Lieferantenausfall Cloud-Provider',
                        meta:    ['Likelihood: hoch', 'Impact: kritisch', 'Owner: CTO'],
                        href:    '#',
                        status:  { variant: 'danger', label: 'Hoch' }
                    }) }}
                    {{ _fa_entity_card.render({
                        type:    'evidence',
                        icon:    'evidence',
                        title:   'EV-2026-091 Penetrationstest-Bericht Q1',
                        meta:    ['ISO 27001 A.8.8', 'sha256 verifiziert'],
                        href:    '#',
                        status:  { variant: 'success', label: 'Validiert' },
                        compact: true
                    }) }}
                </div>

                <details>
                    <summary>Twig snippet</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_entity_card.html.twig' as _fa_entity_card %}

{{ _fa_entity_card.render({
    type:    'finding',                           {# entity-type sets accent-line + icon-color #}
    icon:    'finding',                           {# fa-icon-- name #}
    title:   'F-2026-014 Lückenhafte Logging-Konfig',
    meta:    ['ISO 27001 A.8.15', 'fällig 12.05.'],
    href:    path('audit_finding_show', {id: 14}),
    status:  { variant: 'warning', label: 'Offen' },
    compact: false                                {# true for dense lists #}
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 7c. fa-entity-badge — ISMS-Entity-Marker (10 types)             #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-entity-badge">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-entity-badge</h2>
            </div>
            <div class="fa-section__body">
                <p class="text-muted mb-3">Größer als status-pill, für Listen-Header und Detail-Marker. 10 Entity-Types mit Aurora-Semantik.</p>

                <div class="d-flex flex-wrap gap-2 mb-3">
                    {{ _fa_entity_badge.render({ type: 'finding',       icon: 'finding',          label: 'Finding' }) }}
                    {{ _fa_entity_badge.render({ type: 'nonconformity', icon: 'nonconformity',    label: 'NC' }) }}
                    {{ _fa_entity_badge.render({ type: 'risk',          icon: 'risk-register',    label: 'Risk' }) }}
                    {{ _fa_entity_badge.render({ type: 'control',       icon: 'control',          label: 'Control' }) }}
                    {{ _fa_entity_badge.render({ type: 'evidence',      icon: 'evidence',         label: 'Evidence' }) }}
                    {{ _fa_entity_badge.render({ type: 'policy',        icon: 'policy',           label: 'Policy' }) }}
                    {{ _fa_entity_badge.render({ type: 'asset',         icon: 'asset-server',     label: 'Asset' }) }}
                    {{ _fa_entity_badge.render({ type: 'incident',      icon: 'incident',         label: 'Incident' }) }}
                    {{ _fa_entity_badge.render({ type: 'audit',         icon: 'audit-internal',   label: 'Audit' }) }}
                    {{ _fa_entity_badge.render({ type: 'training',      icon: 'training',         label: 'Training' }) }}
                </div>

                <details>
                    <summary>Twig snippet</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_fa_entity_badge.html.twig' as _fa_entity_badge %}

{{ _fa_entity_badge.render({
    type:  'risk',                  {# 10 types: finding, nonconformity, risk, control,
                                        evidence, policy, asset, incident, audit, training #}
    icon:  'risk-register',         {# fa-icon-- name (optional) #}
    label: 'Risk'
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 8. ALVA 9 MOODS MATRIX                                         #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-alva">
            <div class="fa-section__header">
                <h2 class="fa-section__title">Alva — 9 Moods Matrix</h2>
            </div>
            <div class="fa-section__body">

                <div class="row g-3 mb-3">
                    {% for mood in ['idle', 'happy', 'thinking', 'focused', 'working', 'scanning', 'warning', 'celebrating', 'sleeping'] %}
                        <div class="col-6 col-sm-4 col-lg-2" style="text-align:center;">
                            <div style="background:var(--surface-2); border:1px solid var(--border); border-radius:var(--radius-lg,8px); padding:1rem; display:inline-flex; flex-direction:column; align-items:center; gap:.5rem; width:100%;">
                                {{ alva.render(mood, { size: 80, decorative: true }) }}
                                <span style="color:var(--fg-2); font-size:.75rem; font-family:monospace;">{{ mood }}</span>
                            </div>
                        </div>
                    {% endfor %}
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{% import '_components/_alva.html.twig' as alva %}

{# Decorative (no aria role) #}
{{ alva.render('happy', { size: 96, decorative: true }) }}

{# Accessible (with aria-label) #}
{{ alva.render('warning', { size: 80, ariaLabel: 'Alva warnt vor einem Risiko' }) }}

{# Available moods: #}
{# idle | happy | thinking | focused | working | scanning | warning | celebrating | sleeping #}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 9. FA-CYBER-BTN VARIANTS                                        #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-cyber-btn">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-cyber-btn variants</h2>
            </div>
            <div class="fa-section__body">

                <div class="mb-3">
                    <p class="mb-2" style="color:var(--fg-2); font-size:.875rem;">Variants:</p>
                    <div class="d-flex flex-wrap gap-2 align-items-center mb-3">
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                            <span class="fa-cyber-btn__label">Primary</span>
                        </a>
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i>
                            <span class="fa-cyber-btn__label">Ghost</span>
                        </a>
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--secondary">
                            <i class="fa-icon fa-icon--status-archived" aria-hidden="true"></i>
                            <span class="fa-cyber-btn__label">Secondary</span>
                        </a>
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--accent">
                            <i class="fa-icon fa-icon--nav-stars" aria-hidden="true"></i>
                            <span class="fa-cyber-btn__label">Accent</span>
                        </a>
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--danger">
                            <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i>
                            <span class="fa-cyber-btn__label">Danger</span>
                        </a>
                    </div>

                    <p class="mb-2" style="color:var(--fg-2); font-size:.875rem;">Sizes:</p>
                    <div class="d-flex flex-wrap gap-2 align-items-center mb-3">
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--primary fa-cyber-btn--sm">
                            <span class="fa-cyber-btn__label">Small (sm)</span>
                        </a>
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--primary">
                            <span class="fa-cyber-btn__label">Default</span>
                        </a>
                        <a href="#" class="fa-cyber-btn fa-cyber-btn--primary fa-cyber-btn--lg">
                            <span class="fa-cyber-btn__label">Large (lg)</span>
                        </a>
                    </div>

                    <p class="mb-2" style="color:var(--fg-2); font-size:.875rem;">Disabled state:</p>
                    <div class="d-flex flex-wrap gap-2 align-items-center">
                        <button class="fa-cyber-btn fa-cyber-btn--primary" disabled aria-disabled="true">
                            <span class="fa-cyber-btn__label">Disabled Primary</span>
                        </button>
                        <button class="fa-cyber-btn fa-cyber-btn--ghost" disabled aria-disabled="true">
                            <span class="fa-cyber-btn__label">Disabled Ghost</span>
                        </button>
                    </div>
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{# As <a> link #}
<a href="{{ path('route') }}" class="fa-cyber-btn fa-cyber-btn--primary">
    <i class="fa-icon fa-icon--ui-plus" aria-hidden="true"></i>
    <span class="fa-cyber-btn__label">Neu</span>
</a>

{# As <button> #}
<button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
    <span class="fa-cyber-btn__label">Speichern</span>
</button>

{# Variants: primary | ghost | secondary | accent | danger #}
{# Sizes:    fa-cyber-btn--sm | (default) | fa-cyber-btn--lg #}
{# Disabled: add disabled attr + aria-disabled="true" #}

{# In page-header actions — use variant + icon via macro: #}
{# { label: 'Neu', variant: 'primary', icon: 'plus', href: '#' } #}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# 10. AURORA TOKENS REFERENCE                                     #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="sec-tokens">
            <div class="fa-section__header">
                <h2 class="fa-section__title">Aurora Design Tokens</h2>
            </div>
            <div class="fa-section__body">

                <p class="mb-3" style="color:var(--fg-2); font-size:.875rem;">
                    All colors must be referenced via CSS custom properties — never hardcode hex values.
                    Source of truth: <code>assets/styles/fairy-aurora.css</code>
                </p>

                <div class="table-responsive">
                    <table class="table" style="color:var(--fg);">
                        <thead>
                            <tr style="border-bottom:1px solid var(--border);">
                                <th style="color:var(--fg-2); font-weight:500; width:40px;"></th>
                                <th style="color:var(--fg-2); font-weight:500;">Token</th>
                                <th style="color:var(--fg-2); font-weight:500;">Usage</th>
                                <th style="color:var(--fg-2); font-weight:500;">CSS Usage</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% set tokens = [
                                { token: '--primary',         usage: 'Primary brand colour — interactive elements, key accents' },
                                { token: '--primary-strong',  usage: 'Darker primary for hover states and borders' },
                                { token: '--primary-tint',    usage: 'Very light primary tint for backgrounds, highlights' },
                                { token: '--accent',          usage: 'Secondary accent (Alva wand glow, sparkles)' },
                                { token: '--accent-tint',     usage: 'Light accent tint — subtle callout backgrounds' },
                                { token: '--success',         usage: 'Positive states — SLA met, controls active, OK' },
                                { token: '--warning',         usage: 'Caution states — pending items, medium risks' },
                                { token: '--danger',          usage: 'Error / critical states — incidents, high risks' },
                                { token: '--surface',         usage: 'Page-level background surface' },
                                { token: '--surface-2',       usage: 'Card / panel surface (slightly raised)' },
                                { token: '--fg',              usage: 'Primary text foreground' },
                                { token: '--fg-2',            usage: 'Secondary / muted text' },
                                { token: '--fg-3',            usage: 'Tertiary / placeholder text' },
                                { token: '--border',          usage: 'Default border colour' },
                                { token: '--border-strong',   usage: 'Stronger border for emphasis or dividers' }
                            ] %}
                            {% for t in tokens %}
                                <tr style="border-bottom:1px solid var(--border);">
                                    <td>
                                        <span style="display:inline-block; width:40px; height:40px; border-radius:6px; background:var({{ t.token }}); border:1px solid var(--border);" title="{{ t.token }}"></span>
                                    </td>
                                    <td><code style="color:var(--primary);">var({{ t.token }})</code></td>
                                    <td style="color:var(--fg-2); font-size:.875rem;">{{ t.usage }}</td>
                                    <td><code style="color:var(--fg-3); font-size:.8rem;">color: var({{ t.token }})</code></td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>

                <details>
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.5rem;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto;"><code>{% verbatim %}{# CORRECT — always use CSS custom properties #}
<span style="color: var(--primary);">Primary text</span>
<div style="background: var(--surface-2); border: 1px solid var(--border);">Card</div>

{# WRONG — never use hardcoded hex #}
{# <span style="color: #0284c7;">Primary text</span>  ← stylelint will catch this #}

{# Token categories: #}
{# Brand:    --primary, --primary-strong, --primary-tint, --accent, --accent-tint #}
{# Semantic: --success, --warning, --danger #}
{# Surface:  --surface, --surface-2 #}
{# Text:     --fg, --fg-2, --fg-3 #}
{# Border:   --border, --border-strong #}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ── fa-bulk-bar (Aurora v4 neutral + brand) ──────────────────── #}
        <section id="sec-bulk-bar" class="mb-5">
            <h2 style="font:700 18px var(--font-sans); color:var(--fg); margin:0 0 4px;">fa-bulk-bar</h2>
            <p style="color:var(--fg-2); font-size:13px; margin:0 0 16px;">
                Floating pill — neutral surface (default) or <code>.fa-bulk-bar--brand</code> gradient. Use neutral for general lists, brand for hero workflows (risks, findings).
            </p>

            {# Neutral (surface) variant #}
            <div class="mb-3 p-4" style="background:var(--surface-2); border:1px dashed var(--border); border-radius:var(--r-md); position:relative; min-height:80px; display:flex; align-items:center; justify-content:center;">
                <div class="fa-bulk-bar" role="region" aria-label="{{ 'bulk_actions.toolbar'|trans({}, 'messages') }}" style="position:relative; bottom:auto; left:auto; transform:none; animation:none;">
                    <div class="fa-bulk-bar__count">
                        <span class="fa-bulk-bar__count-num">3</span>
                        <span class="fa-bulk-bar__count-label">{{ 'bulk_actions.selected'|trans({}, 'messages') }}</span>
                    </div>
                    <div class="fa-bulk-bar__divider" aria-hidden="true"></div>
                    <div class="fa-bulk-bar__actions">
                        <button class="fa-bulk-btn"><i class="fa-icon fa-icon--ui-tag" aria-hidden="true"></i> {{ 'bulk_actions.add_tag'|trans({}, 'messages') }}</button>
                        <button class="fa-bulk-btn fa-bulk-btn--success"><i class="fa-icon fa-icon--ui-check" aria-hidden="true"></i> {{ 'bulk_actions.approve'|trans({}, 'messages') }}</button>
                        <button class="fa-bulk-btn fa-bulk-btn--danger"><i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'bulk_actions.delete'|trans({}, 'messages') }}</button>
                    </div>
                    <div class="fa-bulk-bar__divider" aria-hidden="true"></div>
                    <button class="fa-bulk-bar__close" aria-label="{{ 'bulk_actions.deselect'|trans({}, 'messages') }}"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i></button>
                </div>
            </div>

            {# Brand (gradient) variant #}
            <div class="p-4" style="background:var(--surface-2); border:1px dashed var(--border); border-radius:var(--r-md); position:relative; min-height:80px; display:flex; align-items:center; justify-content:center;">
                <div class="fa-bulk-bar fa-bulk-bar--brand" role="region" aria-label="{{ 'bulk_actions.toolbar'|trans({}, 'messages') }}" style="position:relative; bottom:auto; left:auto; transform:none; animation:none;">
                    <div class="fa-bulk-bar__count">
                        <span class="fa-bulk-bar__count-num">7</span>
                        <span class="fa-bulk-bar__count-label">{{ 'bulk_actions.selected'|trans({}, 'messages') }}</span>
                    </div>
                    <div class="fa-bulk-bar__divider" aria-hidden="true"></div>
                    <div class="fa-bulk-bar__actions">
                        <button class="fa-bulk-btn"><i class="fa-icon fa-icon--nav-people" aria-hidden="true"></i> {{ 'bulk_actions.assign'|trans({}, 'messages') }}</button>
                        <button class="fa-bulk-btn fa-bulk-btn--success"><i class="fa-icon fa-icon--ui-check" aria-hidden="true"></i> {{ 'bulk_actions.approve'|trans({}, 'messages') }}</button>
                        <button class="fa-bulk-btn fa-bulk-btn--danger"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i> {{ 'bulk_actions.delete'|trans({}, 'messages') }}</button>
                    </div>
                    <div class="fa-bulk-bar__divider" aria-hidden="true"></div>
                    <button class="fa-bulk-bar__close" aria-label="{{ 'bulk_actions.deselect'|trans({}, 'messages') }}"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i></button>
                </div>
            </div>
        </section>

        {# ── fa-kbd + fa-cheatsheet ────────────────────────────────────── #}
        <section id="sec-kbd" class="mb-5">
            <h2 style="font:700 18px var(--font-sans); color:var(--fg); margin:0 0 4px;">fa-kbd &amp; fa-cheatsheet</h2>
            <p style="color:var(--fg-2); font-size:13px; margin:0 0 16px;">
                Keyboard key tokens + shortcut cheatsheet overlay content.
            </p>
            <div class="d-flex flex-wrap align-items-center gap-4 mb-3 p-4" style="background:var(--surface-2); border:1px solid var(--border); border-radius:var(--r-md);">
                <span class="fa-kbd">⌘</span>
                <div class="fa-kbd-combo"><kbd class="fa-kbd">⌘</kbd><span class="fa-kbd-combo__plus">+</span><kbd class="fa-kbd">K</kbd></div>
                <div class="fa-kbd-combo"><kbd class="fa-kbd fa-kbd--lg">⌘</kbd><span class="fa-kbd-combo__plus">+</span><kbd class="fa-kbd fa-kbd--lg">S</kbd></div>
                <kbd class="fa-kbd">Esc</kbd>
                <kbd class="fa-kbd">?</kbd>
            </div>
            <div class="fa-cheatsheet" style="max-width:600px;">
                <header class="fa-cheatsheet__header">
                    <div>
                        <div class="fa-cheatsheet__sub">Tastenkürzel</div>
                        <h3 class="fa-cheatsheet__title">ISMS-Helper</h3>
                    </div>
                    <kbd class="fa-kbd fa-kbd--lg">?</kbd>
                </header>
                <div class="fa-cheatsheet__grid">
                    <div class="fa-cheatsheet__group">
                        <h4>Navigation</h4>
                        <div class="fa-cheatsheet__row"><span>Command-Palette</span><div class="fa-kbd-combo"><kbd class="fa-kbd">⌘</kbd><kbd class="fa-kbd">K</kbd></div></div>
                        <div class="fa-cheatsheet__row"><span>Quick-Search</span><kbd class="fa-kbd">/</kbd></div>
                    </div>
                    <div class="fa-cheatsheet__group">
                        <h4>Aktionen</h4>
                        <div class="fa-cheatsheet__row"><span>Speichern</span><div class="fa-kbd-combo"><kbd class="fa-kbd">⌘</kbd><kbd class="fa-kbd">S</kbd></div></div>
                        <div class="fa-cheatsheet__row"><span>Schließen</span><kbd class="fa-kbd">Esc</kbd></div>
                    </div>
                </div>
            </div>
        </section>

        {# ── fa-pager ──────────────────────────────────────────────────── #}
        <section id="sec-pager" class="mb-5">
            <h2 style="font:700 18px var(--font-sans); color:var(--fg); margin:0 0 4px;">fa-pager</h2>
            <p style="color:var(--fg-2); font-size:13px; margin:0 0 16px;">
                Aurora pagination — pill shell with glow, gradient active page.
            </p>
            <div class="fa-pager-bar mb-3">
                <div class="fa-pager-bar__info"><strong>21–40</strong> von <strong>247</strong></div>
                <nav class="fa-pager" aria-label="Seitennavigation">
                    <button class="fa-pager__btn" aria-label="Erste Seite"><i class="fa-icon fa-icon--util-chevron-left"></i></button>
                    <button class="fa-pager__btn" aria-label="Vorherige"><i class="fa-icon fa-icon--util-chevron-left"></i></button>
                    <button class="fa-pager__btn" aria-label="Seite 1">1</button>
                    <button class="fa-pager__btn" aria-label="Seite 2">2</button>
                    <button class="fa-pager__btn is-active" aria-current="page" aria-label="Seite 3">3</button>
                    <span class="fa-pager__ellipsis">…</span>
                    <button class="fa-pager__btn" aria-label="Seite 13">13</button>
                    <button class="fa-pager__btn" aria-label="Nächste"><i class="fa-icon fa-icon--util-chevron-right"></i></button>
                    <button class="fa-pager__btn" aria-label="Letzte"><i class="fa-icon fa-icon--util-chevron-right"></i></button>
                </nav>
                <div class="fa-pager-perpage">
                    <label for="ds-perpage">Pro Seite</label>
                    <select id="ds-perpage"><option>20</option><option>50</option><option>100</option></select>
                </div>
            </div>
            {# compact #}
            <nav class="fa-pager fa-pager--compact" aria-label="Kompakt-Pager">
                <button class="fa-pager__btn"><i class="fa-icon fa-icon--util-chevron-left"></i></button>
                <button class="fa-pager__btn">1</button>
                <button class="fa-pager__btn is-active" aria-current="page">2</button>
                <button class="fa-pager__btn">3</button>
                <button class="fa-pager__btn"><i class="fa-icon fa-icon--util-chevron-right"></i></button>
            </nav>
        </section>

        {# ── fa-menu (dropdown) ────────────────────────────────────────── #}
        <section id="sec-fa-menu" class="mb-5">
            <h2 style="font:700 18px var(--font-sans); color:var(--fg); margin:0 0 4px;">fa-menu</h2>
            <p style="color:var(--fg-2); font-size:13px; margin:0 0 16px;">
                Dropdown / overflow-action menu. Toggle <code>.is-open</code> on <code>.fa-menu</code>.
            </p>
            <div style="min-height:220px; display:flex; align-items:flex-start; padding:24px; background:var(--surface-2); border:1px dashed var(--border); border-radius:var(--r-md);">
                <div class="fa-menu-anchor">
                    <button class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm" onclick="this.nextElementSibling.classList.toggle('is-open')" type="button">
                        <i class="fa-icon fa-icon--ui-more" aria-hidden="true"></i> Aktionen
                    </button>
                    <div class="fa-menu is-open">
                        <div class="fa-menu__group">
                            <a href="#" class="fa-menu__item"><i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i> Bearbeiten</a>
                            <a href="#" class="fa-menu__item"><i class="fa-icon fa-icon--ui-files" aria-hidden="true"></i> Duplizieren</a>
                        </div>
                        <div class="fa-menu__divider"></div>
                        <div class="fa-menu__group">
                            <a href="#" class="fa-menu__item fa-menu__item--danger"><i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> Löschen</a>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# fa-form-layout — Outline-Rail + Section-Cards                   #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="fa-form-layout">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-form-layout — Outline-Rail + Section-Cards</h2>
            </div>
            <div class="fa-section__body p-0">
                <p class="px-3 pt-3 pb-1" style="color:var(--fg-2); font-size:13px;">
                    Für 15+-Feld-Forms mit 4–10 thematischen Abschnitten. Persistenter Überblick mit Status
                    (done · current · error · pending), Progress-Bar, Auto-Save-Indicator, Collapse/Expand.
                    Verwendet Stimulus-Controller <code>form-layout</code> für Scrollspy, Live-Zählung und Jumplinks.
                </p>
                <div style="padding:0;">
                    {{ _fa_form.render({
                        eyebrow:  '<i class="fa-icon fa-icon--nav-shield"></i> DSGVO Art. 35 · DPIA',
                        title:    'Datenschutz-Folgenabschätzung',
                        subtitle: 'HR-Anwendungsserver 04 · ID DPIA-2026-014',
                        progress: { done: 5, total: 10 },
                        sections: [
                            { id: 'ds-sec-1', title: 'Grunddaten',       status: 'done',    fields: 5, filled: 5 },
                            { id: 'ds-sec-2', title: 'Verarbeitung',      status: 'done',    fields: 4, filled: 4 },
                            { id: 'ds-sec-3', title: 'Datenkategorien',   status: 'done',    fields: 3, filled: 3 },
                            { id: 'ds-sec-4', title: 'Empfänger',         status: 'done',    fields: 2, filled: 2 },
                            { id: 'ds-sec-5', title: 'Rechtsgrundlage',   status: 'done',    fields: 3, filled: 3 },
                            { id: 'ds-sec-6', title: 'Risiken',           status: 'current', fields: 5, filled: 2,
                              body: '<div class="fa-form-section__grid"><div class="fa-cyber-input"><div class="fa-cyber-input__header"><label class="fa-cyber-input__label">Eintrittswahrscheinlichkeit <span class="fa-cyber-input__req">*</span></label></div><div class="fa-cyber-input__frame"><input class="fa-cyber-input__field" type="text" value="Mittel"></div></div><div class="fa-cyber-input"><div class="fa-cyber-input__header"><label class="fa-cyber-input__label">Schweregrad <span class="fa-cyber-input__req">*</span></label></div><div class="fa-cyber-input__frame"><input class="fa-cyber-input__field" type="text" value="Hoch"></div></div></div>' },
                            { id: 'ds-sec-7', title: 'Maßnahmen',         status: 'pending', fields: 4, filled: 0 },
                            { id: 'ds-sec-8', title: 'DPO-Konsultation',  status: 'error',   fields: 3, filled: 1,
                              errorHint: 'DPO-Stellungnahme + Datum fehlen' },
                            { id: 'ds-sec-9', title: 'Restrisiko',        status: 'pending', fields: 2, filled: 0 },
                            { id: 'ds-sec-10', title: 'Freigabe',         status: 'pending', fields: 3, filled: 0 }
                        ],
                        actions: {
                            close:  { label: 'Als Entwurf schließen', variant: 'ghost' },
                            next:   { label: 'Nächster Abschnitt',    variant: 'secondary' },
                            submit: { label: 'DPIA abschließen',      variant: 'primary' }
                        }
                    }) }}
                </div>

                <details class="p-3" style="border-top:1px solid var(--border);">
                    <summary style="cursor:pointer; color:var(--primary); margin-bottom:.75rem; font-weight:600;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto; font-size:12px;"><code>{% verbatim %}{% import '_components/_fa_form_layout.html.twig' as _fa_form %}

{{ _fa_form.render({
    eyebrow:      'DSGVO Art. 35 · DPIA',
    title:        'Datenschutz-Folgenabschätzung',
    subtitle:     'HR-Anwendungsserver 04 · ID DPIA-2026-014',
    progress:     { done: 5, total: 10 },
    draftSavedAt: entity.updatedAt,
    sections: [
        { id: 'sec-1', title: 'Grunddaten',       status: 'done',    fields: 5, filled: 5 },
        { id: 'sec-6', title: 'Risiken',           status: 'current', fields: 5, filled: 2,
          body: include('dpia/_section_risks.html.twig') },
        { id: 'sec-8', title: 'DPO-Konsultation',  status: 'error',   fields: 3, filled: 1,
          errorHint: 'DPO-Stellungnahme + Datum fehlen' },
    ],
    actions: {
        close:  { label: 'Als Entwurf schließen', variant: 'ghost' },
        next:   { label: 'Nächster Abschnitt',    variant: 'secondary' },
        submit: { label: 'DPIA abschließen',      variant: 'primary' }
    }
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# fa-modal--wizard — High-stakes linear-flow modal              #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="fa-modal-wizard">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-modal--wizard — High-stakes Linear-Flow Modal</h2>
            </div>
            <div class="fa-section__body p-3">
                <p style="color:var(--fg-2); font-size:13px; margin-bottom:1.25rem;">
                    Für erzwungene lineare Flows wo Schritte vor dem Weitergehen validiert werden müssen.
                    4-stufiges Beispiel: DSGVO-Datenpanne Ersterfassung (Art. 33/34).
                    Verwendet Stimulus-Controller <code>modal-wizard</code> für Step-Navigation,
                    Validierungs-Gates und Cancel-Guard.
                </p>

                {# ── Live demo: rendered inline (no Bootstrap modal backdrop) ── #}
                <div style="background:var(--surface-2); border:1px solid var(--border); border-radius:var(--r-lg); padding:2rem; display:flex; justify-content:center; align-items:flex-start; min-height:520px;">
                    {{ _fa_modal_wizard.render({
                        id: 'ds-breach-wizard',
                        title: 'Datenpanne melden',
                        subtitle: 'DSGVO Art. 33/34 · Ersterfassung',
                        steps: [
                            {
                                id: 'step-severity',
                                label: 'Schweregrad',
                                title: '1. Schweregradeinstufung',
                                required: true,
                                validationHint: 'Bitte Schweregrad auswählen und Anzahl betroffener Systeme angeben.',
                                body: '<div style="display:grid;gap:14px;">'
                                    ~ '<div class="fa-cyber-input"><div class="fa-cyber-input__header"><label class="fa-cyber-input__label">Schweregrad <span class="fa-cyber-input__req">*</span></label></div><div class="fa-cyber-input__frame"><select class="fa-cyber-input__field" required><option value="">— bitte wählen —</option><option value="low">Niedrig</option><option value="medium">Mittel</option><option value="high">Hoch</option><option value="critical">Kritisch</option></select></div></div>'
                                    ~ '<div class="fa-cyber-input"><div class="fa-cyber-input__header"><label class="fa-cyber-input__label">Betroffene Systeme (Anzahl) <span class="fa-cyber-input__req">*</span></label></div><div class="fa-cyber-input__frame"><input class="fa-cyber-input__field" type="number" min="1" placeholder="z.B. 3" required></div></div>'
                                    ~ '</div>'
                            },
                            {
                                id: 'step-notification',
                                label: 'Meldepflicht',
                                title: '2. Meldepflichtentscheidung',
                                required: true,
                                validationHint: 'Bitte eine Option auswählen.',
                                body: '<div style="display:flex;flex-direction:column;gap:10px;">'
                                    ~ '<p style="font:500 13px/1.5 var(--font-sans); color:var(--fg-2); margin:0 0 6px;">Muss die Datenpanne der Aufsichtsbehörde gemeldet werden?</p>'
                                    ~ '<label style="display:flex;align-items:flex-start;gap:12px;padding:11px 13px;background:var(--surface);border:1px solid var(--border);border-radius:var(--r-md);cursor:pointer;"><input type="radio" name="ds-notification" value="yes" required style="margin-top:3px;"> <span><strong style="display:block;margin-bottom:2px;">Ja — Behörde benachrichtigen</strong><span style="font-size:12px;color:var(--fg-3);">Innerhalb von 72 Stunden nach Kenntnis (Art. 33 DSGVO)</span></span></label>'
                                    ~ '<label style="display:flex;align-items:flex-start;gap:12px;padding:11px 13px;background:var(--surface);border:1px solid var(--border);border-radius:var(--r-md);cursor:pointer;"><input type="radio" name="ds-notification" value="no" style="margin-top:3px;"> <span><strong style="display:block;margin-bottom:2px;">Nein — kein voraussichtliches Risiko</strong><span style="font-size:12px;color:var(--fg-3);">Interne Dokumentation gemäß Art. 33 Abs. 5 DSGVO ausreichend</span></span></label>'
                                    ~ '</div>'
                            },
                            {
                                id: 'step-record',
                                label: 'Erfassung',
                                title: '3. Vorfallbeschreibung',
                                required: true,
                                validationHint: 'Bitte Beschreibung ausfüllen.',
                                body: '<div style="display:grid;gap:14px;">'
                                    ~ '<div class="fa-cyber-input"><div class="fa-cyber-input__header"><label class="fa-cyber-input__label">Beschreibung der Datenpanne <span class="fa-cyber-input__req">*</span></label></div><div class="fa-cyber-input__frame"><textarea class="fa-cyber-input__field" rows="4" placeholder="Was ist passiert? Welche Daten sind betroffen?" required></textarea></div></div>'
                                    ~ '<div class="fa-cyber-input"><div class="fa-cyber-input__header"><label class="fa-cyber-input__label">Beweise / Anhänge <span style="color:var(--fg-3);font-weight:400;">(optional)</span></label></div><div class="fa-cyber-input__frame"><input class="fa-cyber-input__field" type="text" placeholder="Dateiname oder Ticket-Referenz"></div></div>'
                                    ~ '</div>'
                            },
                            {
                                id: 'step-review',
                                label: 'Prüfen',
                                title: '4. Zusammenfassung prüfen',
                                required: false,
                                body: '<div class="fa-modal__wizard-review-grid">'
                                    ~ '<div class="fa-modal__wizard-review-row"><div class="fa-modal__wizard-review-label">Schweregrad</div><div class="fa-modal__wizard-review-value">—</div></div>'
                                    ~ '<div class="fa-modal__wizard-review-row"><div class="fa-modal__wizard-review-label">Betroffene Systeme</div><div class="fa-modal__wizard-review-value">—</div></div>'
                                    ~ '<div class="fa-modal__wizard-review-row"><div class="fa-modal__wizard-review-label">Meldepflicht</div><div class="fa-modal__wizard-review-value">—</div></div>'
                                    ~ '<div class="fa-modal__wizard-review-row"><div class="fa-modal__wizard-review-label">Beschreibung</div><div class="fa-modal__wizard-review-value">—</div></div>'
                                    ~ '</div>'
                                    ~ '<p style="font:500 12px/1.6 var(--font-sans);color:var(--fg-3);margin:14px 0 0;"><i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> Nach dem Absenden wird ein Workflow zur Behördenmeldung gestartet (sofern Meldepflicht bejaht).</p>'
                            }
                        ],
                        actions: {
                            cancel: { label: 'Abbrechen' },
                            submit: { label: 'Datenpanne erfassen', variant: 'danger' }
                        }
                    }) }}
                </div>

                <details class="mt-3">
                    <summary style="cursor:pointer; color:var(--primary); font-weight:600; padding:.5rem 0;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto; font-size:12px; margin-top:.75rem;"><code>{% verbatim %}{% import '_components/_fa_modal_wizard.html.twig' as _fa_modal_wizard %}

{{ _fa_modal_wizard.render({
    id: 'breach-wizard',
    title: 'Datenpanne melden',
    subtitle: 'DSGVO Art. 33/34 · Ersterfassung',
    steps: [
        {
            id: 'severity',
            label: 'Schweregrad',
            title: '1. Schweregradeinstufung',
            required: true,
            validationHint: 'Bitte Schweregrad und Systemanzahl angeben.',
            body: include('data_breach/_step_severity.html.twig'),
        },
        {
            id: 'notification',
            label: 'Meldepflicht',
            title: '2. Meldepflichtentscheidung',
            required: true,
            validationHint: 'Bitte eine Option auswählen.',
            body: include('data_breach/_step_notification.html.twig'),
        },
        {
            id: 'record',
            label: 'Erfassung',
            title: '3. Vorfallbeschreibung',
            required: true,
            validationHint: 'Bitte Beschreibung ausfüllen.',
            body: include('data_breach/_step_record.html.twig'),
        },
        {
            id: 'review',
            label: 'Prüfen',
            title: '4. Zusammenfassung prüfen',
            required: false,
            body: include('data_breach/_step_review.html.twig'),
        },
    ],
    actions: {
        cancel: { label: 'Abbrechen' },
        submit: { label: 'Datenpanne erfassen', variant: 'danger' },
    },
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# fa-tabs — Tab-group for Settings-Pages                         #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="fa-tabs">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-tabs — Tab-Group für Settings-Pages</h2>
            </div>
            <div class="fa-section__body p-3">
                <p style="color:var(--fg-2); font-size:13px; margin-bottom:1.25rem;">
                    Tab-Group für Settings-Pages mit 3–6 gleichberechtigten Gruppen.
                    NICHT für regulatorische Formulare (→ fa-form-layout) oder lineare Flows (→ fa-modal--wizard).
                    Verwendet Stimulus-Controller <code>tabs</code> für Keyboard-Navigation, ARIA-Management und URL-Hash-Sync.
                </p>

                {# ── Live demo ─────────────────────────────────────────────── #}
                <div style="background:var(--surface-2); border:1px solid var(--border); border-radius:var(--r-lg); padding:2rem;">
                    {{ _fa_tabs.render({
                        id: 'design-system-tabs-demo',
                        tabs: [
                            {
                                id: 'tab-general',
                                label: 'Allgemein'|trans({}, 'messages'),
                                icon: 'ui-settings',
                                body: '<p style="margin:0;color:var(--fg-2);">Tab-Inhalt 1 — Allgemeine Einstellungen</p>'
                            },
                            {
                                id: 'tab-branding',
                                label: 'Branding'|trans({}, 'messages'),
                                icon: 'nav-palette',
                                body: '<p style="margin:0;color:var(--fg-2);">Tab-Inhalt 2 — Branding-Konfiguration</p>'
                            },
                            {
                                id: 'tab-security',
                                label: 'Sicherheit'|trans({}, 'messages'),
                                icon: 'ui-lock',
                                body: '<p style="margin:0;color:var(--fg-2);">Tab-Inhalt 3 — Sicherheitseinstellungen</p>'
                            },
                            {
                                id: 'tab-audit',
                                label: 'Audit'|trans({}, 'messages'),
                                icon: 'audit-trail',
                                body: '<p style="margin:0;color:var(--fg-2);">Tab-Inhalt 4 — Audit-Protokoll</p>'
                            }
                        ],
                        initialTab: 'tab-general',
                        urlHashSync: false
                    }) }}
                </div>

                <details class="mt-3">
                    <summary style="cursor:pointer; color:var(--primary); font-weight:600; padding:.5rem 0;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto; font-size:12px; margin-top:.75rem;"><code>{% verbatim %}{% import '_components/_fa_tabs.html.twig' as _fa_tabs %}

{{ _fa_tabs.render({
    id: 'tenant-settings',
    tabs: [
        {
            id: 'general',
            label: 'Allgemein'|trans({}, 'messages'),
            icon: 'ui-settings',
            body: include('tenant/_tab_general.html.twig'),
        },
        {
            id: 'branding',
            label: 'Branding'|trans({}, 'messages'),
            icon: 'nav-palette',
            body: include('tenant/_tab_branding.html.twig'),
        },
        {
            id: 'security',
            label: 'Sicherheit'|trans({}, 'messages'),
            icon: 'ui-lock',
            body: include('tenant/_tab_security.html.twig'),
            errors: 2,
        },
        {
            id: 'audit',
            label: 'Audit'|trans({}, 'messages'),
            icon: 'audit-trail',
            body: include('tenant/_tab_audit.html.twig'),
            disabled: true,
        },
    ],
    initialTab: 'general',
    urlHashSync: true,
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# fa-modal--confirm — Destructive / approval confirmation        #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="fa-modal-confirm">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-modal--confirm — Destructive / Approval Confirmation</h2>
            </div>
            <div class="fa-section__body p-3">
                <p style="color:var(--fg-2); font-size:13px; margin-bottom:1.25rem;">
                    Modaler Bestätigungs-Dialog für Lösch-, Freigabe-, Ablehnungs-Aktionen.
                    4 Tone-Modifier (<code>neutral</code> / <code>danger</code> / <code>warning</code> / <code>success</code>),
                    optionale Type-to-Confirm-Phrase + Cooldown-Sekunden für Hochrisiko-Aktionen.
                    Stimulus-Controller <code>fa-modal</code> (Shell) + optional <code>fa-confirm</code> (Phrase/Cooldown).
                </p>

                {# ── Trigger buttons ────────────────────────────────────── #}
                <div style="display:flex; flex-wrap:wrap; gap:.5rem; margin-bottom:1rem;">
                    <button type="button"
                            class="fa-cyber-btn fa-cyber-btn--ghost"
                            data-controller="fa-modal-dispatcher"
                            data-action="click->fa-modal-dispatcher#open"
                            data-fa-modal-id="ds-confirm-neutral">
                        Neutral öffnen
                    </button>
                    <button type="button"
                            class="fa-cyber-btn fa-cyber-btn--warning"
                            data-controller="fa-modal-dispatcher"
                            data-action="click->fa-modal-dispatcher#open"
                            data-fa-modal-id="ds-confirm-warning">
                        Warning öffnen
                    </button>
                    <button type="button"
                            class="fa-cyber-btn fa-cyber-btn--danger"
                            data-controller="fa-modal-dispatcher"
                            data-action="click->fa-modal-dispatcher#open"
                            data-fa-modal-id="ds-confirm-danger">
                        Danger (type-to-confirm) öffnen
                    </button>
                    <button type="button"
                            class="fa-cyber-btn fa-cyber-btn--primary"
                            data-controller="fa-modal-dispatcher"
                            data-action="click->fa-modal-dispatcher#open"
                            data-fa-modal-id="ds-confirm-success">
                        Success öffnen
                    </button>
                </div>

                {# ── Modal instances ───────────────────────────────────── #}
                {{ _fa_modal.confirm({
                    id: 'ds-confirm-neutral',
                    tone: 'neutral',
                    title: 'Änderung übernehmen?',
                    message: 'Möchten Sie die aktuelle Auswahl als Standard speichern?',
                    confirmLabel: 'Übernehmen',
                }) }}

                {{ _fa_modal.confirm({
                    id: 'ds-confirm-warning',
                    tone: 'warning',
                    title: 'Audit abschließen?',
                    message: 'Nach dem Abschluss können keine weiteren Findings hinzugefügt werden.',
                    confirmLabel: 'Abschließen',
                    submitIcon: 'ui-check',
                }) }}

                {{ _fa_modal.confirm({
                    id: 'ds-confirm-danger',
                    tone: 'danger',
                    title: 'API-Key dauerhaft widerrufen?',
                    sub: 'Der Key <code>lih_live_91de…</code> wird sofort ungültig.',
                    diff: [
                        { label: 'Aktive Verbindungen', value: '14 → 0' },
                        { label: 'Reporting-Service',   value: 'offline', tone: 'danger' },
                    ],
                    typeToConfirm: 'reporting-service',
                    confirmHint: 'Tippe <code>reporting-service</code> zum Bestätigen',
                    cooldownSeconds: 5,
                    confirmLabel: 'Endgültig widerrufen',
                    submitIcon: 'shield-x',
                }) }}

                {{ _fa_modal.confirm({
                    id: 'ds-confirm-success',
                    tone: 'success',
                    title: 'Genehmigung freigeben?',
                    message: 'Die Richtlinie wird sofort für alle Tenant-User wirksam.',
                    confirmLabel: 'Freigeben',
                    submitIcon: 'ui-check',
                }) }}

                <details class="mt-3">
                    <summary style="cursor:pointer; color:var(--primary); font-weight:600; padding:.5rem 0;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto; font-size:12px; margin-top:.75rem;"><code>{% verbatim %}{% import '_components/_fa_modal.html.twig' as _fa_modal %}

{{ _fa_modal.confirm({
    id:              'delete-risk',
    tone:            'danger',
    title:           'Risiko löschen?',
    message:         'Diese Aktion ist permanent.',
    typeToConfirm:   'risk-12345',
    cooldownSeconds: 5,
    confirmLabel:    'Löschen',
    formAction:      path('risk_delete', { id: 12345 }),
    formCsrfToken:   csrf_token('risk_delete'),
}) }}

{# Open trigger anywhere on the page #}
<button type="button"
        data-controller="fa-modal-dispatcher"
        data-action="click->fa-modal-dispatcher#open"
        data-fa-modal-id="delete-risk">
    Risiko löschen
</button>{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

        {# ═══════════════════════════════════════════════════════════════ #}
        {# fa-modal--settings — Preferences / configuration               #}
        {# ═══════════════════════════════════════════════════════════════ #}
        <section class="fa-section mb-5" id="fa-modal-settings">
            <div class="fa-section__header">
                <h2 class="fa-section__title">fa-modal--settings — Preferences / Configuration</h2>
            </div>
            <div class="fa-section__body p-3">
                <p style="color:var(--fg-2); font-size:13px; margin-bottom:1.25rem;">
                    Modaler Settings-Dialog für Präferenz-Toggles, Widget-Sichtbarkeit, Konfigurations-Listen.
                    Body wird als pre-gerenderter HTML-Block übergeben (typisch <code>include(…)</code>).
                </p>

                <div style="display:flex; flex-wrap:wrap; gap:.5rem; margin-bottom:1rem;">
                    <button type="button"
                            class="fa-cyber-btn fa-cyber-btn--primary"
                            data-controller="fa-modal-dispatcher"
                            data-action="click->fa-modal-dispatcher#open"
                            data-fa-modal-id="ds-settings-dashboard">
                        Dashboard anpassen öffnen
                    </button>
                </div>

                {{ _fa_modal.settings({
                    id: 'ds-settings-dashboard',
                    icon: 'sliders',
                    title: 'Dashboard anpassen',
                    subtitle: 'Sichtbarkeit der Widgets',
                    body: '<div style="display:flex;flex-direction:column;gap:8px;">'
                        ~ '<label style="display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:var(--r-md);cursor:pointer;"><input type="checkbox" checked> <span><strong style="display:block;margin-bottom:2px;">KPI-Tile: Risiken</strong><span style="font-size:11.5px;color:var(--fg-3);">Aggregierte Risiko-Score-Übersicht</span></span></label>'
                        ~ '<label style="display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:var(--r-md);cursor:pointer;"><input type="checkbox" checked> <span><strong style="display:block;margin-bottom:2px;">KPI-Tile: Audit-Findings</strong><span style="font-size:11.5px;color:var(--fg-3);">Offene Findings nach Schweregrad</span></span></label>'
                        ~ '<label style="display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:var(--r-md);cursor:pointer;"><input type="checkbox"> <span><strong style="display:block;margin-bottom:2px;">Workflow-Inbox</strong><span style="font-size:11.5px;color:var(--fg-3);">Anstehende Freigaben + Reviews</span></span></label>'
                        ~ '<label style="display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:var(--r-md);cursor:pointer;"><input type="checkbox" checked> <span><strong style="display:block;margin-bottom:2px;">DPIA-Status</strong><span style="font-size:11.5px;color:var(--fg-3);">GDPR Art. 35 Datenschutz-Folgenabschätzungen</span></span></label>'
                        ~ '</div>',
                    primaryLabel: 'Speichern',
                    primaryIcon: 'ui-check',
                }) }}

                <details class="mt-3">
                    <summary style="cursor:pointer; color:var(--primary); font-weight:600; padding:.5rem 0;">Code kopieren</summary>
                    <pre style="background:var(--surface-2); border:1px solid var(--border); border-radius:6px; padding:1rem; overflow-x:auto; font-size:12px; margin-top:.75rem;"><code>{% verbatim %}{% import '_components/_fa_modal.html.twig' as _fa_modal %}

{{ _fa_modal.settings({
    id:            'dashboard-customize',
    icon:          'sliders',
    title:         'Dashboard anpassen',
    subtitle:      'Sichtbarkeit der Widgets',
    body:          include('dashboard/_widget_toggles.html.twig'),
    primaryLabel:  'Speichern',
    formAction:    path('dashboard_settings_save'),
    formCsrfToken: csrf_token('dashboard_settings'),
}) }}{% endverbatim %}</code></pre>
                </details>
            </div>
        </section>

    </div>{# /container-xl #}
</main>
{% endblock %}
