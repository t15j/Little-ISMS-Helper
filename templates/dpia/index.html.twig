{% extends 'base.html.twig' %}
{% import '_components/_fa_alva_hint.html.twig' as _fa_alva_hint %}
{% trans_default_domain 'privacy' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
{% block title %}{{ 'dpia.index.title'|trans({}, 'privacy')}}{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.privacy'|trans({}, 'nav'), url: path('app_processing_activity_index') },
            { label: 'nav.dpia'|trans({}, 'nav') }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'nav-clipboard-check', label: 'DPIA / DSFA' },
        title: 'dpia.index.page_title'|trans({}, 'privacy'),
        subtitle: 'dpia.index.page_subtitle'|trans({}, 'privacy'),
        actions: [
            { label: 'dpia.index.button_dashboard'|trans({}, 'privacy'), variant: 'ghost', icon: 'nav-dashboard', href: path('app_dpia_dashboard') },
            { label: 'dpia.index.button_new_dpia'|trans({}, 'privacy'), variant: 'primary', icon: 'plus', href: path('app_dpia_new') }
        ]
    }) }}
{% set _global_hints = alva_hints('dpia_index') %}
{% if _global_hints|length > 0 %}
    {% for _h in _global_hints %}
        {{ _fa_alva_hint.render(_h) }}
    {% endfor %}
{% endif %}


    <main class="fa-aurora-surface">

    {# Overall Compliance Score #}
    {% embed '_components/_card.html.twig' with {class: 'mb-4 text-center'} %}
        {% block card_body %}
            <div class="text-muted mb-2 fw-semibold">{{ 'dpia.index.compliance_score'|trans({}, 'privacy') }}</div>
            {% set score = compliance_score.overall_score %}
            <div class="display-1 mb-0
                {% if score >= 80 %}text-success
                {% elseif score >= 60 %}text-warning
                {% else %}text-danger{% endif %}">
                {{ score }}%
            </div>
            <p class="text-muted mt-2">
                {{ compliance_score.approved_dpias }} {{ 'dpia.index.of'|trans({}, 'privacy') }} {{ compliance_score.total_dpias }} {{ 'dpia.index.dpias_approved'|trans({}, 'privacy') }}
                {% if compliance_score.due_for_review > 0 %}
                    · {{ compliance_score.due_for_review }} {{ 'dpia.index.due_for_review'|trans({}, 'privacy') }}
                {% endif %}
            </p>
        {% endblock %}
    {% endembed %}

    {# Key Metrics #}
    <div class="row mb-4">
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    <div class="text-muted fw-semibold">{{ 'dpia.index.total_dpias'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold">{{ statistics.total }}</div>
                    <div class="mt-2">
                        <small class="text-success"><i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i> {{ statistics.approved }} {{ 'dpia.index.approved'|trans({}, 'privacy') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    <div class="text-muted fw-semibold">{{ 'dpia.index.in_review'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold text-info">{{ statistics.in_review }}</div>
                    <div class="mt-2">
                        <small class="text-muted"><i class="fa-icon fa-icon--status-pending" aria-hidden="true"></i> {{ 'dpia.index.awaiting_approval'|trans({}, 'privacy') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    <div class="text-muted fw-semibold">{{ 'dpia.index.high_risk'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold text-danger">{{ statistics.high_risk }}</div>
                    <div class="mt-2">
                        <small class="text-warning"><i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ 'dpia.index.requires_attention'|trans({}, 'privacy') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-3">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                    <div class="text-muted fw-semibold">{{ 'dpia.index.completeness'|trans({}, 'privacy') }}</div>
                    <div class="fs-2 fw-bold">{{ statistics.completeness_rate }}%</div>
                    {{ _fa_progress.render({ value: statistics.completeness_rate, tone: 'success', class: 'mt-2' }) }}
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Filters #}
    {% embed '_components/_search_filter_form.html.twig' with {
        action: path('app_dpia_index'),
        filter_keys: ['filter'],
        show_search: false,
        form_id: 'dpia-filter-form'
    } %}
        {% block filters %}
            <div class="col-md-4">
                <label for="filter-status" class="form-label">{{ 'dpia.index.filter_dpias'|trans({}, 'privacy') }}</label>
                <select class="form-select fa-cyber-input__field" id="filter-status" name="filter">
                    <option value="">{{ 'dpia.index.all'|trans({}, 'privacy') }} ({{ statistics.total }})</option>
                    <option value="draft" {{ current_filter == 'draft' ? 'selected' : '' }}>{{ 'dpia.index.draft'|trans({}, 'privacy') }} ({{ statistics.draft }})</option>
                    <option value="in_review" {{ current_filter == 'in_review' ? 'selected' : '' }}>{{ 'dpia.index.in_review'|trans({}, 'privacy') }} ({{ statistics.in_review }})</option>
                    <option value="approved" {{ current_filter == 'approved' ? 'selected' : '' }}>{{ 'dpia.index.approved_filter'|trans({}, 'privacy') }} ({{ statistics.approved }})</option>
                    <option value="high_risk" {{ current_filter == 'high_risk' ? 'selected' : '' }}>{{ 'dpia.index.high_risk_filter'|trans({}, 'privacy') }} ({{ statistics.high_risk }})</option>
                    <option value="incomplete" {{ current_filter == 'incomplete' ? 'selected' : '' }}>{{ 'dpia.index.incomplete'|trans({}, 'privacy') }}</option>
                    <option value="awaiting_dpo" {{ current_filter == 'awaiting_dpo' ? 'selected' : '' }}>{{ 'dpia.index.awaiting_dpo'|trans({}, 'privacy') }} ({{ statistics.awaiting_dpo_consultation }})</option>
                    <option value="requires_supervisory" {{ current_filter == 'requires_supervisory' ? 'selected' : '' }}>{{ 'dpia.index.requires_supervisory'|trans({}, 'privacy') }} ({{ statistics.requires_supervisory_consultation }})</option>
                </select>
            </div>
        {% endblock %}
    {% endembed %}

    {# DPIA Table #}
    {% embed '_components/_card.html.twig' with {noPadding: true} %}
        {% block card_attrs %}data-controller="bulk-actions" data-bulk-actions-endpoint-value="/{{ app.request.locale }}/dpia"{% endblock %}
        {% block card_header %}
            <div class="card-header"><h2 class="h5 mb-0"><i class="fa-icon fa-icon--evidence" aria-hidden="true"></i> {{ 'dpia.index.dpias_list'|trans({}, 'privacy') }} ({{ dpias|length }})</h2></div>
        {% endblock %}
        {% block card_body %}
            {% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
            {% if dpias|length > 0 %}
                {% embed '_components/_table.html.twig' with {
                    'noMargin': true
                } %}
                    {% block table_head %}
                        <tr>
                            <th scope="col" class="bulk-select-column">
                                <input type="checkbox"
                                       class="form-check-input"
                                       data-action="bulk-actions#selectAll"
                                       data-bulk-actions-target="selectAllCheckbox"
                                       aria-label="{{ 'bulk_actions.select_all'|trans({}, 'messages') }}">
                            </th>
                            <th scope="col">{{ 'dpia.index.reference'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.title_col'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.status'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.risk_level'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.residual_risk'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.completeness'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.dpo'|trans({}, 'privacy') }}</th>
                            <th scope="col">{{ 'dpia.index.actions'|trans({}, 'privacy') }}</th>
                        </tr>
                    {% endblock %}
                    {% block table_body %}
                        {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                        {% for dpia in dpias %}
                            <tr>
                                <td class="bulk-select-column">
                                    <input type="checkbox"
                                           class="form-check-input"
                                           data-bulk-actions-target="item"
                                           data-action="bulk-actions#selectItem"
                                           value="{{ dpia.id }}"
                                           aria-label="{{ 'common.select'|trans({}, 'messages') }} {{ dpia.referenceNumber }}">
                                </td>
                                <td>
                                    <a href="{{ path('app_dpia_show', {id: dpia.id}) }}">
                                        {{ dpia.referenceNumber }}
                                    </a>
                                </td>
                                <td>{{ dpia.title }}</td>
                                <td>
                                    {% set status_class = {
                                        'draft': 'secondary',
                                        'in_review': 'info',
                                        'approved': 'success',
                                        'rejected': 'danger',
                                        'requires_revision': 'warning'
                                    }[dpia.status] %}
                                    {% include '_components/_badge.html.twig' with { variant: status_class, content: dpia.status|replace({'_': ' '})|upper } %}
                                </td>
                                <td>
                                    {% if dpia.riskLevel %}
                                        {% set risk_class = {
                                            'low': 'success',
                                            'medium': 'info',
                                            'high': 'warning',
                                            'critical': 'danger'
                                        }[dpia.riskLevel] %}
                                        {% include '_components/_badge.html.twig' with { variant: risk_class, content: dpia.riskLevel|upper } %}
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if dpia.residualRiskLevel %}
                                        {% set risk_class = {
                                            'low': 'success',
                                            'medium': 'info',
                                            'high': 'warning',
                                            'critical': 'danger'
                                        }[dpia.residualRiskLevel] %}
                                        {% include '_components/_badge.html.twig' with { variant: risk_class, content: dpia.residualRiskLevel|upper } %}
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% set percentage = dpia.completenessPercentage %}
                                    {{ _fa_progress.render({ value: percentage, tone: percentage == 100 ? 'success' : 'warning', showText: true }) }}
                                </td>
                                <td>
                                    {% if dpia.dpoConsultationDate %}
                                        <i class="fa-icon fa-icon--status-ok" aria-hidden="true" title="{{ 'dpia.index.dpo_consulted'|trans({}, 'privacy') }} {{ dpia.dpoConsultationDate|date('Y-m-d') }}"></i>
                                    {% else %}
                                        <i class="fa-icon fa-icon--status-critical" aria-hidden="true" title="{{ 'dpia.index.dpo_not_yet_consulted'|trans({}, 'privacy') }}"></i>
                                    {% endif %}
                                </td>
                                <td>
                                    <div class="btn-group btn-group-sm" role="group" aria-label="{{ 'common.actions'|trans({}, 'messages') }}">
                                        <a href="{{ path('app_dpia_show', {id: dpia.id}) }}"
                                           class="fa-cyber-btn fa-cyber-btn--ghost"
                                           title="{{ 'common.view'|trans({}, 'messages') }}" aria-label="{{ 'common.view'|trans({}, 'messages') }}">
                                            <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i>
                                        </a>
                                        {% if dpia.status in ['draft', 'requires_revision'] %}
                                            <a href="{{ path('app_dpia_edit', {id: dpia.id}) }}"
                                               class="fa-cyber-btn fa-cyber-btn--ghost"
                                               title="{{ 'common.edit'|trans({}, 'messages') }}" aria-label="{{ 'common.edit'|trans({}, 'messages') }}">
                                                <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i>
                                            </a>
                                        {% endif %}
                                        <a href="{{ path('app_dpia_export_pdf', {id: dpia.id}) }}"
                                           class="fa-cyber-btn fa-cyber-btn--success"
                                           title="{{ 'common.export_pdf'|trans({}, 'messages') }}" aria-label="{{ 'common.export_pdf'|trans({}, 'messages') }}">
                                            <i class="fa-icon fa-icon--nav-file-pdf" aria-hidden="true"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                    {% endblock %}
                {% endembed %}

                {# Bulk Action Bar #}
                {% include '_components/_bulk_action_bar.html.twig' with {
                    actions: ['export', 'delete']
                } %}
            {% else %}
                {{ _fa_empty_state.render({
                    mood: 'sleeping',
                    title: 'dpia.index.no_dpias_found'|trans({}, 'privacy'),
                    actions: [
                        { label: 'dpia.index.create_first_dpia'|trans({}, 'privacy'), variant: 'primary', icon: 'plus', href: path('app_dpia_new') }
                    ]
                }) }}
            {% endif %}
        {% endblock %}
    {% endembed %}
    </main>
</div>
{% endblock %}
