{% extends 'base.html.twig' %}
{% import '_components/_fa_alva_hint.html.twig' as _fa_alva_hint %}
{% trans_default_domain 'risk_treatment_plan' %}

{% block title %}{{ 'risk_treatment_plan.title'|trans({}, 'risk_treatment_plan') }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}
{% import '_components/_fa_progress.html.twig' as _fa_progress %}
{% import '_components/_fa_cyber_field.html.twig' as _fa_cyber %}

{% include '_components/_breadcrumb.html.twig' with {
    breadcrumbs: [
        { label: 'nav.risks'|trans({}, 'nav'), url: path('app_risk_index') },
        { label: 'nav.treatment_plans'|trans({}, 'nav') }
    ]
} %}

{{ _fa_page_header.render({
    badge: { icon: 'nav-clipboard-check', label: 'nav.treatment_plans'|trans({}, 'nav') },
    title: 'risk_treatment_plan.title'|trans({}, 'risk_treatment_plan'),
    subtitle: 'risk_treatment_plan.description'|trans({}, 'risk_treatment_plan'),
    actions: [
        { label: 'risk_treatment_plan.new'|trans({}, 'risk_treatment_plan'), variant: 'primary', icon: 'plus', href: path('app_risk_treatment_plan_new') }
    ]
}) }}
{% set _global_hints = alva_hints('risk_treatment_plan_index') %}
{% if _global_hints|length > 0 %}
    {% for _h in _global_hints %}
        {{ _fa_alva_hint.render(_h) }}
    {% endfor %}
{% endif %}


<main class="fa-aurora-surface">
<div class="stats-grid mb-4">
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'risk_treatment_plan.stats.total'|trans({}, 'risk_treatment_plan'), value: stats.total, variant: 'primary' }) }}
    </div>
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'risk_treatment_plan.stats.planned'|trans({}, 'risk_treatment_plan'), value: stats.planned, variant: 'secondary' }) }}
    </div>
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'risk_treatment_plan.stats.in_progress'|trans({}, 'risk_treatment_plan'), value: stats.in_progress, variant: 'info' }) }}
    </div>
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'risk_treatment_plan.stats.completed'|trans({}, 'risk_treatment_plan'), value: stats.completed, variant: 'success' }) }}
    </div>
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'risk_treatment_plan.stats.overdue'|trans({}, 'risk_treatment_plan'), value: overduePlans|length, variant: 'danger' }) }}
    </div>
    <div class="col-auto">
        {{ _fa_feature_card.render({ label: 'risk_treatment_plan.stats.avg_completion'|trans({}, 'risk_treatment_plan'), value: stats.avg_completion|number_format(0) ~ '%', variant: 'warning' }) }}
    </div>
</div>

{# Skip link for filters section - WCAG 2.4.1 #}
<a href="#filter-section" class="skip-link">{{ 'accessibility.skip_to_filters'|trans({}, 'messages') }}</a>

{# Filter Section #}
{% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
    {% block card_header %}
        <div class="card-header">
            <h2><i class="fa-icon fa-icon--ui-funnel" aria-hidden="true"></i> {{ 'common.filters'|trans({}, 'messages') }}</h2>
        </div>
    {% endblock %}
    {% block card_body %}
        {% import '_components/_fa_cyber_field.html.twig' as _fa_cyber %}
        <form method="get" action="{{ path('app_risk_treatment_plan_index') }}" class="row g-3" role="search" aria-label="{{ 'common.filters'|trans({}, 'messages') }}" id="filter-section" tabindex="-1">
            <div class="col-md-3">
                <label for="filter-status" class="form-label">{{ 'risk_treatment_plan.field.status'|trans({}, 'risk_treatment_plan') }}</label>
                <select class="form-select fa-cyber-input__field" id="filter-status" name="status" data-controller="ui-actions" data-action="change->ui-actions#autoSubmit">
                    <option value="">{{ 'common.all'|trans({}, 'messages') }}</option>
                    <option value="planned" {{ app.request.query.get('status') == 'planned' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.planned'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="in_progress" {{ app.request.query.get('status') == 'in_progress' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.in_progress'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="completed" {{ app.request.query.get('status') == 'completed' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.completed'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="on_hold" {{ app.request.query.get('status') == 'on_hold' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.on_hold'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="cancelled" {{ app.request.query.get('status') == 'cancelled' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.cancelled'|trans({}, 'risk_treatment_plan') }}</option>
                </select>
            </div>
            <div class="col-md-3">
                <label for="filter-priority" class="form-label">{{ 'risk_treatment_plan.field.priority'|trans({}, 'risk_treatment_plan') }}</label>
                <select class="form-select fa-cyber-input__field" id="filter-priority" name="priority" data-controller="ui-actions" data-action="change->ui-actions#autoSubmit">
                    <option value="">{{ 'common.all'|trans({}, 'messages') }}</option>
                    <option value="critical" {{ app.request.query.get('priority') == 'critical' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.critical'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="high" {{ app.request.query.get('priority') == 'high' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.high'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="medium" {{ app.request.query.get('priority') == 'medium' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.medium'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="low" {{ app.request.query.get('priority') == 'low' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.low'|trans({}, 'risk_treatment_plan') }}</option>
                </select>
            </div>
            <div class="col-md-3">
                {{ _fa_cyber.text('responsible_person', {
                    id: 'filter-responsible',
                    label: 'risk_treatment_plan.field.responsible_person'|trans({}, 'risk_treatment_plan'),
                    value: app.request.query.get('responsible_person'),
                    placeholder: 'common.search'|trans({}, 'messages') ~ '...'
                }) }}
            </div>
            <div class="col-md-3">
                <label for="filter-overdue" class="form-label">{{ 'risk_treatment_plan.filter.overdue'|trans({}, 'risk_treatment_plan') }}</label>
                <select class="form-select fa-cyber-input__field" id="filter-overdue" name="overdue_only" data-controller="ui-actions" data-action="change->ui-actions#autoSubmit">
                    <option value="">{{ 'common.all'|trans({}, 'messages') }}</option>
                    <option value="1" {{ app.request.query.get('overdue_only') == '1' ? 'selected' : '' }}>{{ 'risk_treatment_plan.filter.overdue_only'|trans({}, 'risk_treatment_plan') }}</option>
                </select>
            </div>
            <div class="col-12">
                <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                    <i class="fa-icon fa-icon--ui-search" aria-hidden="true"></i> {{ 'common.filter'|trans({}, 'messages') }}
                </button>
                <a href="{{ path('app_risk_treatment_plan_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                    <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> {{ 'common.reset'|trans({}, 'messages') }}
                </a>
            </div>
        </form>
    {% endblock %}
{% endembed %}

{# Treatment Plans Table #}
{% embed '_components/_card.html.twig' %}
    {% block card_header %}
        <div class="card-header">
            <h2><i class="fa-icon fa-icon--ui-list" aria-hidden="true"></i> {{ 'risk_treatment_plan.list.title'|trans({}, 'risk_treatment_plan') }} ({{ plans|length }})</h2>
        </div>
    {% endblock %}
    {% block card_body %}
    {% embed '_components/_table.html.twig' with {
        'noMargin': true
    } %}
        {% block table_head %}
            <tr>
                <th scope="col">{{ 'risk_treatment_plan.field.title'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col">{{ 'risk_treatment_plan.field.risk'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'risk_treatment_plan.field.status'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'risk_treatment_plan.field.priority'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col">{{ 'risk_treatment_plan.field.responsible_person'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'risk_treatment_plan.field.completion'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col">{{ 'risk_treatment_plan.field.target_date'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'common.actions'|trans({}, 'messages') }}</th>
            </tr>
        {% endblock %}
        {% block table_body %}
            {% import '_components/_fa_progress.html.twig' as _fa_progress %}
            {% for plan in plans %}
            <tr class="{{ plan.isOverdue() ? 'table-warning' : '' }}">
                <td>
                    <strong>{{ plan.title }}</strong>
                    {% if plan.isOverdue() %}
                        {% include '_components/_badge.html.twig' with { variant: 'danger', class: 'ms-2', icon: 'status-warning', content: 'risk_treatment_plan.overdue'|trans({}, 'risk_treatment_plan') } %}
                    {% endif %}
                </td>
                <td>
                    {% if plan.risk %}
                        <a href="{{ path('app_risk_show', {id: plan.risk.id}) }}">{{ plan.risk.title }}</a>
                    {% else %}
                        <span class="text-muted">-</span>
                    {% endif %}
                </td>
                <td class="text-center">
                    {% include '_components/_badge.html.twig' with { variant: plan.status == 'completed' ? 'success' : (plan.status == 'in_progress' ? 'primary' : (plan.status == 'cancelled' ? 'secondary' : (plan.status == 'on_hold' ? 'warning' : 'info'))), content: ('risk_treatment_plan.status.' ~ plan.status)|trans({}, 'risk_treatment_plan')|upper } %}
                </td>
                <td class="text-center">
                    {% include '_components/_badge.html.twig' with { variant: plan.priority == 'critical' ? 'danger' : (plan.priority == 'high' ? 'warning' : (plan.priority == 'medium' ? 'info' : 'secondary')), content: ('risk_treatment_plan.priority.' ~ plan.priority)|trans({}, 'risk_treatment_plan')|upper } %}
                </td>
                <td>
                    {{ plan.effectiveResponsiblePerson ?? '<span class="text-muted">-</span>'|raw }}
                </td>
                <td class="text-center">
                    {{ _fa_progress.render({ value: plan.completionPercentage, tone: plan.completionPercentage >= 75 ? 'success' : (plan.completionPercentage >= 50 ? 'info' : (plan.completionPercentage >= 25 ? 'warning' : 'danger')), showText: true }) }}
                </td>
                <td>
                    {{ plan.targetCompletionDate|date('Y-m-d') }}
                    <br>
                    <small class="text-muted">
                        {% set daysUntil = plan.daysUntilTarget %}
                        {% if daysUntil < 0 %}
                            <span class="text-danger">{{ daysUntil|abs }} {{ 'risk_treatment_plan.days_overdue'|trans({}, 'risk_treatment_plan') }}</span>
                        {% elseif daysUntil == 0 %}
                            <span class="text-warning">{{ 'risk_treatment_plan.due_today'|trans({}, 'risk_treatment_plan') }}</span>
                        {% else %}
                            {{ daysUntil }} {{ 'risk_treatment_plan.days_remaining'|trans({}, 'risk_treatment_plan') }}
                        {% endif %}
                    </small>
                </td>
                <td class="text-center">
                    <div class="btn-group btn-group-sm" role="group" aria-label="{{ 'common.actions'|trans({}, 'messages') }}">
                        <a href="{{ path('app_risk_treatment_plan_show', {id: plan.id}) }}"
                           class="fa-cyber-btn fa-cyber-btn--ghost"
                           title="{{ 'common.view'|trans({}, 'messages') }}" aria-label="{{ 'common.view'|trans({}, 'messages') }}">
                            <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i>
                        </a>
                        <a href="{{ path('app_risk_treatment_plan_edit', {id: plan.id}) }}"
                           class="fa-cyber-btn fa-cyber-btn--ghost"
                           title="{{ 'common.edit'|trans({}, 'messages') }}" aria-label="{{ 'common.edit'|trans({}, 'messages') }}">
                            <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i>
                        </a>
                    </div>
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="8" class="text-center text-muted">
                    {{ 'risk_treatment_plan.no_plans'|trans({}, 'risk_treatment_plan') }}

                </td>
            </tr>
            {% endfor %}
        {% endblock %}
    {% endembed %}
    {% endblock %}
{% endembed %}
</main>
{% endblock %}
