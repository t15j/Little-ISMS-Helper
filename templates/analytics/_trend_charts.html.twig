{# Trend Charts Component
   Usage: {% include 'analytics/_trend_charts.html.twig' %}

   Features:
   - Risk trends over time
   - Asset growth trends
   - Incident trends
   - Time period filtering
#}
{% trans_default_domain 'analytics' %}

{% embed '_components/_card.html.twig' with { 'class': 'analytics-card trend-card' ~ (expanded is defined and expanded ? ' expanded' : '') } %}
    {% block card_attrs %}data-controller="trend-chart" data-trend-chart-url-value="{{ path('app_analytics_trends_data') }}" data-trend-chart-locale-value="{{ app.request.locale }}"{% endblock %}
    {% block card_header %}
        <div class="card-header">
            <h2 class="card-title">
                <i class="fa-icon fa-icon--nav-bar-chart text-info" aria-hidden="true"></i>
                {{ 'analytics.card.trend_analysis'|trans({}, 'analytics') }}
            </h2>
            <div class="card-actions">
                <button class="fa-cyber-btn fa-cyber-btn--sm fa-cyber-btn--ghost"
                        data-action="click->trend-chart#refresh"
                        title="{{ 'analytics.action.refresh'|trans({}, 'analytics') }}">
                    <i class="fa-icon fa-icon--ui-refresh" aria-hidden="true"></i>
                </button>
                <button class="fa-cyber-btn fa-cyber-btn--sm fa-cyber-btn--ghost"
                        data-action="click->trend-chart#exportImage"
                        title="{{ 'analytics.action.export_png'|trans({}, 'analytics') }}">
                    <i class="fa-icon fa-icon--nav-document" aria-hidden="true"></i>
                </button>
            </div>
        </div>
    {% endblock %}
    {% block card_body %}
        {# Loading State #}
        <div class="trend-loading d-none" data-trend-chart-target="loading">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">{{ 'analytics.loading.loading'|trans({}, 'analytics') }}</span>
            </div>
            <p class="mt-2 text-muted">{{ 'analytics.loading.loading_trends'|trans({}, 'analytics') }}</p>
        </div>

        {# Trend Charts Container #}
        <div class="trend-container" data-trend-chart-target="container">
            {# Chart Selector Tabs #}
            <ul class="nav nav-pills trend-tabs mb-3">
                <li class="nav-item">
                    <button class="nav-link active"
                            data-action="click->trend-chart#showRiskTrend"
                            data-trend-chart-target="riskTab">
                        <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ 'analytics.trend_tabs.risks'|trans({}, 'analytics') }}
                    </button>
                </li>
                <li class="nav-item">
                    <button class="nav-link"
                            data-action="click->trend-chart#showAssetTrend"
                            data-trend-chart-target="assetTab">
                        <i class="fa-icon fa-icon--asset-server" aria-hidden="true"></i> {{ 'analytics.trend_tabs.assets'|trans({}, 'analytics') }}
                    </button>
                </li>
                <li class="nav-item">
                    <button class="nav-link"
                            data-action="click->trend-chart#showIncidentTrend"
                            data-trend-chart-target="incidentTab">
                        <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ 'analytics.trend_tabs.incidents'|trans({}, 'analytics') }}
                    </button>
                </li>
            </ul>

            {# Risk Trend Chart #}
            <div class="trend-chart-wrapper" data-trend-chart-target="riskChart">
                <h3 class="chart-title">{{ 'analytics.chart_title.risk_trends_last_12'|trans({}, 'analytics') }}</h3>
                <canvas data-trend-chart-target="riskCanvas"
                        width="800"
                        height="300"></canvas>
                <div class="chart-stats mt-3">
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.total_risks'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value" data-trend-chart-target="riskTotal">--</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.high_risk'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value text-danger" data-trend-chart-target="riskHigh">--</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.trend'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value" data-trend-chart-target="riskTrend">--</span>
                    </div>
                </div>
            </div>

            {# Asset Trend Chart #}
            <div class="trend-chart-wrapper d-none" data-trend-chart-target="assetChart">
                <h3 class="chart-title">{{ 'analytics.chart_title.asset_growth_last_12'|trans({}, 'analytics') }}</h3>
                <canvas data-trend-chart-target="assetCanvas"
                        width="800"
                        height="300"></canvas>
                <div class="chart-stats mt-3">
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.total_assets'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value" data-trend-chart-target="assetTotal">--</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.growth'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value" data-trend-chart-target="assetGrowth">--</span>
                    </div>
                </div>
            </div>

            {# Incident Trend Chart #}
            <div class="trend-chart-wrapper d-none" data-trend-chart-target="incidentChart">
                <h3 class="chart-title">{{ 'analytics.chart_title.incident_trends_last_12'|trans({}, 'analytics') }}</h3>
                <canvas data-trend-chart-target="incidentCanvas"
                        width="800"
                        height="300"></canvas>
                <div class="chart-stats mt-3">
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.total_incidents'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value" data-trend-chart-target="incidentTotal">--</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.critical'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value text-danger" data-trend-chart-target="incidentCritical">--</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">{{ 'analytics.stat_label.avg_per_month'|trans({}, 'analytics') }}:</span>
                        <span class="stat-value" data-trend-chart-target="incidentAvg">--</span>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
{% endembed %}
