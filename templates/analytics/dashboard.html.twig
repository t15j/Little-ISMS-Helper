{% extends 'base.html.twig' %}
{% trans_default_domain 'analytics' %}

{% block title %}{{ 'analytics.page.title'|trans({}, 'analytics') }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
<div class="analytics-dashboard" data-controller="analytics">
    {# Page Header #}
    <div class="page-header">
        <div>
            <h1>
                <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                {{ 'analytics.page.title'|trans({}, 'analytics') }}
            </h1>
            <p class="text-muted">{{ 'analytics.page.subtitle'|trans({}, 'analytics') }}</p>
        </div>
        <div class="page-header-actions">
            {# Time Period Filter #}
            <select class="form-select fa-cyber-input__field"
                    data-analytics-target="periodFilter"
                    data-action="change->analytics#changePeriod">
                <option value="6">{{ 'analytics.filter.period.last_6_months'|trans({}, 'analytics') }}</option>
                <option value="12" selected>{{ 'analytics.filter.period.last_12_months'|trans({}, 'analytics') }}</option>
                <option value="24">{{ 'analytics.filter.period.last_24_months'|trans({}, 'analytics') }}</option>
            </select>

            {# Export Dropdown — each item is a POST form that dispatches an async ExportAnalyticsJob #}
            <div class="dropdown">
                <button class="fa-cyber-btn fa-cyber-btn--ghost dropdown-toggle"
                        type="button"
                        data-bs-toggle="dropdown">
                    <i class="fa-icon fa-icon--nav-download" aria-hidden="true"></i> {{ 'analytics.action.export'|trans({}, 'analytics') }}
                </button>
                <ul class="dropdown-menu">
                    <li>
                        <form method="post" action="{{ path('app_analytics_export_dispatch', {type: 'risks'}) }}" data-turbo="false" class="d-inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('analytics_export_dispatch') }}">
                            <button type="submit" class="dropdown-item">
                                <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ 'analytics.action.export_risks_csv'|trans({}, 'analytics') }}
                            </button>
                        </form>
                    </li>
                    <li>
                        <form method="post" action="{{ path('app_analytics_export_dispatch', {type: 'assets'}) }}" data-turbo="false" class="d-inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('analytics_export_dispatch') }}">
                            <button type="submit" class="dropdown-item">
                                <i class="fa-icon fa-icon--asset-server" aria-hidden="true"></i> {{ 'analytics.action.export_assets_csv'|trans({}, 'analytics') }}
                            </button>
                        </form>
                    </li>
                    <li>
                        <form method="post" action="{{ path('app_analytics_export_dispatch', {type: 'compliance'}) }}" data-turbo="false" class="d-inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('analytics_export_dispatch') }}">
                            <button type="submit" class="dropdown-item">
                                <i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i> {{ 'analytics.action.export_compliance_csv'|trans({}, 'analytics') }}
                            </button>
                        </form>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                        <a class="dropdown-item"
                           href="#"
                           data-action="click->analytics#printDashboard">
                            <i class="fa-icon fa-icon--ui-printer" aria-hidden="true"></i> {{ 'analytics.action.print_dashboard'|trans({}, 'analytics') }}
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    {# Tab Navigation #}
    <ul class="nav nav-tabs analytics-tabs" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active"
                    data-bs-toggle="tab"
                    data-bs-target="#overview-tab"
                    type="button"
                    role="tab">
                <i class="fa-icon fa-icon--nav-dashboard" aria-hidden="true"></i> {{ 'analytics.tabs.overview'|trans({}, 'analytics') }}
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link"
                    data-bs-toggle="tab"
                    data-bs-target="#heat-map-tab"
                    type="button"
                    role="tab">
                <i class="fa-icon fa-icon--nav-grid" aria-hidden="true"></i> {{ 'analytics.tabs.heat_map'|trans({}, 'analytics') }}
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link"
                    data-bs-toggle="tab"
                    data-bs-target="#compliance-tab"
                    type="button"
                    role="tab">
                <i class="fa-icon fa-icon--nav-process" aria-hidden="true"></i> {{ 'analytics.tabs.compliance_radar'|trans({}, 'analytics') }}
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link"
                    data-bs-toggle="tab"
                    data-bs-target="#trends-tab"
                    type="button"
                    role="tab">
                <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i> {{ 'analytics.tabs.trends'|trans({}, 'analytics') }}
            </button>
        </li>
        {% if is_granted('ROLE_MANAGER') %}
        <li class="nav-item ms-auto" role="presentation">
            <span class="nav-link disabled text-muted border-0 bg-transparent">
                <i class="fa-icon fa-icon--threat" aria-hidden="true"></i> {{ 'analytics.tabs.advanced_section'|trans({}, 'analytics') }}
            </span>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" href="{{ path('app_analytics_compliance_frameworks') }}">
                <i class="fa-icon fa-icon--nav-process" aria-hidden="true"></i> {{ 'analytics.tabs.frameworks'|trans({}, 'analytics') }}
            </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" href="{{ path('app_analytics_control_effectiveness') }}">
                <i class="fa-icon fa-icon--nav-sliders" aria-hidden="true"></i> {{ 'analytics.tabs.controls'|trans({}, 'analytics') }}
            </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" href="{{ path('app_analytics_risk_forecast') }}">
                <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i> {{ 'analytics.tabs.forecast'|trans({}, 'analytics') }}
            </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" href="{{ path('app_analytics_asset_criticality') }}">
                <i class="fa-icon fa-icon--asset-server" aria-hidden="true"></i> {{ 'analytics.tabs.assets'|trans({}, 'analytics') }}
            </a>
        </li>
        {% endif %}
    </ul>

    {# Tab Content #}
    <div class="tab-content analytics-content">
        {# Overview Tab #}
        <div class="tab-pane fade show active"
             id="overview-tab"
             role="tabpanel">
            <div class="row">
                {# Risk Heat Map Card #}
                <div class="col-md-6 mb-4">
                    {% include 'analytics/_risk_heat_map.html.twig' %}
                </div>

                {# Compliance Radar Card #}
                <div class="col-md-6 mb-4">
                    {% include 'analytics/_compliance_radar.html.twig' %}
                </div>
            </div>

            {# Trend Charts Row #}
            <div class="row">
                <div class="col-12">
                    {% include 'analytics/_trend_charts.html.twig' %}
                </div>
            </div>
        </div>

        {# Risk Heat Map Tab #}
        <div class="tab-pane fade"
             id="heat-map-tab"
             role="tabpanel">
            <div class="row">
                <div class="col-12">
                    {% include 'analytics/_risk_heat_map.html.twig' with {expanded: true} %}
                </div>
            </div>
        </div>

        {# Compliance Tab #}
        <div class="tab-pane fade"
             id="compliance-tab"
             role="tabpanel">
            <div class="row">
                <div class="col-12">
                    {% include 'analytics/_compliance_radar.html.twig' with {expanded: true} %}
                </div>
            </div>
        </div>

        {# Trends Tab #}
        <div class="tab-pane fade"
             id="trends-tab"
             role="tabpanel">
            <div class="row">
                <div class="col-12">
                    {% include 'analytics/_trend_charts.html.twig' with {expanded: true} %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
