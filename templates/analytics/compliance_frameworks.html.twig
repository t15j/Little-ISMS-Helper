{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% trans_default_domain 'analytics' %}

{% block title %}{{ 'analytics.compliance.title'|trans({}, 'analytics') }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
<div class="analytics-dashboard" data-controller="compliance-analytics">
    {# Page Header #}
    <div class="page-header">
        <div>
            <h1>
                <i class="fa-icon fa-icon--nav-process" aria-hidden="true"></i>
                {{ 'analytics.compliance.title'|trans({}, 'analytics') }}
            </h1>
            <p class="text-muted">{{ 'analytics.compliance.subtitle'|trans({}, 'analytics') }}</p>
        </div>
        <div class="page-header-actions">
            <a href="{{ path('app_analytics_dashboard') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'analytics.action.back_to_basic'|trans({}, 'analytics') }}
            </a>
        </div>
    </div>

    {# Tab Navigation #}
    <ul class="nav nav-tabs mb-4" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#comparison-tab" type="button" role="tab">
                <i class="fa-icon fa-icon--nav-bar-chart me-1" aria-hidden="true"></i> {{ 'analytics.compliance.tabs.comparison'|trans({}, 'analytics') }}
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#overlap-tab" type="button" role="tab">
                <i class="fa-icon me-1 fa-icon--nav-process" aria-hidden="true"></i> {{ 'analytics.compliance.tabs.overlap'|trans({}, 'analytics') }}
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#roadmap-tab" type="button" role="tab">
                <i class="fa-icon fa-icon--learning-path me-1" aria-hidden="true"></i> {{ 'analytics.compliance.tabs.roadmap'|trans({}, 'analytics') }}
            </button>
        </li>
    </ul>

    {# Tab Content #}
    <div class="tab-content">
        {# Framework Comparison Tab #}
        <div class="tab-pane fade show active" id="comparison-tab" role="tabpanel">
            <div class="row">
                {# Comparison Chart #}
                <div class="col-lg-8 mb-4">
                    {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                        {% block card_header %}
                            <div class="card-header">
                                <h5 class="mb-0">{{ 'analytics.compliance.comparison.chart_title'|trans({}, 'analytics') }}</h5>
                            </div>
                        {% endblock %}
                        {% block card_body %}
                            <div style="height: 300px; position: relative;">
                                <canvas id="frameworkComparisonChart"></canvas>
                            </div>
                        {% endblock %}
                    {% endembed %}
                </div>
                {# Summary Cards #}
                <div class="col-lg-4 mb-4">
                    {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                        {% block card_header %}
                            <div class="card-header">
                                <h5 class="mb-0">{{ 'analytics.compliance.comparison.summary'|trans({}, 'analytics') }}</h5>
                            </div>
                        {% endblock %}
                        {% block card_body %}
                            {% for framework in comparison.frameworks %}
                            <div class="d-flex justify-content-between align-items-center mb-3 pb-3 border-bottom">
                                <div>
                                    <h6 class="mb-0">{{ framework.name }}</h6>
                                    <small class="text-muted">{{ framework.total }} {{ 'analytics.compliance.comparison.requirements'|trans({}, 'analytics') }}</small>
                                </div>
                                <div class="text-end">
                                    <h4 class="mb-0
                                        {% if framework.compliance_percentage >= 80 %}text-success
                                        {% elseif framework.compliance_percentage >= 50 %}text-warning
                                        {% else %}text-danger{% endif %}
                                    ">{{ framework.compliance_percentage }}%</h4>
                                    <small class="text-muted">{{ framework.fulfilled }}/{{ framework.applicable }}</small>
                                </div>
                            </div>
                            {% else %}
                            <p class="text-muted text-center">{{ 'analytics.compliance.comparison.no_data'|trans({}, 'analytics') }}</p>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                </div>
            </div>

            {# Detailed Table #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h5 class="mb-0">{{ 'analytics.compliance.comparison.detailed_breakdown'|trans({}, 'analytics') }}</h5>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_fa_table.html.twig' %}
                        {% block table_head %}
                            <tr>
                                <th scope="col" class="fa-table__th">{{ 'analytics.compliance.table.framework'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.table.total'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.table.compliant'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.table.partial'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.table.non_compliant'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.table.compliance'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th">{{ 'analytics.compliance.table.progress'|trans({}, 'analytics') }}</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for framework in comparison.frameworks %}
                            <tr class="fa-table__row">
                                <td class="fa-table__cell"><strong>{{ framework.name }}</strong></td>
                                <td class="fa-table__cell fa-table__cell--center">{{ framework.applicable }}</td>
                                <td class="fa-table__cell fa-table__cell--center text-success">{{ framework.fulfilled }}</td>
                                <td class="fa-table__cell fa-table__cell--center text-warning">{{ framework.in_progress }}</td>
                                <td class="fa-table__cell fa-table__cell--center text-danger">{{ framework.not_started }}</td>
                                <td class="fa-table__cell fa-table__cell--center">
                                    {% include '_components/_badge.html.twig' with {
                                        variant: framework.compliance_percentage >= 80 ? 'success' : (framework.compliance_percentage >= 50 ? 'warning' : 'danger'),
                                        content: framework.compliance_percentage ~ '%'
                                    } %}
                                </td>
                                <td class="fa-table__cell">
                                    {% if framework.applicable > 0 %}
                                    <div class="progress progress-h-20">
                                        <div class="progress-bar bg-success" style="width: {{ (framework.fulfilled / framework.applicable * 100)|round }}%"></div>
                                        <div class="progress-bar bg-warning" style="width: {{ (framework.in_progress / framework.applicable * 100)|round }}%"></div>
                                        <div class="progress-bar bg-danger" style="width: {{ (framework.not_started / framework.applicable * 100)|round }}%"></div>
                                    </div>
                                    {% else %}
                                    <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                            </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                {% endblock %}
            {% endembed %}
        </div>

        {# Framework Overlap Tab #}
        <div class="tab-pane fade" id="overlap-tab" role="tabpanel">
            <div class="row">
                <div class="col-lg-6 mb-4">
                    {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                        {% block card_header %}
                            <div class="card-header">
                                <h5 class="mb-0">{{ 'analytics.compliance.overlap.title'|trans({}, 'analytics') }}</h5>
                            </div>
                        {% endblock %}
                        {% block card_body %}
                            <div class="text-center mb-4">
                                <h2 class="" style="color: var(--primary-strong, var(--primary)); font-weight: 700;">{{ overlap.frameworks|length }}</h2>
                                <p class="text-muted">{{ 'analytics.compliance.overlap.unique_controls'|trans({}, 'analytics') }}</p>
                            </div>
                            <div class="row text-center">
                                <div class="col-6">
                                    <h4 class="" style="color: var(--success-strong, var(--success)); font-weight: 700;">{{ overlap.overlaps|length }}</h4>
                                    <p class="text-muted small">{{ 'analytics.compliance.overlap.shared_controls'|trans({}, 'analytics') }}</p>
                                </div>
                                <div class="col-6">
                                    {% set total_overlap = 0 %}
                                    {% for o in overlap.overlaps %}
                                        {% set total_overlap = total_overlap + o.overlap_percentage %}
                                    {% endfor %}
                                    <h4 class="" style="color: var(--info-strong, var(--info)); font-weight: 700;">{{ overlap.overlaps|length > 0 ? (total_overlap / overlap.overlaps|length)|round : 0 }}%</h4>
                                    <p class="text-muted small">{{ 'analytics.compliance.overlap.overlap_rate'|trans({}, 'analytics') }}</p>
                                </div>
                            </div>
                        {% endblock %}
                    {% endembed %}
                </div>
                <div class="col-lg-6 mb-4">
                    {% embed '_components/_card.html.twig' with { 'class': 'h-100' } %}
                        {% block card_header %}
                            <div class="card-header">
                                <h5 class="mb-0">{{ 'analytics.compliance.overlap.efficiency'|trans({}, 'analytics') }}</h5>
                            </div>
                        {% endblock %}
                        {% block card_body %}
                            {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                            {% if overlap.overlaps|length > 0 %}
                            {{ _fa_alert.render({ variant: 'success', body: 'analytics.compliance.overlap.efficiency_info'|trans({}, 'analytics') }) }}
                            <p>{{ 'analytics.compliance.overlap.efficiency_description'|trans({'%percentage%': (total_overlap / overlap.overlaps|length)|round}, 'analytics') }}</p>
                            {% else %}
                            {{ _fa_alert.render({ variant: 'info', body: 'analytics.compliance.comparison.no_data'|trans({}, 'analytics') }) }}
                            {% endif %}
                            <div style="height: 200px; position: relative;">
                                <canvas id="overlapChart"></canvas>
                            </div>
                        {% endblock %}
                    {% endembed %}
                </div>
            </div>

            {# Framework Overlaps List #}
            {% if overlap.overlaps|length > 0 %}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h5 class="mb-0">{{ 'analytics.compliance.overlap.matrix_title'|trans({}, 'analytics') }}</h5>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_fa_table.html.twig' %}
                        {% block table_head %}
                            <tr>
                                <th scope="col" class="fa-table__th">Framework 1</th>
                                <th scope="col" class="fa-table__th">Framework 2</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.overlap.shared_controls'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.overlap.overlap_rate'|trans({}, 'analytics') }}</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for o in overlap.overlaps %}
                            <tr class="fa-table__row">
                                <td class="fa-table__cell">{{ o.framework1_name }}</td>
                                <td class="fa-table__cell">{{ o.framework2_name }}</td>
                                <td class="fa-table__cell fa-table__cell--center">{{ o.shared_controls }}</td>
                                <td class="fa-table__cell fa-table__cell--center">
                                    {% include '_components/_badge.html.twig' with {
                                        variant: o.overlap_percentage > 50 ? 'success' : (o.overlap_percentage > 20 ? 'warning' : 'secondary'),
                                        content: o.overlap_percentage ~ '%'
                                    } %}
                                </td>
                            </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                {% endblock %}
            {% endembed %}
            {% endif %}
        </div>

        {# Compliance Roadmap Tab #}
        <div class="tab-pane fade" id="roadmap-tab" role="tabpanel">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">{{ 'analytics.compliance.roadmap.title'|trans({}, 'analytics') }}</h5>
                        {% include '_components/_badge.html.twig' with { variant: 'primary', content: roadmap.roadmap|length ~ ' ' ~ 'analytics.compliance.tabs.frameworks'|trans({}, 'analytics') } %}
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% if roadmap.roadmap|length > 0 %}
                    {# Aurora KPI-Tiles statt Bootstrap bg-* (Kontrast garantiert, CLAUDE Pitfall #11). #}
                    {% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}
                    <div class="row g-3 mb-4">
                        <div class="col-md-4">
                            {{ _fa_feature_card.render({
                                variant: 'success',
                                value: roadmap.summary.frameworks_compliant,
                                label: 'analytics.compliance.table.compliant'|trans({}, 'analytics'),
                            }) }}
                        </div>
                        <div class="col-md-4">
                            {{ _fa_feature_card.render({
                                variant: 'warning',
                                value: roadmap.summary.frameworks_in_progress,
                                label: 'analytics.compliance.table.partial'|trans({}, 'analytics'),
                            }) }}
                        </div>
                        <div class="col-md-4">
                            {{ _fa_feature_card.render({
                                variant: 'danger',
                                value: roadmap.summary.frameworks_not_started,
                                label: 'analytics.compliance.table.non_compliant'|trans({}, 'analytics'),
                            }) }}
                        </div>
                    </div>

                    {% embed '_components/_fa_table.html.twig' %}
                        {% block table_head %}
                            <tr>
                                <th scope="col" class="fa-table__th">{{ 'analytics.compliance.table.framework'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">{{ 'analytics.compliance.table.compliance'|trans({}, 'analytics') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">Gaps</th>
                                <th scope="col" class="fa-table__th fa-table__th--center">Est. Weeks</th>
                                <th scope="col" class="fa-table__th">Status</th>
                                <th scope="col" class="fa-table__th">{{ 'analytics.compliance.table.progress'|trans({}, 'analytics') }}</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for item in roadmap.roadmap %}
                            <tr class="fa-table__row">
                                <td class="fa-table__cell">
                                    <strong>{{ item.framework }}</strong>
                                    {% if item.mandatory %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', class: 'ms-1', content: 'Mandatory' } %}
                                    {% endif %}
                                </td>
                                <td class="fa-table__cell fa-table__cell--center">
                                    {% include '_components/_badge.html.twig' with {
                                        variant: item.current_compliance >= 80 ? 'success' : (item.current_compliance >= 50 ? 'warning' : 'danger'),
                                        content: item.current_compliance ~ '%'
                                    } %}
                                </td>
                                <td class="fa-table__cell fa-table__cell--center">{{ item.gaps_remaining }}</td>
                                <td class="fa-table__cell fa-table__cell--center">{{ item.estimated_weeks }}</td>
                                <td class="fa-table__cell">
                                    {% set status_variant = item.status == 'completed' ? 'success' : (item.status == 'on_track' ? 'info' : (item.status in ['in_progress', 'at_risk'] ? 'warning' : (item.status == 'critical' ? 'danger' : 'secondary'))) %}
                                    {% include '_components/_badge.html.twig' with { variant: status_variant, content: item.status|replace({'_': ' '})|title } %}
                                </td>
                                <td class="fa-table__cell">
                                    <div class="progress progress-h-20">
                                        <div class="progress-bar
                                            {% if item.current_compliance >= 80 %}bg-success
                                            {% elseif item.current_compliance >= 50 %}bg-warning
                                            {% else %}bg-danger{% endif %}
                                        " style="width: {{ item.current_compliance }}%">
                                            {{ item.current_compliance }}%
                                        </div>
                                    </div>
                                </td>
                            </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fa-icon fa-icon--status-ok display-1" aria-hidden="true"></i>
                        <h4 class="mt-3">{{ 'analytics.compliance.roadmap.no_gaps'|trans({}, 'analytics') }}</h4>
                        <p class="text-muted">{{ 'analytics.compliance.roadmap.fully_compliant'|trans({}, 'analytics') }}</p>
                    </div>
                    {% endif %}
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('turbo:load', function() {
            // Resolve Aurora design-tokens to actual color values for Chart.js.
            // Chart.js does NOT resolve CSS custom properties — it needs literal
            // color strings (hex/rgb/rgba). Read tokens at chart-init.
            const _tokens = getComputedStyle(document.documentElement);
            const T = (name) => _tokens.getPropertyValue(name).trim();
            const COLORS = {
                success: T('--success'),
                warning: T('--warning'),
                danger:  T('--danger'),
                primary: T('--primary'),
                fg3:     T('--fg-3'),
            };

            // Framework Comparison Chart
            const comparisonCtx = document.getElementById('frameworkComparisonChart');
            if (comparisonCtx) {
                const comparisonData = {{ comparison.frameworks|json_encode|raw }};
                new Chart(comparisonCtx, {
                    type: 'bar',
                    data: {
                        labels: comparisonData.map(f => f.name),
                        datasets: [
                            {
                                label: '{{ 'analytics.compliance.chart.compliant'|trans({}, 'analytics') }}',
                                data: comparisonData.map(f => f.fulfilled),
                                backgroundColor: COLORS.success
                            },
                            {
                                label: '{{ 'analytics.compliance.chart.partial'|trans({}, 'analytics') }}',
                                data: comparisonData.map(f => f.in_progress),
                                backgroundColor: COLORS.warning
                            },
                            {
                                label: '{{ 'analytics.compliance.chart.non_compliant'|trans({}, 'analytics') }}',
                                data: comparisonData.map(f => f.not_started),
                                backgroundColor: COLORS.danger
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: {
                            x: { stacked: true },
                            y: { stacked: true, beginAtZero: true }
                        }
                    }
                });
            }

            // Overlap Chart
            const overlapCtx = document.getElementById('overlapChart');
            if (overlapCtx) {
                const overlapData = {{ overlap|json_encode|raw }};
                const totalShared = overlapData.overlaps.reduce((sum, o) => sum + o.shared_controls, 0);
                const totalFrameworks = overlapData.frameworks.length;
                new Chart(overlapCtx, {
                    type: 'doughnut',
                    data: {
                        labels: ['{{ 'analytics.compliance.chart.shared'|trans({}, 'analytics') }}', '{{ 'analytics.compliance.chart.unique'|trans({}, 'analytics') }}'],
                        datasets: [{
                            data: [totalShared, Math.max(0, totalFrameworks - totalShared)],
                            backgroundColor: [COLORS.success, COLORS.fg3]
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false
                    }
                });
            }
        });
    </script>
{% endblock %}
