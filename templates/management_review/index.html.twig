{% extends 'base.html.twig' %}
{% trans_default_domain 'management_review' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_status_pill.html.twig' as pill %}

{% block title %}{{ 'management_review.title'|trans({}, 'management_review') }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.management_reviews'|trans({}, 'nav') }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'nav-clipboard-check', label: 'nav.management_reviews'|trans({}, 'nav') },
        title: 'management_review.title'|trans({}, 'management_review'),
        actions: is_granted('ROLE_ADMIN') ? [
            { label: 'management_review.new'|trans({}, 'management_review'), variant: 'primary', icon: 'plus', href: path('app_management_review_new'), attrs: {'data-turbo-action': 'advance'} }
        ] : []
    }) }}

    <main class="fa-aurora-surface">

    {# V3 B4 / EF-1: Auto-Collect Card (Alva-Tipp pattern) #}
    {% if is_granted('ROLE_ADMIN') %}
        <div class="card fa-section border-info mb-4">
            <div class="card-body py-3">
                <div class="d-flex align-items-start gap-3 flex-wrap">
                    <div class="flex-grow-1">
                        <div class="d-flex align-items-center gap-2 mb-1">
                            <i class="fa-icon fa-icon--nav-stars text-info" aria-hidden="true"></i>
                            <strong>{{ 'management_review.auto_collect.title'|trans({}, 'management_review') }}</strong>
                            {{ pill.render('Alva-Tipp', 'primary', {size: 'sm'}) }}
                        </div>
                        <p class="text-muted mb-0 small">{{ 'management_review.auto_collect.help'|trans({}, 'management_review') }}</p>
                    </div>
                    <form method="post" action="{{ path('app_management_review_auto_collect') }}" class="d-inline">
                        <input type="hidden" name="_token" value="{{ csrf_token('auto_collect_review') }}">
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--info">
                            <i class="fa-icon fa-icon--ui-magic" aria-hidden="true"></i>
                            {{ 'management_review.auto_collect.cta'|trans({}, 'management_review') }}
                        </button>
                    </form>
                </div>
            </div>
        </div>
    {% endif %}

    {# ISO 27001 Clause Information #}
    {{ _fa_alert.render({ variant: 'info', class: 'mb-4', title: 'management_review.iso_requirement'|trans({}, 'management_review'), body: '<span class="small">' ~ 'management_review.iso_clause_info'|trans({}, 'management_review') ~ '</span>' }) }}

    {# Statistics Cards #}
    <turbo-frame id="review-stats">
        <div class="row mb-4">
            <div class="col-md-3 mb-3">
                {% embed '_components/_card.html.twig' with { 'variant': 'bordered', 'borderColor': 'primary' } %}
                    {% block card_body %}
                        <div class="d-flex align-items-center">
                            <div class="flex-shrink-0">
                                <i class="fa-icon fa-icon--nav-collection text-primary icon-2rem" aria-hidden="true"></i>
                            </div>
                            <div class="ms-3">
                                <div class="text-muted small mb-1">{{ 'management_review.statistics.total'|trans({}, 'management_review') }}</div>
                                <div class="fs-2 fw-bold">{{ statistics.total }}</div>
                            </div>
                        </div>
                    {% endblock %}
                {% endembed %}
            </div>

            <div class="col-md-3 mb-3">
                {% embed '_components/_card.html.twig' with { 'variant': 'bordered', 'borderColor': 'warning' } %}
                    {% block card_body %}
                        <div class="d-flex align-items-center">
                            <div class="flex-shrink-0">
                                <i class="fa-icon fa-icon--ui-hourglass text-warning icon-2rem" aria-hidden="true"></i>
                            </div>
                            <div class="ms-3">
                                <div class="text-muted small mb-1">{{ 'management_review.statistics.planned'|trans({}, 'management_review') }}</div>
                                <div class="fs-2 fw-bold">{{ statistics.planned }}</div>
                            </div>
                        </div>
                    {% endblock %}
                {% endembed %}
            </div>

            <div class="col-md-3 mb-3">
                {% embed '_components/_card.html.twig' with { 'variant': 'bordered', 'borderColor': 'success' } %}
                    {% block card_body %}
                        <div class="d-flex align-items-center">
                            <div class="flex-shrink-0">
                                <i class="fa-icon fa-icon--status-ok icon-2rem" aria-hidden="true"></i>
                            </div>
                            <div class="ms-3">
                                <div class="text-muted small mb-1">{{ 'management_review.statistics.completed'|trans({}, 'management_review') }}</div>
                                <div class="fs-2 fw-bold">{{ statistics.completed }}</div>
                            </div>
                        </div>
                    {% endblock %}
                {% endembed %}
            </div>

            <div class="col-md-3 mb-3">
                {% embed '_components/_card.html.twig' with { 'variant': 'bordered', 'borderColor': 'info' } %}
                    {% block card_body %}
                        <div class="d-flex align-items-center">
                            <div class="flex-shrink-0">
                                <i class="fa-icon fa-icon--ui-calendar-event text-info icon-2rem" aria-hidden="true"></i>
                            </div>
                            <div class="ms-3">
                                <div class="text-muted small mb-1">{{ 'management_review.statistics.this_year'|trans({}, 'management_review') }}</div>
                                <div class="fs-2 fw-bold">{{ statistics.this_year }}</div>
                            </div>
                        </div>
                    {% endblock %}
                {% endembed %}
            </div>
        </div>
    </turbo-frame>

    {# Management Reviews List #}
    {% embed '_components/_card.html.twig' %}
        {% block card_attrs %}data-controller="bulk-actions" data-bulk-actions-endpoint-value="/{{ app.request.locale }}/management-review"{% endblock %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="h5 mb-0"><i class="fa-icon fa-icon--ui-list" aria-hidden="true"></i> {{ 'management_review.all_reviews'|trans({}, 'management_review') }}</h2>
            </div>
        {% endblock %}
        {% block card_body %}
            {% if reviews|length == 0 %}
            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
                {% block alert_body %}
                    {{ 'management_review.no_reviews'|trans({}, 'management_review') }}
                    {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_management_review_new') }}" class="fa-alert__link" data-turbo-action="advance">
                        {{ 'management_review.create_first'|trans({}, 'management_review') }}
                    </a>
                    {% endif %}
                {% endblock %}
            {% endembed %}
            {% else %}
            {% embed '_components/_table.html.twig' with {
                'class': 'table-hover table-striped',
                'noMargin': true
            } %}
                {% block table_head %}
                    <tr>
                        <th scope="col" class="bulk-select-column">
                            <input type="checkbox"
                                   class="form-check-input"
                                   data-action="bulk-actions#selectAll"
                                   data-bulk-actions-target="selectAllCheckbox"
                                   aria-label="{{ 'bulk_actions.select_all'|trans({}, 'messages') }}">
                        </th>
                        <th scope="col">{{ 'management_review.title'|trans({}, 'management_review') }}</th>
                        <th scope="col">{{ 'management_review.field.review_date'|trans({}, 'management_review') }}</th>
                        <th scope="col">{{ 'management_review.field.status'|trans({}, 'management_review') }}</th>
                        <th scope="col">{{ 'management_review.field.participants'|trans({}, 'management_review') }}</th>
                        <th scope="col">{{ 'common.actions'|trans({}, 'messages') }}</th>
                    </tr>
                {% endblock %}
                {% block table_body %}
                    {% for review in reviews %}
                    <tr>
                        <td class="bulk-select-column">
                            <input type="checkbox"
                                   class="form-check-input"
                                   data-bulk-actions-target="item"
                                   data-action="bulk-actions#selectItem"
                                   value="{{ review.id }}"
                                   aria-label="{{ 'common.select'|trans({}, 'messages') }} {{ review.title }}">
                        </td>
                        <td>
                            <strong>{{ review.title }}</strong>
                        </td>
                        <td>{{ review.reviewDate|date('d.m.Y') }}</td>
                        <td>
                            {% set statusClass = {
                                'planned': 'warning',
                                'in_progress': 'info',
                                'completed': 'success',
                                'cancelled': 'danger'
                            } %}
                            {% include '_components/_badge.html.twig' with { variant: statusClass[review.status] ?? 'secondary', content: ('management_review.status.' ~ review.status)|trans({}, 'management_review') } %}
                        </td>
                        <td>
                            {% if review.participants|length > 0 %}
                            <i class="fa-icon fa-icon--group" aria-hidden="true"></i>
                            {% include '_components/_badge.html.twig' with { variant: 'primary', content: review.participants|length } %}
                            {% else %}
                            <span class="text-muted">-</span>
                            {% endif %}
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="{{ path('app_management_review_show', {id: review.id}) }}"
                                   class="fa-cyber-btn fa-cyber-btn--ghost"
                                   data-turbo-action="advance"
                                   title="{{ 'common.view'|trans({}, 'messages') }}" aria-label="{{ 'common.view'|trans({}, 'messages') }}">
                                    <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i>
                                </a>
                                {% if is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('app_management_review_edit', {id: review.id}) }}"
                                   class="fa-cyber-btn fa-cyber-btn--ghost"
                                   data-turbo-action="advance"
                                   title="{{ 'common.edit'|trans({}, 'messages') }}" aria-label="{{ 'common.edit'|trans({}, 'messages') }}">
                                    <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i>
                                </a>
                                <button type="button"
                                        class="fa-cyber-btn fa-cyber-btn--danger" data-controller="fa-modal-dispatcher" data-action="click->fa-modal-dispatcher#open" data-fa-modal-id="deleteModal{{ review.id }}"
                                        title="{{ 'common.delete'|trans({}, 'messages') }}" aria-label="{{ 'common.delete'|trans({}, 'messages') }}">
                                    <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i>
                                </button>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                {% endblock %}
            {% endembed %}

            {# Bulk Action Bar — admin-only delete #}
            {% if is_granted('ROLE_ADMIN') %}
            {% include '_components/_bulk_action_bar.html.twig' with {
                actions: ['delete']
            } %}
            {% endif %}
            {% endif %}
        {% endblock %}
    {% endembed %}
    </main>
</div>

{# Delete Modals #}
{% for review in reviews %}
<div class="fa-modal fa-modal--confirm fa-modal--danger" id="deleteModal{{ review.id }}"
     data-controller="fa-modal" data-fa-modal-mode-value="confirm" data-fa-modal-tone-value="danger"
     role="alertdialog" aria-modal="true" aria-labelledby="deleteModal{{ review.id }}Label" aria-hidden="true">
    <div class="fa-modal__backdrop" data-fa-modal-target="backdrop" data-action="click->fa-modal#backdropClick" aria-hidden="true"></div>
    <div class="fa-modal__container" data-fa-modal-target="dialog"><header class="fa-modal__header"><span class="fa-modal__icon" aria-hidden="true"><i class="fa-icon fa-icon--ui-trash"></i></span><div class="fa-modal__heading"><h3 class="fa-modal__title" id="deleteModalLabel{{ review.id }}">
                    {{ 'management_review.confirm_delete'|trans({}, 'management_review') }}
                </h3></div><button type="button" class="fa-modal__close" data-action="fa-modal#cancel" aria-label="Close"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i></button></header>
            <div class="fa-modal__body">
                <p>{{ 'management_review.delete_warning'|trans({'%title%': review.title}, 'management_review') }}</p>
                <p class="text-muted"><small>{{ 'common.action_irreversible'|trans({}, 'messages') }}</small></p>
            </div>
            <footer class="fa-modal__footer">
                <button type="button" class="fa-cyber-btn fa-cyber-btn--ghost" data-action="fa-modal#cancel">{{ 'common.cancel'|trans({}, 'messages') }}</button>
                <form method="post" action="{{ path('app_management_review_delete', {id: review.id}) }}" class="d-inline" data-fa-modal-target="form">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.id) }}">
                    <button type="submit" class="fa-cyber-btn fa-cyber-btn--danger">
                        <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                    </button>
                </form></div>
</div>
</div>
{% endfor %}
{% endblock %}
