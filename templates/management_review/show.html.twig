{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% trans_default_domain 'management_review' %}

{% block title %}{{ review.title }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    {# M-4 — module-gating: Management Review (ISO 27001 Cl. 9.3) #}
    {% if not is_module_active('reviews') %}
        {{ _fa_alert.render({
            variant: 'info',
            icon: 'nav-shield-alert',
            body: 'common.module_not_active'|trans({'%module%': 'Management Review'}, 'messages')
        }) }}
    {% else %}
    {# Lifecycle Y.5 — status transition dropdown (management_review_lifecycle) #}
    {% include '_components/_lifecycle_toolbar.html.twig' with {
        entity: review,
        entity_type: 'management-review',
        workflow_name: 'management_review_lifecycle'
    } only %}
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="{{ path('app_management_review_index') }}" data-turbo-action="advance">
                    <i class="fa-icon fa-icon--audit-trail" aria-hidden="true"></i> {{ 'management_review.title'|trans({}, 'management_review') }}
                </a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">{{ review.title }}</li>
        </ol>
    </nav>

    {{ _fa_page_header.render({
        badge: { icon: 'nav-clipboard-check', label: 'management_review.title'|trans({}, 'management_review') },
        title: review.title
    }) }}

    <div class="row">
        <div class="col-lg-8">
            {# Main Review Information #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--audit-trail" aria-hidden="true"></i> {{ 'management_review.field.basic_info'|trans({}, 'management_review') }}
                        </h2>
                        {% set statusClass = {
                            'planned': 'warning',
                            'in_progress': 'info',
                            'completed': 'success',
                            'cancelled': 'danger'
                        } %}
                        {% include '_components/_badge.html.twig' with { variant: statusClass[review.status] ?? 'secondary', class: 'fa-status-pill--lg', content: ('management_review.status.' ~ review.status)|trans({}, 'management_review') } %}
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <div class="text-muted small mb-2">
                                <i class="fa-icon fa-icon--ui-calendar-event" aria-hidden="true"></i> {{ 'management_review.field.review_date'|trans({}, 'management_review') }}
                            </div>
                            <p class="fs-5">{{ review.reviewDate|date('d.m.Y') }}</p>
                        </div>
                        <div class="col-md-6">
                            <div class="text-muted small mb-2">
                                <i class="fa-icon fa-icon--group" aria-hidden="true"></i> {{ 'management_review.field.participants'|trans({}, 'management_review') }}
                                {% include '_components/_badge.html.twig' with { variant: 'primary', content: review.effectiveParticipantCount } %}
                            </div>
                            {% if review.effectiveParticipantCount > 0 %}
                            <ul class="list-unstyled mb-0">
                                {% for participant in review.participants %}
                                <li><i class="fa-icon fa-icon--ui-person" aria-hidden="true"></i> {{ participant.firstName }} {{ participant.lastName }}</li>
                                {% endfor %}
                                {% for personParticipant in review.personParticipants %}
                                <li><i class="fa-icon fa-icon--ui-person" aria-hidden="true"></i> {{ personParticipant.fullName }}</li>
                                {% endfor %}
                            </ul>
                            {% else %}
                            <p class="text-muted">-</p>
                            {% endif %}
                        </div>
                    </div>
                {% endblock %}
            {% endembed %}

            {# Inputs to the Review #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-inbox" aria-hidden="true"></i> {{ 'management_review.section.inputs'|trans({}, 'management_review') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% if review.changesRelevantToISMS %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.changes_relevant_to_isms'|trans({}, 'management_review') }}</div>
                        <p>{{ review.changesRelevantToISMS|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.feedbackFromInterestedParties %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.feedback_from_interested_parties'|trans({}, 'management_review') }}</div>
                        <p>{{ review.feedbackFromInterestedParties|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.auditResults %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.audit_results'|trans({}, 'management_review') }}</div>
                        <p>{{ review.auditResults|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.performanceEvaluation %}
                    <div class="mb-0">
                        <div class="text-muted small fw-semibold">{{ 'management_review.performance_evaluation'|trans({}, 'management_review') }}</div>
                        <p>{{ review.performanceEvaluation|nl2br }}</p>
                    </div>
                    {% endif %}
                {% endblock %}
            {% endembed %}

            {# Status of Previous Actions #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-refresh" aria-hidden="true"></i> {{ 'management_review.section.followup'|trans({}, 'management_review') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% if review.previousReviewActions %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.previous_review_actions'|trans({}, 'management_review') }}</div>
                        <p>{{ review.previousReviewActions|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.nonConformitiesStatus %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.nonconformities_status'|trans({}, 'management_review') }}</div>
                        <p>{{ review.nonConformitiesStatus|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.correctiveActionsStatus %}
                    <div class="mb-0">
                        <div class="text-muted small fw-semibold">{{ 'management_review.corrective_actions_status'|trans({}, 'management_review') }}</div>
                        <p>{{ review.correctiveActionsStatus|nl2br }}</p>
                    </div>
                    {% endif %}
                {% endblock %}
            {% endembed %}

            {# Outputs from the Review #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4 border-success' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-send" aria-hidden="true"></i> {{ 'management_review.section.outputs'|trans({}, 'management_review') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% if review.opportunitiesForImprovement %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.opportunities_for_improvement'|trans({}, 'management_review') }}</div>
                        <p>{{ review.opportunitiesForImprovement|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.resourceNeeds %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.resource_needs'|trans({}, 'management_review') }}</div>
                        <p>{{ review.resourceNeeds|nl2br }}</p>
                    </div>
                    {% endif %}

                    {% if review.decisions %}
                    <div class="mb-3">
                        <div class="text-muted small fw-semibold">{{ 'management_review.decisions'|trans({}, 'management_review') }}</div>
                        {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'warning', icon: 'regulator' } } %}
                            {% block alert_body %}<strong><i class="fa-icon fa-icon--review" aria-hidden="true"></i> {{ 'management_review.management_decisions'|trans({}, 'management_review') }}</strong> <p class="mb-0 mt-2">{{ review.decisions|nl2br }}</p>{% endblock %}
                        {% endembed %}
                    </div>
                    {% endif %}

                    {% if review.actionItems %}
                    <div class="mb-0">
                        <div class="text-muted small fw-semibold">{{ 'management_review.action_items'|trans({}, 'management_review') }}</div>
                        {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', icon: 'nav-list-check' } } %}
                            {% block alert_body %}<strong><i class="fa-icon fa-icon--nav-list-check" aria-hidden="true"></i> {{ 'management_review.action_items_label'|trans({}, 'management_review') }}</strong> <p class="mb-0 mt-2">{{ review.actionItems|nl2br }}</p>{% endblock %}
                        {% endembed %}
                    </div>
                    {% endif %}
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-lg-4">
            {# Action Buttons #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="h5 mb-0"><i class="fa-icon fa-icon--nav-tools" aria-hidden="true"></i> {{ 'common.actions'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="d-grid gap-2">
                        {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_management_review_edit', {id: review.id}) }}"
                           class="fa-cyber-btn fa-cyber-btn--primary"
                           data-turbo-action="advance">
                            <i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i> {{ 'common.edit'|trans({}, 'messages') }}
                        </a>
                        {% endif %}

                        {% if is_granted('ROLE_MANAGER') %}
                        <div>
                            <a href="{{ path('app_management_review_pdf', {id: review.id}) }}"
                               class="fa-cyber-btn fa-cyber-btn--ghost"
                               data-turbo="false">
                                <i class="fa-icon fa-icon--nav-file-pdf" aria-hidden="true"></i>
                                {{ 'management_review.action.pdf'|trans({}, 'management_review') }}
                            </a>
                            {% include '_components/_fa_pdf_locale_switch.html.twig' with {route: 'app_management_review_pdf', route_params: {id: review.id}} only %}
                        </div>
                        {% endif %}

                        <a href="{{ path('app_management_review_index') }}"
                           class="fa-cyber-btn fa-cyber-btn--ghost"
                           data-turbo-action="advance">
                            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'common.back_to_list'|trans({}, 'messages') }}
                        </a>

                        {% if is_granted('ROLE_ADMIN') %}
                        <hr>
                        <button type="button"
                                class="fa-cyber-btn fa-cyber-btn--danger" data-controller="fa-modal-dispatcher" data-action="click->fa-modal-dispatcher#open" data-fa-modal-id="deleteModal">
                            <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                        </button>
                        {% endif %}
                    </div>
                {% endblock %}
            {% endembed %}

            {# ISO 27001 Clause Reference #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4 border-info' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="h5 mb-0"><i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i> ISO 27001:2022</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <h3 class="h6 fw-bold">{{ 'management_review.clause_93'|trans({}, 'management_review') }}</h3>
                    <p class="small mb-2">{{ 'management_review.clause_93_description'|trans({}, 'management_review') }}</p>

                    <hr>

                    <div class="fw-bold small">{{ 'management_review.inputs_label'|trans({}, 'management_review') }}</div>
                    <ul class="small mb-2">
                        <li>{{ 'management_review.input_1'|trans({}, 'management_review') }}</li>
                        <li>{{ 'management_review.input_2'|trans({}, 'management_review') }}</li>
                        <li>{{ 'management_review.input_3'|trans({}, 'management_review') }}</li>
                        <li>{{ 'management_review.input_4'|trans({}, 'management_review') }}</li>
                    </ul>

                    <div class="fw-bold small">{{ 'management_review.outputs_label'|trans({}, 'management_review') }}</div>
                    <ul class="small mb-0">
                        <li>{{ 'management_review.output_1'|trans({}, 'management_review') }}</li>
                        <li>{{ 'management_review.output_2'|trans({}, 'management_review') }}</li>
                        <li>{{ 'management_review.output_3'|trans({}, 'management_review') }}</li>
                    </ul>
                {% endblock %}
            {% endembed %}

            {# Metadata #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="h5 mb-0"><i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i> {{ 'common.metadata'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="row mb-0 small">
                        <dt class="col-sm-5 text-muted">{{ 'common.created_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-7">{{ review.createdAt|date('d.m.Y H:i') }}</dd>

                        <dt class="col-sm-5 text-muted">{{ 'common.updated_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-7">{{ review.updatedAt ? review.updatedAt|date('d.m.Y H:i') : '-' }}</dd>

                        <dt class="col-sm-5 text-muted">{{ 'common.id'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-7"><code>{{ review.id }}</code></dd>
                    </dl>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
    {% endif %} {# /M-4 reviews module gate #}
</div>

{# Delete Confirmation Modal #}
{% if is_granted('ROLE_ADMIN') %}
<div class="fa-modal fa-modal--confirm fa-modal--danger" id="deleteModal"
     data-controller="fa-modal" data-fa-modal-mode-value="confirm" data-fa-modal-tone-value="danger"
     role="alertdialog" aria-modal="true" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="fa-modal__backdrop" data-fa-modal-target="backdrop" data-action="click->fa-modal#backdropClick" aria-hidden="true"></div>
    <div class="fa-modal__container" data-fa-modal-target="dialog"><header class="fa-modal__header"><span class="fa-modal__icon" aria-hidden="true"><i class="fa-icon fa-icon--ui-trash"></i></span><div class="fa-modal__heading"><h3 class="fa-modal__title" id="deleteModalLabel">
                    {{ 'management_review.confirm_delete'|trans({}, 'management_review') }}
                </h3></div><button type="button" class="fa-modal__close" data-action="fa-modal#cancel" aria-label="Close"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i></button></header>
            <div class="fa-modal__body">
                <p>{{ 'management_review.delete_warning'|trans({'%title%': review.title}, 'management_review') }}</p>
                <p class="text-muted"><small>{{ 'common.action_irreversible'|trans({}, 'messages') }}</small></p>
            </div>
            <footer class="fa-modal__footer">
                <button type="button" class="fa-cyber-btn fa-cyber-btn--ghost" data-action="fa-modal#cancel">{{ 'common.cancel'|trans({}, 'messages') }}</button>
                <form method="post" action="{{ path('app_management_review_delete', {id: review.id}) }}" class="d-inline" data-fa-modal-target="form">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.id) }}">
                    <button type="submit" class="fa-cyber-btn fa-cyber-btn--danger">
                        <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                    </button>
                </form></div>
</div>
</div>
{% endif %}
{% endblock %}
