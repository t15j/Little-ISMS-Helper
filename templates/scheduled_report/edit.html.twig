{% extends 'base.html.twig' %}
{% trans_default_domain 'scheduled_reports' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'scheduled_report.edit.title'|trans({}, 'scheduled_reports') }} - {{ report.name }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.reports'|trans({}, 'nav'), url: path('app_reports_index') },
            { label: 'scheduled_report.title'|trans({}, 'scheduled_reports'), url: path('app_scheduled_report_index') },
            { label: report.name, url: path('app_scheduled_report_show', {id: report.id}) },
            { label: 'scheduled_report.edit.title'|trans({}, 'scheduled_reports') }
        ]
    } %}

    {{ _fa_page_header.render({
    title: ('scheduled_report.edit.title'|trans({}, 'scheduled_reports')),
    badge: { icon: 'ui-edit', label: '' }
}) }}

    {% embed '_components/_card.html.twig' with { 'class': 'shadow-sm' } %}
        {% block card_header %}
            <div class="card-header">
                <h3 class="h5 mb-0">
                    <i class="fa-icon fa-icon--ui-settings" aria-hidden="true"></i> {{ 'scheduled_report.form.configuration'|trans({}, 'scheduled_reports') }}
                </h3>
            </div>
        {% endblock %}
        {% block card_body %}
            {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

            <div class="row">
                {# Basic Info #}
                <div class="col-md-6 mb-3">
                    {{ form_row(form.name) }}
                </div>
                <div class="col-md-6 mb-3">
                    {{ form_row(form.reportType) }}
                </div>
            </div>

            <div class="row">
                <div class="col-md-12 mb-3">
                    {{ form_row(form.description) }}
                </div>
            </div>

            <hr>

            <h4 class="h6 mb-3">
                <i class="fa-icon fa-icon--ui-calendar-event" aria-hidden="true"></i> {{ 'scheduled_report.form.schedule_settings'|trans({}, 'scheduled_reports') }}
            </h4>

            <div class="row">
                <div class="col-md-4 mb-3">
                    {{ form_row(form.schedule) }}
                </div>
                <div class="col-md-4 mb-3" id="dayOfWeekGroup" style="{% if report.schedule != 'weekly' %}display: none;{% endif %}">
                    {{ form_row(form.dayOfWeek) }}
                </div>
                <div class="col-md-4 mb-3" id="dayOfMonthGroup" style="{% if report.schedule != 'monthly' %}display: none;{% endif %}">
                    {{ form_row(form.dayOfMonth) }}
                </div>
                <div class="col-md-4 mb-3">
                    {{ form_row(form.preferredTime) }}
                </div>
            </div>

            <hr>

            <h4 class="h6 mb-3">
                <i class="fa-icon fa-icon--nav-document" aria-hidden="true"></i> {{ 'scheduled_report.form.output_settings'|trans({}, 'scheduled_reports') }}
            </h4>

            <div class="row">
                <div class="col-md-6 mb-3">
                    {{ form_row(form.format) }}
                </div>
                <div class="col-md-6 mb-3">
                    {{ form_row(form.locale) }}
                </div>
            </div>

            <hr>

            <h4 class="h6 mb-3">
                <i class="fa-icon fa-icon--ui-send" aria-hidden="true"></i> {{ 'scheduled_report.form.delivery_settings'|trans({}, 'scheduled_reports') }}
            </h4>

            <div class="row">
                <div class="col-md-12 mb-3">
                    {{ form_row(form.recipientsText) }}
                </div>
            </div>

            <hr>

            <div class="d-flex justify-content-between">
                <a href="{{ path('app_scheduled_report_show', {id: report.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                    <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'scheduled_report.action.cancel'|trans({}, 'scheduled_reports') }}
                </a>
                <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                    <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i> {{ 'scheduled_report.action.save'|trans({}, 'scheduled_reports') }}
                </button>
            </div>

            {{ form_end(form) }}
        {% endblock %}
    {% endembed %}

    {# Last Run Info (if available) #}
    {% if report.lastRunAt %}
    {% embed '_components/_card.html.twig' with { 'class': 'shadow-sm mt-4' } %}
        {% block card_header %}
            <div class="card-header">
                <h3 class="h5 mb-0">
                    <i class="fa-icon fa-icon--nav-calendar-check" aria-hidden="true"></i> {{ 'scheduled_report.last_run.title'|trans({}, 'scheduled_reports') }}
                </h3>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="row">
                <div class="col-md-4">
                    <strong>{{ 'scheduled_report.last_run.time'|trans({}, 'scheduled_reports') }}:</strong><br>
                    {{ report.lastRunAt|date('Y-m-d H:i') }}
                </div>
                <div class="col-md-4">
                    <strong>{{ 'scheduled_report.last_run.status'|trans({}, 'scheduled_reports') }}:</strong><br>
                    {% if report.lastRunStatus == 1 %}
                    {% include '_components/_badge.html.twig' with { variant: 'success', content: 'scheduled_report.last_run.success'|trans({}, 'scheduled_reports') } %}
                    {% else %}
                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'scheduled_report.last_run.failed'|trans({}, 'scheduled_reports') } %}
                    {% endif %}
                </div>
                <div class="col-md-4">
                    <strong>{{ 'scheduled_report.last_run.message'|trans({}, 'scheduled_reports') }}:</strong><br>
                    {{ report.lastRunMessage|default('-') }}
                </div>
            </div>
        {% endblock %}
    {% endembed %}
    {% endif %}
</div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
document.addEventListener('turbo:load', function() {
    const scheduleSelect = document.getElementById('scheduled_report_schedule');
    const dayOfWeekGroup = document.getElementById('dayOfWeekGroup');
    const dayOfMonthGroup = document.getElementById('dayOfMonthGroup');

    function updateScheduleFields() {
        if (!scheduleSelect) return;

        const schedule = scheduleSelect.value;

        if (dayOfWeekGroup) {
            dayOfWeekGroup.style.display = schedule === 'weekly' ? 'block' : 'none';
        }
        if (dayOfMonthGroup) {
            dayOfMonthGroup.style.display = schedule === 'monthly' ? 'block' : 'none';
        }
    }

    if (scheduleSelect) {
        scheduleSelect.addEventListener('change', updateScheduleFields);
        updateScheduleFields();
    }
});
</script>
{% endblock %}
