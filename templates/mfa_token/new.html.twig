{% extends 'admin/layout.html.twig' %}

{% block admin_title %}{{ 'mfa_token.new'|trans({}, 'mfa') }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'mfa_token.new.subtitle'|trans({}, 'mfa') }}</p>
{% endblock %}

{% block admin_content %}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.admin'|trans({}, 'nav'), url: path('admin_dashboard') },
            { label: 'nav.mfa'|trans({}, 'nav'), url: path('admin_mfa_index') },
            { label: 'mfa_token.new'|trans({}, 'mfa') }
        ]
    } %}

    {% embed '_components/_card.html.twig' %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0 h5">{{ 'mfa_token.new'|trans({}, 'mfa') }}</h2>
            </div>
        {% endblock %}
        {% block card_body %}
            {{ form_start(form) }}

            {# H-03: strict_whitelist — MFA token form must never leak hidden
               fields like secret, last_used_at, recovery_codes via catch-all. #}
            {% include '_components/_auto_form.html.twig' with {
                form: form,
                strict_whitelist: true,
                sections: {
                    'basic_info': {
                        'label': 'mfa_token.section.basic_info'|trans({}, 'mfa'),
                        'fields': ['user', 'tokenType', 'deviceName', 'phoneNumber']
                    },
                    'settings': {
                        'label': 'mfa_token.section.settings'|trans({}, 'mfa'),
                        'fields': ['isActive', 'isPrimary', 'expiresAt']
                    }
                },
                'col_widths': {
                    'user': 12,
                    'tokenType': 6,
                    'deviceName': 6,
                    'phoneNumber': 12,
                    'isActive': 6,
                    'isPrimary': 6,
                    'expiresAt': 12
                }
            } %}

            <div class="d-flex justify-content-between pt-3 border-top">
                <a href="{{ path('admin_mfa_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                    {{ 'common.back'|trans({}, 'messages') }}
                </a>
                <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                    {{ 'common.save'|trans({}, 'messages') }}
                </button>
            </div>
            {{ form_end(form) }}
        {% endblock %}
    {% endembed %}
{% endblock %}
