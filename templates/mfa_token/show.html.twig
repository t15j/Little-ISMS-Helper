{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block admin_title %}{{ mfa_token.tokenTypeName }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'mfa_token.details.subtitle'|trans({}, 'mfa') }}</p>
{% endblock %}

{% block admin_content %}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.admin'|trans({}, 'nav'), url: path('admin_dashboard') },
            { label: 'nav.mfa'|trans({}, 'nav'), url: path('admin_mfa_index') },
            { label: mfa_token.tokenTypeName }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'nav-shield-lock', label: 'nav.mfa'|trans({}, 'nav') },
        title: mfa_token.tokenTypeName,
        subtitle: 'mfa_token.details.subtitle'|trans({}, 'mfa'),
        actions: [
            { label: 'common.back'|trans({}, 'messages'), variant: 'ghost', icon: 'util-arrow-left', href: path('admin_mfa_index') }
        ]
    }) }}

    <div class="row">
        <div class="col-lg-8">
            {# Token Details #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="mb-0"><i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i> {{ 'mfa_token.details.title'|trans({}, 'mfa') }}</h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="row">
                        <dt class="col-sm-4">{{ 'mfa_token.field.user'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            <i class="fa-icon fa-icon--ui-person" aria-hidden="true"></i>
                            {{ mfa_token.user.firstName }} {{ mfa_token.user.lastName }}
                            <span class="text-muted">({{ mfa_token.user.email }})</span>
                        </dd>

                        <dt class="col-sm-4">{{ 'mfa_token.field.token_type'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            {% include '_components/_badge.html.twig' with { variant: 'primary', icon: mfa_token.tokenType == 'totp' ? 'phone' : 'ui-key', content: ('mfa_token.type.' ~ mfa_token.tokenType)|trans({}, 'mfa') } %}
                        </dd>

                        <dt class="col-sm-4">{{ 'mfa_token.field.device_name'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            <i class="fa-icon fa-icon--{{ mfa_token.tokenType == 'totp' ? 'phone' : 'ui-key' }}" aria-hidden="true"></i>
                            {{ mfa_token.deviceName ?? 'N/A' }}
                        </dd>

                        <dt class="col-sm-4">{{ 'mfa_token.field.status'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            {% if mfa_token.isActive %}
                                {% include '_components/_badge.html.twig' with { variant: 'success', icon: 'status-ok', content: 'mfa_token.status.active'|trans({}, 'mfa') } %}
                            {% else %}
                                {% include '_components/_badge.html.twig' with { variant: 'secondary', icon: 'ui-x', content: 'mfa_token.status.inactive'|trans({}, 'mfa') } %}
                            {% endif %}
                        </dd>

                        <dt class="col-sm-4">{{ 'mfa_token.field.is_primary'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            {% if mfa_token.isPrimary %}
                                {% include '_components/_badge.html.twig' with { variant: 'warning', icon: 'ui-star', content: 'mfa_token.primary'|trans({}, 'mfa') } %}
                            {% else %}
                                <span class="text-muted">{{ 'common.no'|trans({}, 'messages') }}</span>
                            {% endif %}
                        </dd>
                    </dl>
                {% endblock %}
            {% endembed %}

            {# Usage Statistics #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="mb-0"><i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i> {{ 'mfa_token.usage.title'|trans({}, 'mfa') }}</h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="row">
                        <dt class="col-sm-4">{{ 'mfa_token.field.enrolled_at'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            <i class="fa-icon fa-icon--nav-calendar-check" aria-hidden="true"></i>
                            {{ mfa_token.enrolledAt|date('Y-m-d H:i:s') }}
                            <small class="text-muted">({{ mfa_token.enrolledAt|date('r') }})</small>
                        </dd>

                        <dt class="col-sm-4">{{ 'mfa_token.field.last_used'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            {% if mfa_token.lastUsedAt %}
                                <i class="fa-icon fa-icon--ui-clock" aria-hidden="true"></i>
                                {{ mfa_token.lastUsedAt|date('Y-m-d H:i:s') }}
                                <small class="text-muted">({{ mfa_token.lastUsedAt|date('r') }})</small>
                            {% else %}
                                <span class="text-muted">{{ 'mfa_token.never_used'|trans({}, 'mfa') }}</span>
                            {% endif %}
                        </dd>

                        <dt class="col-sm-4">{{ 'mfa_token.field.usage_count'|trans({}, 'mfa') }}</dt>
                        <dd class="col-sm-8">
                            {% include '_components/_badge.html.twig' with { variant: 'info', content: mfa_token.usageCount } %}
                            {{ 'mfa_token.usage.times'|trans({}, 'mfa') }}
                        </dd>

                        {% if mfa_token.expiresAt %}
                            <dt class="col-sm-4">{{ 'mfa_token.field.expires_at'|trans({}, 'mfa') }}</dt>
                            <dd class="col-sm-8">
                                <i class="fa-icon fa-icon--ui-calendar" aria-hidden="true"></i>
                                {{ mfa_token.expiresAt|date('Y-m-d H:i:s') }}
                                {% if mfa_token.isExpired %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'mfa_token.status.expired'|trans({}, 'mfa'), class: 'ms-2' } %}
                                {% endif %}
                            </dd>
                        {% endif %}
                    </dl>
                {% endblock %}
            {% endembed %}

            {# Backup Codes (if TOTP) #}
            {% if mfa_token.tokenType == 'totp' %}
                {% embed '_components/_card.html.twig' with { 'class': 'mb-4 border-warning' } %}
                    {% block card_header %}
                        <div class="card-header">
                            <h3 class="mb-0"><i class="fa-icon fa-icon--nav-key text-warning" aria-hidden="true"></i> {{ 'mfa_token.backup_codes.title'|trans({}, 'mfa') }}</h3>
                        </div>
                    {% endblock %}
                    {% block card_body %}
                        {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <div>
                                <strong>{{ 'mfa_token.backup_codes.remaining'|trans({}, 'mfa') }}:</strong>
                                {% include '_components/_badge.html.twig' with { variant: remaining_backup_codes > 2 ? 'success' : (remaining_backup_codes > 0 ? 'warning' : 'danger'), content: remaining_backup_codes ~ ' / 10' } %}
                            </div>
                            <form method="post" action="{{ path('admin_mfa_regenerate_backup', {id: mfa_token.id}) }}"
                                  data-controller="ui-actions"
                                  data-action="submit->ui-actions#confirm"
                                  data-ui-actions-message-param="{{ 'mfa_token.backup_codes.regenerate_confirm'|trans({}, 'mfa') }}">
                                <input type="hidden" name="_token" value="{{ csrf_token('regenerate_backup_' ~ mfa_token.id) }}">
                                <button type="submit" class="fa-cyber-btn fa-cyber-btn--warning fa-cyber-btn--sm">
                                    <i class="fa-icon fa-icon--ui-refresh" aria-hidden="true"></i> {{ 'mfa_token.backup_codes.regenerate'|trans({}, 'mfa') }}
                                </button>
                            </form>
                        </div>

                        {% if remaining_backup_codes <= 2 %}
                            {% set backupVariant = remaining_backup_codes == 0 ? 'danger' : 'warning' %}
                            {% set backupMsg = remaining_backup_codes == 0 ? 'mfa_token.backup_codes.no_codes_left'|trans({}, 'mfa') : 'mfa_token.backup_codes.low_codes'|trans({}, 'mfa') %}
                            {{ _fa_alert.render({ variant: backupVariant, body: backupMsg }) }}
                        {% endif %}

                        <p class="small text-muted mb-0">
                            {{ 'mfa_token.backup_codes.description'|trans({}, 'mfa') }}
                        </p>
                    {% endblock %}
                {% endembed %}
            {% endif %}
        </div>

        <div class="col-lg-4">
            {# Actions #}
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="mb-0"><i class="fa-icon fa-icon--ui-settings" aria-hidden="true"></i> {{ 'common.actions'|trans({}, 'messages') }}</h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="d-grid gap-2">
                        {% if mfa_token.isActive %}
                            <form method="post" action="{{ path('admin_mfa_disable', {id: mfa_token.id}) }}"
                                  data-controller="ui-actions"
                                  data-action="submit->ui-actions#confirm"
                                  data-ui-actions-message-param="{{ 'mfa_token.confirm_disable'|trans({}, 'mfa') }}">
                                <input type="hidden" name="_token" value="{{ csrf_token('disable_' ~ mfa_token.id) }}">
                                <button type="submit" class="fa-cyber-btn fa-cyber-btn--warning w-100">
                                    <i class="fa-icon fa-icon--status-pending" aria-hidden="true"></i> {{ 'mfa_token.action.disable'|trans({}, 'mfa') }}
                                </button>
                            </form>
                        {% endif %}

                        <form method="post" action="{{ path('admin_mfa_delete', {id: mfa_token.id}) }}"
                              data-controller="ui-actions"
                              data-action="submit->ui-actions#confirm"
                              data-ui-actions-message-param="{{ 'mfa_token.confirm_delete'|trans({}, 'mfa') }}">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete_' ~ mfa_token.id) }}">
                            <button type="submit" class="fa-cyber-btn fa-cyber-btn--danger w-100">
                                <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                            </button>
                        </form>
                    </div>
                {% endblock %}
            {% endembed %}

            {# System Information #}
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="mb-0"><i class="fa-icon fa-icon--asset-database" aria-hidden="true"></i> {{ 'common.system_info'|trans({}, 'messages') }}</h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="mb-0 small">
                        <dt>{{ 'common.id'|trans({}, 'messages') }}</dt>
                        <dd class="font-monospace">{{ mfa_token.id }}</dd>

                        <dt>{{ 'common.created_at'|trans({}, 'messages') }}</dt>
                        <dd>{{ mfa_token.createdAt|date('Y-m-d H:i:s') }}</dd>

                        {% if mfa_token.updatedAt %}
                            <dt>{{ 'common.updated_at'|trans({}, 'messages') }}</dt>
                            <dd>{{ mfa_token.updatedAt|date('Y-m-d H:i:s') }}</dd>
                        {% endif %}
                    </dl>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
{% endblock %}
