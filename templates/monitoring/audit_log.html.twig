{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% trans_default_domain 'audit_log' %}

{% block admin_title %}{{ 'audit_log.page.title'|trans({}, 'audit_log') }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'audit_log.page.monitoring_subtitle'|trans({}, 'audit_log') }}</p>
{% endblock %}

{% block admin_content %}
    <div class="mb-3">
        <a href="{{ path('monitoring_health') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'audit_log.action.back_to_health'|trans({}, 'audit_log') }}
        </a>
    </div>

    {# Statistics #}
    <div class="row mb-4">
        <div class="col-md-4">
            {% embed '_components/_card.html.twig' with { 'class': 'border-primary' } %}
                {% block card_body %}
                    <div class="text-center">
                        <div class="card-title text-muted fw-semibold">{{ 'audit_log.stats.total_entries'|trans({}, 'audit_log') }}</div>
                        <p class="display-6 text-primary">{{ statistics.total }}</p>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-4">
            {% embed '_components/_card.html.twig' with { 'class': 'border-success' } %}
                {% block card_body %}
                    <div class="card-title text-muted fw-semibold">{{ 'audit_log.stats.top_actions'|trans({}, 'audit_log') }}</div>
                    {% for stat in statistics.action_stats|slice(0, 5) %}
                        <div class="d-flex justify-content-between">
                            {% include '_components/_badge.html.twig' with { variant: 'primary', content: stat.action } %}
                            <span>{{ stat.count }}</span>
                        </div>
                    {% endfor %}
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-md-4">
            {% embed '_components/_card.html.twig' with { 'class': 'border-info' } %}
                {% block card_body %}
                    <div class="card-title text-muted fw-semibold">{{ 'audit_log.stats.top_entities'|trans({}, 'audit_log') }}</div>
                    {% for stat in statistics.entity_stats|slice(0, 5) %}
                        <div class="d-flex justify-content-between">
                            <span>{{ stat.entityType }}</span>
                            {% include '_components/_badge.html.twig' with { variant: 'info', content: stat.count } %}
                        </div>
                    {% endfor %}
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Quick Filters #}
    {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--ui-funnel" aria-hidden="true"></i> {{ 'audit_log.section.quick_filters'|trans({}, 'audit_log') }}
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="btn-group btn-group-sm" role="group">
                <a href="{{ path('monitoring_audit_log', {filter: 'all'}) }}"
                   class="fa-cyber-btn fa-cyber-btn--sm {{ current_filter == 'all' ? 'fa-cyber-btn--primary' : 'fa-cyber-btn--ghost' }}">
                    {{ 'audit_log.filter.quick.all'|trans({}, 'audit_log') }}
                </a>
                <a href="{{ path('monitoring_audit_log', {filter: 'today'}) }}"
                   class="fa-cyber-btn fa-cyber-btn--sm {{ current_filter == 'today' ? 'fa-cyber-btn--primary' : 'fa-cyber-btn--ghost' }}">
                    {{ 'audit_log.filter.quick.today'|trans({}, 'audit_log') }}
                </a>
                <a href="{{ path('monitoring_audit_log', {filter: 'week'}) }}"
                   class="fa-cyber-btn fa-cyber-btn--sm {{ current_filter == 'week' ? 'fa-cyber-btn--primary' : 'fa-cyber-btn--ghost' }}">
                    {{ 'audit_log.filter.quick.this_week'|trans({}, 'audit_log') }}
                </a>
                <a href="{{ path('monitoring_audit_log', {filter: 'critical'}) }}"
                   class="fa-cyber-btn fa-cyber-btn--sm {{ current_filter == 'critical' ? 'fa-cyber-btn--danger' : 'fa-cyber-btn--ghost' }}">
                    {{ 'audit_log.filter.quick.critical_actions'|trans({}, 'audit_log') }}
                </a>
            </div>
        {% endblock %}
    {% endembed %}

    {# Audit Log Entries #}
    {% embed '_components/_card.html.twig' with { 'noPadding': true } %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--ui-list" aria-hidden="true"></i> {{ 'audit_log.section.entries'|trans({}, 'audit_log') }} ({{ logs|length }})
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            {% if logs|length > 0 %}
                {% embed '_components/_table.html.twig' with {
                    'class': 'table-hover mb-0',
                    'theadClass': 'table-light',
                    'noMargin': true,
                    'responsive': true
                } %}
                    {% block table_head %}
                        <tr>
                            <th scope="col">{{ 'audit_log.table.timestamp'|trans({}, 'audit_log') }}</th>
                            <th scope="col">{{ 'audit_log.table.user'|trans({}, 'audit_log') }}</th>
                            <th scope="col">{{ 'audit_log.table.action'|trans({}, 'audit_log') }}</th>
                            <th scope="col">{{ 'audit_log.table.entity'|trans({}, 'audit_log') }}</th>
                            <th scope="col">{{ 'audit_log.table.ip_address'|trans({}, 'audit_log') }}</th>
                            <th scope="col" class="text-end">{{ 'audit_log.table.details'|trans({}, 'audit_log') }}</th>
                        </tr>
                    {% endblock %}
                    {% block table_body %}
                        {% for log in logs %}
                            <tr>
                                <td>
                                    <small>{{ log.createdAt|date('Y-m-d H:i:s') }}</small>
                                </td>
                                <td>
                                    <i class="fa-icon fa-icon--ui-person" aria-hidden="true"></i>
                                    {{ log.userName }}
                                </td>
                                <td>
                                    {% include '_components/_badge.html.twig' with { variant: log.action in ['delete', 'destroy', 'remove'] ? 'danger' : (log.action in ['create', 'add'] ? 'success' : 'primary'), content: log.action } %}
                                </td>
                                <td>
                                    {% if log.entityType %}
                                        {{ log.entityType }}
                                        {% if log.entityId %}
                                            <code>#{{ log.entityId }}</code>
                                        {% endif %}
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <code class="small">{{ log.ipAddress ?? '-' }}</code>
                                </td>
                                <td class="text-end">
                                    {% if log.description or log.newValues %}
                                        <button type="button"
                                                class="fa-cyber-btn fa-cyber-btn--sm fa-cyber-btn--ghost"
                                                data-controller="fa-modal-dispatcher" data-action="click->fa-modal-dispatcher#open" data-fa-modal-id="logModal{{ log.id }}">
                                            <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i> {{ 'audit_log.table.view_details'|trans({}, 'audit_log') }}
                                        </button>
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                            </tr>

                            {# Modal for log details #}
                            {% if log.description or log.newValues %}
                                <div class="fa-modal fa-modal--confirm fa-modal--neutral" id="logModal{{ log.id }}"
     data-controller="fa-modal" data-fa-modal-mode-value="confirm" data-fa-modal-tone-value="neutral"
     role="dialog" aria-modal="true" aria-labelledby="logModal{{ log.id }}Label" aria-hidden="true">
    <div class="fa-modal__backdrop" data-fa-modal-target="backdrop" data-action="click->fa-modal#backdropClick" aria-hidden="true"></div>
    <div class="fa-modal__container" data-fa-modal-target="dialog"><header class="fa-modal__header"><span class="fa-modal__icon" aria-hidden="true"><i class="fa-icon fa-icon--ui-info"></i></span><div class="fa-modal__heading"><h3  id="logModal{{ log.id }}Label" class="fa-modal__title">{{ 'audit_log.modal.title'|trans({}, 'audit_log') }}</h3></div><button type="button" class="fa-modal__close" data-action="fa-modal#cancel" aria-label="Close"><i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i></button></header>
                                            <div class="fa-modal__body">
                                                <dl class="row">
                                                    <dt class="col-sm-3">{{ 'audit_log.table.timestamp'|trans({}, 'audit_log') }}:</dt>
                                                    <dd class="col-sm-9">{{ log.createdAt|date('Y-m-d H:i:s') }}</dd>

                                                    <dt class="col-sm-3">{{ 'audit_log.table.user'|trans({}, 'audit_log') }}:</dt>
                                                    <dd class="col-sm-9">{{ log.userName }}</dd>

                                                    <dt class="col-sm-3">{{ 'audit_log.table.action'|trans({}, 'audit_log') }}:</dt>
                                                    <dd class="col-sm-9">
                                                        {% include '_components/_badge.html.twig' with { variant: 'primary', content: log.action } %}
                                                    </dd>

                                                    {% if log.description %}
                                                        <dt class="col-sm-3">{{ 'audit_log.table.description'|trans({}, 'audit_log') }}:</dt>
                                                        <dd class="col-sm-9">{{ log.description }}</dd>
                                                    {% endif %}

                                                    {% if log.newValues %}
                                                        <dt class="col-sm-3">{{ 'audit_log.table.changes'|trans({}, 'audit_log') }}:</dt>
                                                        <dd class="col-sm-9">
                                                            <pre class="bg-body-secondary p-2 rounded">{{ log.newValues }}</pre>
                                                        </dd>
                                                    {% endif %}
                                                </dl></div>
</div>
                                </div>
                            {% endif %}
                        {% endfor %}
                    {% endblock %}
                {% endembed %}
            {% else %}
                <div class="p-4 text-center">
                    <i class="fa-icon fa-icon--ui-info text-info fs-1" aria-hidden="true"></i>
                    <p class="mt-2">{{ 'audit_log.message.no_entries_filtered'|trans({}, 'audit_log') }}</p>
                </div>
            {% endif %}
        {% endblock %}
    {% endembed %}

    {# Info Box #}
    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', class: 'mt-4' } } %}
        {% block alert_body %}
        <div class="fa-alert__title">{{ 'audit_log.info.about_title'|trans({}, 'audit_log') }}</div>
        <p class="mb-0">
            {{ 'audit_log.info.about_text'|trans({}, 'audit_log') }}
            <a href="{{ path('app_audit_log_index') }}" class="fa-alert__link">{{ 'audit_log.info.module_link'|trans({}, 'audit_log') }}</a>.
        </p>
        {% endblock %}
    {% endembed %}
{% endblock %}
