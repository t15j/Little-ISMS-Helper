{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% trans_default_domain 'monitoring' %}
{% block admin_title %}{{ 'monitoring.errors.title'|trans({}, 'monitoring') }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'monitoring.errors.subtitle'|trans({}, 'monitoring') }}</p>
{% endblock %}

{% block admin_content %}
    <div class="mb-3">
        <a href="{{ path('monitoring_health') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'monitoring.back_to_health'|trans({}, 'monitoring') }}
        </a>
    </div>

    {% if has_log %}
        {# Statistics #}
        <div class="row mb-4">
            <div class="col-md-3">
                {% embed '_components/_card.html.twig' with { 'class': 'border-danger' } %}
                    {% block card_body %}
                        <div class="text-center">
                            <div class="card-title text-muted fw-semibold">{{ 'monitoring.errors.total'|trans({}, 'monitoring') }}</div>
                            <p class="display-6 text-danger">{{ error_stats.total }}</p>
                            <small>{{ 'monitoring.errors.shown'|trans({'%count%': errors|length}, 'monitoring') }}</small>
                        </div>
                    {% endblock %}
                {% endembed %}
            </div>
            <div class="col-md-3">
                {% embed '_components/_card.html.twig' with { 'class': 'border-warning' } %}
                    {% block card_body %}
                        <div class="text-center">
                            <div class="card-title text-muted fw-semibold">{{ 'monitoring.errors.file_size'|trans({}, 'monitoring') }}</div>
                            <p class="display-6 text-warning small">{{ error_stats.file_size }}</p>
                            <small>{{ 'monitoring.errors.log_file_size'|trans({}, 'monitoring') }}</small>
                        </div>
                    {% endblock %}
                {% endembed %}
            </div>
            <div class="col-md-6">
                {% embed '_components/_card.html.twig' with { 'class': 'border-info' } %}
                    {% block card_body %}
                        <div class="card-title text-muted fw-semibold">{{ 'monitoring.by_level'|trans({}, 'monitoring')}}</div>
                        {% for level, count in error_stats.by_level %}
                            <div class="d-flex justify-content-between mb-2">
                                {% include '_components/_badge.html.twig' with { variant: level == 'ERROR' ? 'danger' : (level == 'WARNING' ? 'warning' : 'info'), content: level } %}
                                <span>{{ count }}</span>
                            </div>
                        {% endfor %}
                    {% endblock %}
                {% endembed %}
            </div>
        </div>

        {# Log File Info #}
        {{ _fa_alert.render({ variant: 'info', class: 'mb-4', body: '<strong>' ~ 'monitoring.errors.log_file'|trans({}, 'monitoring') ~ ':</strong> <code>' ~ log_file ~ '</code><br><strong>' ~ 'monitoring.errors.last_modified'|trans({}, 'monitoring') ~ ':</strong> ' ~ error_stats.last_modified }) }}

        {# Filter Options #}
        {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
            {% block card_body %}
                <form method="get" class="row g-3">
                    <div class="col-md-2">
                        <label for="limit" class="form-label small">{{ 'monitoring.show_entries'|trans({}, 'monitoring')}}</label>
                        <select name="limit" id="limit" class="form-select fa-cyber-input__field">
                            <option value="50" {{ app.request.query.get('limit') == 50 ? 'selected' : '' }}>50</option>
                            <option value="100" {{ app.request.query.get('limit') == 100 or not app.request.query.get('limit') ? 'selected' : '' }}>100</option>
                            <option value="200" {{ app.request.query.get('limit') == 200 ? 'selected' : '' }}>200</option>
                            <option value="500" {{ app.request.query.get('limit') == 500 ? 'selected' : '' }}>500</option>
                            <option value="1000" {{ app.request.query.get('limit') == 1000 ? 'selected' : '' }}>1000</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label for="level" class="form-label small">{{ 'monitoring.errors.filter.level'|trans({}, 'monitoring') }}</label>
                        <select name="level[]" id="level" class="form-select fa-cyber-input__field" multiple size="4">
                            {% for level in available_levels %}
                                <option value="{{ level }}" {{ level in filters.levels ? 'selected' : '' }}>{{ level }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label for="channel" class="form-label small">{{ 'monitoring.errors.filter.channel'|trans({}, 'monitoring') }}</label>
                        <select name="channel[]" id="channel" class="form-select fa-cyber-input__field" multiple size="4">
                            {% for channel in available_channels %}
                                <option value="{{ channel }}" {{ channel in filters.channels ? 'selected' : '' }}>{{ channel }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label for="since" class="form-label small">{{ 'monitoring.errors.filter.since'|trans({}, 'monitoring') }}</label>
                        <select name="since" id="since" class="form-select fa-cyber-input__field">
                            <option value="" {{ filters.since == '' ? 'selected' : '' }}>{{ 'monitoring.errors.filter.since_all'|trans({}, 'monitoring') }}</option>
                            <option value="1h" {{ filters.since == '1h' ? 'selected' : '' }}>{{ 'monitoring.errors.filter.since_1h'|trans({}, 'monitoring') }}</option>
                            <option value="24h" {{ filters.since == '24h' ? 'selected' : '' }}>{{ 'monitoring.errors.filter.since_24h'|trans({}, 'monitoring') }}</option>
                            <option value="7d" {{ filters.since == '7d' ? 'selected' : '' }}>{{ 'monitoring.errors.filter.since_7d'|trans({}, 'monitoring') }}</option>
                            <option value="30d" {{ filters.since == '30d' ? 'selected' : '' }}>{{ 'monitoring.errors.filter.since_30d'|trans({}, 'monitoring') }}</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label for="search" class="form-label small">{{ 'monitoring.errors.filter.search'|trans({}, 'monitoring') }}</label>
                        <input type="search" name="search" id="search" class="form-control fa-cyber-input__field" value="{{ filters.search }}" placeholder="{{ 'monitoring.errors.filter.search_placeholder'|trans({}, 'monitoring') }}">
                    </div>
                    <div class="col-12 d-flex gap-2">
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary fa-cyber-btn--sm">
                            <i class="fa-icon fa-icon--ui-search" aria-hidden="true"></i>
                            {{ 'monitoring.errors.filter.apply'|trans({}, 'monitoring') }}
                        </button>
                        <a href="{{ path('monitoring_errors') }}" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
                            <i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i>
                            {{ 'monitoring.errors.filter.reset'|trans({}, 'monitoring') }}
                        </a>
                    </div>
                </form>
            {% endblock %}
        {% endembed %}

        {# Error List #}
        {% embed '_components/_card.html.twig' with { 'noPadding': true } %}
            {% block card_header %}
                <div class="card-header">
                    <h2 class="mb-0">
                        <i class="fa-icon fa-icon--ui-list text-danger" aria-hidden="true"></i> {{ 'monitoring.errors.recent'|trans({}, 'monitoring') }}
                    </h2>
                </div>
            {% endblock %}
            {% block card_body %}
                {% if errors|length > 0 %}
                    <div class="list-group list-group-flush">
                        {% for error in errors %}
                            <div class="list-group-item">
                                <div class="d-flex w-100 justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <div class="d-flex align-items-center mb-2">
                                            {% include '_components/_badge.html.twig' with { variant: error.level == 'ERROR' ? 'danger' : (error.level == 'WARNING' ? 'warning' : (error.level == 'CRITICAL' ? 'dark' : 'info')), class: 'me-2', content: error.level } %}
                                            <small class="text-muted">
                                                <i class="fa-icon fa-icon--ui-clock" aria-hidden="true"></i> {{ error.timestamp }}
                                            </small>
                                            {% include '_components/_badge.html.twig' with { variant: 'secondary', class: 'ms-2', content: error.channel } %}
                                        </div>
                                        <p class="mb-1 font-monospace small">{{ error.message|slice(0, 300) }}{% if error.message|length > 300 %}...{% endif %}</p>
                                        <details class="mt-2">
                                            <summary class="text-muted small" style="cursor: pointer;">
                                                <i class="fa-icon fa-icon--nav-tools" aria-hidden="true"></i> {{ 'monitoring.errors.show_full'|trans({}, 'monitoring') }}
                                            </summary>
                                            <pre class="mt-2 mb-0 small bg-body-secondary p-3 rounded">{{ error.raw }}</pre>
                                        </details>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <div class="p-4 text-center">
                        <i class="fa-icon fa-icon--status-ok fs-1" aria-hidden="true"></i>
                        <p class="mt-2">{{ 'monitoring.no_errors_found'|trans({}, 'monitoring')}}</p>
                    </div>
                {% endif %}
            {% endblock %}
        {% endembed %}

        {# Warning Box #}
        {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'warning', class: 'mt-4' } } %}
            {% block alert_body %}
            <div class="fa-alert__title">{{ 'monitoring.errors.important_notes'|trans({}, 'monitoring') }}</div>
            <ul class="mb-0">
                <li>{{ 'monitoring.reverse_order'|trans({}, 'monitoring')}}</li>
                <li>{{ 'monitoring.recent_entries_only'|trans({}, 'monitoring')}}</li>
                <li>{{ 'monitoring.full_log_analysis'|trans({}, 'monitoring')}}</li>
                <li>{{ 'monitoring.large_files_warning'|trans({}, 'monitoring')}}</li>
            </ul>
            {% endblock %}
        {% endembed %}
    {% else %}
        {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
            {% block alert_body %}
            <div class="fa-alert__title">{{ 'monitoring.errors.no_log_file'|trans({}, 'monitoring') }}</div>
            <p class="mb-0">
                {{ 'monitoring.errors.no_log_file_text'|trans({'%file%': log_file}, 'monitoring')|raw }}
            </p>
            {% endblock %}
        {% endembed %}
    {% endif %}
</div>
{% endblock %}
