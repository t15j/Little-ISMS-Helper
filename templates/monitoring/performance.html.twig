{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% trans_default_domain 'monitoring' %}

{% block admin_title %}{{ 'monitoring.performance.title'|trans({}, 'monitoring') }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'monitoring.performance.subtitle'|trans({}, 'monitoring') }}</p>
{% endblock %}

{% block admin_content %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}

    <div class="mb-3">
        <a href="{{ path('monitoring_health') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'monitoring.back_to_health'|trans({}, 'monitoring') }}
        </a>
    </div>

    {# Overview Statistics #}
    <div class="row mb-4">
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'monitoring.performance.total_requests'|trans({}, 'monitoring'), value: total_requests, hint: 'monitoring.performance.last_24h'|trans({}, 'monitoring'), variant: 'primary' }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'monitoring.performance.unique_users'|trans({}, 'monitoring'), value: unique_users, hint: 'monitoring.performance.active_today'|trans({}, 'monitoring'), variant: 'success' }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'monitoring.performance.memory_usage'|trans({}, 'monitoring'), value: memory_usage, hint: 'monitoring.performance.current'|trans({}, 'monitoring'), variant: 'info' }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'monitoring.performance.memory_peak'|trans({}, 'monitoring'), value: memory_peak, hint: 'monitoring.performance.peak_usage'|trans({}, 'monitoring'), variant: 'warning' }) }}
        </div>
    </div>

    <div class="row">
        {# Top Actions #}
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--nav-activity" aria-hidden="true"></i> {{ 'monitoring.performance.top_actions'|trans({}, 'monitoring') }} ({{ 'monitoring.performance.last_24h'|trans({}, 'monitoring') }})
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    {% if top_actions|length > 0 %}
                        <div class="list-group list-group-flush">
                            {% for action, count in top_actions %}
                                <div class="list-group-item d-flex justify-content-between align-items-center">
                                    <span>
                                        <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                                        <code>{{ action }}</code>
                                    </span>
                                    <span>
                                        {% include '_components/_badge.html.twig' with { variant: 'primary', pill: true, content: count } %}
                                        <small class="text-muted ms-2">
                                            ({{ (count / total_requests * 100)|number_format(1) }}%)
                                        </small>
                                    </span>
                                </div>
                            {% endfor %}
                        </div>
                    {% else %}
                        {{ _fa_alert.render({ variant: 'info', class: 'mb-0', body: 'monitoring.performance.no_data'|trans({}, 'monitoring') }) }}
                    {% endif %}
                {% endblock %}
            {% endembed %}
        </div>

        {# Top Entities #}
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--nav-collection" aria-hidden="true"></i> {{ 'monitoring.performance.top_entities'|trans({}, 'monitoring') }} ({{ 'monitoring.performance.last_24h'|trans({}, 'monitoring') }})
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    {% if top_entities|length > 0 %}
                        <div class="list-group list-group-flush">
                            {% for entity, count in top_entities %}
                                <div class="list-group-item d-flex justify-content-between align-items-center">
                                    <span>
                                        <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                                        {{ entity }}
                                    </span>
                                    <span>
                                        {% include '_components/_badge.html.twig' with { variant: 'success', pill: true, content: count } %}
                                        <small class="text-muted ms-2">
                                            ({{ (count / total_requests * 100)|number_format(1) }}%)
                                        </small>
                                    </span>
                                </div>
                            {% endfor %}
                        </div>
                    {% else %}
                        {{ _fa_alert.render({ variant: 'info', class: 'mb-0', body: 'monitoring.performance.no_data'|trans({}, 'monitoring') }) }}
                    {% endif %}
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# System Resources #}
    {% embed '_components/_card.html.twig' %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--nav-cpu" aria-hidden="true"></i> {{ 'monitoring.performance.system_resources'|trans({}, 'monitoring') }}
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="row">
                <div class="col-md-4">
                    <dl class="row">
                        <dt class="col-sm-6">{{ 'monitoring.performance.memory_limit'|trans({}, 'monitoring') }}:</dt>
                        <dd class="col-sm-6"><code>{{ memory_limit }}</code></dd>

                        <dt class="col-sm-6">{{ 'monitoring.performance.current_usage'|trans({}, 'monitoring') }}:</dt>
                        <dd class="col-sm-6"><code>{{ memory_usage }}</code></dd>

                        <dt class="col-sm-6">{{ 'monitoring.performance.peak_usage'|trans({}, 'monitoring') }}:</dt>
                        <dd class="col-sm-6"><code>{{ memory_peak }}</code></dd>
                    </dl>
                </div>
                <div class="col-md-8">
                    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', class: 'mb-0' } } %}
                        {% block alert_body %}
                        <div class="fa-alert__title">{{ 'monitoring.performance.note_title'|trans({}, 'monitoring') }}</div>
                        <p class="mb-0">
                            {{ 'monitoring.performance.note_text'|trans({}, 'monitoring')|raw }}
                        </p>
                        {% endblock %}
                    {% endembed %}
                </div>
            </div>
        {% endblock %}
    {% endembed %}
</div>
{% endblock %}
