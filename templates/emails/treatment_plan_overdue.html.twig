{% extends 'emails/base.html.twig' %}

{% block subject %}Risikobehandlungsplan überfällig: {{ plan.title|default('Plan') }}{% endblock %}

{% block content %}
    <h2 style="color: #1e1b4b; font-size: 20px; font-weight: 600; margin: 0 0 16px 0;">
        Behandlungsplan überfällig
    </h2>
    <p style="font-size: 14px; color: #4c4a73; margin: 0 0 16px 0;">
        Der folgende Risikobehandlungsplan hat seine geplante Abschlussfrist überschritten:
    </p>

    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0"
           style="background-color: #fee2e2; border-left: 4px solid #dc2626; margin-bottom: 20px;">
        <tr>
            <td style="padding: 14px 16px;">
                <p style="margin: 0 0 4px 0; font-weight: 600; color: #1e1b4b; font-size: 14px;">
                    {{ plan.title|default('(ohne Titel)') }}
                </p>
                <p style="margin: 0; font-family: 'Courier New', monospace; font-size: 11px; color: #b91c1c;">
                    {% if plan.targetCompletionDate %}Deadline: {{ plan.targetCompletionDate|date('d.m.Y') }}{% endif %}
                    {% if plan.daysOverdue is not null %} · {{ plan.daysOverdue }} Tage überfällig{% endif %}
                </p>
            </td>
        </tr>
    </table>

    {% if link is defined and link %}
        <table role="presentation" cellspacing="0" cellpadding="0" border="0" style="margin: 8px 0 20px 0;">
            <tr>
                <td style="background-color: #0369a1; border-radius: 6px;">
                    <a href="{{ link }}" style="display: inline-block; padding: 10px 20px; color: #ffffff; text-decoration: none; font-weight: 500; font-size: 13px;">
                        Plan öffnen →
                    </a>
                </td>
            </tr>
        </table>
    {% endif %}

    <p style="font-size: 12px; color: #6d6b92; margin: 0;">
        Bitte aktualisieren Sie den Status oder verlängern Sie die Frist zeitnah.
    </p>
{% endblock %}
