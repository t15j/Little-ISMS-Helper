<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #dc2626; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .overdue-alert { background: #fee2e2; border: 1px solid #dc2626; padding: 10px; margin: 15px 0; border-radius: 4px; color: #991b1b; }
        .warning { background: #fef3c7; border-left: 4px solid #f59e0b; padding: 10px; margin: 15px 0; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>⚠️ {{ 'emails.workflow.overdue_title'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <div class="overdue-alert">
                <strong>🚨 {{ 'emails.workflow.overdue_alert'|trans({}, 'emails') }}</strong>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.workflow_field'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.workflow.name }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.related_entity'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.entityType }} (ID: {{ instance.entityId }})</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.current_step'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.currentStep.name }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.step_type'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.currentStep.stepType|upper }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.workflow_status'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.status|upper }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.started'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.startedAt|date('d.m.Y H:i') }}</span>
            </div>

            {% if instance.dueDate %}
            <div class="field">
                <span class="label">{{ 'emails.workflow.due_date'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.dueDate|date('d.m.Y') }}</span>
            </div>
            {% endif %}

            <div class="warning">
                <strong>{{ 'emails.workflow.action_required'|trans({}, 'emails') }}:</strong> {{ 'emails.workflow.action_required_text'|trans({}, 'emails') }}
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.workflow.automated_alert'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.workflow.login_process_step'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
