<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #dc2626; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
        .severity-critical { color: #dc2626; font-weight: bold; }
        .severity-high { color: #ea580c; font-weight: bold; }
        .severity-medium { color: #d97706; font-weight: bold; }
        .severity-low { color: #059669; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚨 {{ 'emails.incident.new_reported'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <div class="field">
                <span class="label">{{ 'emails.incident.title_field'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.title }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.incident.severity'|trans({}, 'emails') }}:</span>
                <span class="value severity-{{ incident.severity ? incident.severity.value|lower : '' }}">{{ incident.severity ? incident.severity.value|upper : '' }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.incident.category'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.category }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.incident.reported'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.createdAt|date('d.m.Y H:i') }}</span>
            </div>

            {% if incident.description %}
            <div class="field">
                <span class="label">{{ 'emails.incident.description'|trans({}, 'emails') }}:</span>
                <p class="value">{{ incident.description }}</p>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">{{ 'emails.incident.status'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.status ? incident.status.value|upper : '' }}</span>
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_notification'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.incident.footer_action'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
