{% extends 'setup/_layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% set active_step = 'demo' %}

{% block title %}{{ 'setup.step10.title'|trans({}, 'setup') }}{% endblock %}

{% block step_content %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
{% embed '_components/_card.html.twig' with { 'class': 'shadow-lg' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h1 class="h3 mb-0">
                            <i class="fa-icon me-2 fa-icon--nav-document" aria-hidden="true"></i>
                            {{ 'setup.step10.header'|trans({}, 'setup') }}
                        </h1>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="p-4">
                        <p class="lead mb-4">
                            {{ 'setup.step10.description'|trans({}, 'setup') }}
                        </p>

                        <div class="fa-alert fa-alert--warning" role="alert">
                            <i class="fa-icon fa-icon--status-warning fa-alert__icon" aria-hidden="true"></i>
                            <div class="fa-alert__body"><p class="h5 mb-2">
                                <i class="fa-icon fa-icon--status-warning me-2" aria-hidden="true"></i>
                                {{ 'setup.step10.warning.title'|trans({}, 'setup') }}
                            </p>
                            <ul class="mb-0">
                                <li>{{ 'setup.step10.warning.optional'|trans({}, 'setup') }}</li>
                                <li>{{ 'setup.can_delete_later'|trans({}, 'setup')}}</li>
                                <li>{{ 'setup.step10.warning.selection'|trans({}, 'setup') }}</li>
                            </ul>
                            </div>
                        </div>

                        <h2 class="h5 mb-3">
                            <i class="fa-icon me-2 fa-icon--asset-database" aria-hidden="true"></i>
                            {{ 'setup.step10.import_title'|trans({}, 'setup') }}
                        </h2>

                        {% if sample_data|length > 0 %}
                            <form method="post" action="{{ path('setup_step10_sample_data_import') }}"
                                  data-controller="async-job"
                                  data-async-job-status-url-value="{{ path('setup_job_status') }}?job=sample_data"
                                  data-async-job-poll-interval-value="2000"
                                  data-async-job-message-running-value="{{ 'setup.alva.busy.sample_data'|trans({}, 'setup') }}"
                                  data-async-job-message-success-value="{{ 'setup.alva.success.sample_data'|trans({}, 'setup') }}"
                                  data-async-job-redirect-url-value="{{ path('setup_step11_complete') }}"
                                  data-action="submit->async-job#start">
                                <input type="hidden" name="_token" value="{{ csrf_token('setup_sample_data') }}">
                                <h3 class="h5 mb-3 mt-4">
                                    <i class="fa-icon fa-icon--ui-check me-2" aria-hidden="true"></i>
                                    {{ 'setup.step10.available_title'|trans({}, 'setup') }}
                                </h3>

                                {% for key, data in sample_data %}
                                    {% embed '_components/_card.html.twig' with { 'class': 'mb-3' } %}
                                        {% block card_body %}
                                            <div class="form-check">
                                                <input class="form-check-input" type="checkbox" name="samples[{{ key }}]" value="1" id="sample_{{ key }}">
                                                <label class="form-check-label w-100" for="sample_{{ key }}">
                                                    <div class="d-flex align-items-start">
                                                        <div class="flex-grow-1">
                                                            <p class="h6 mb-1">{{ data.name }}</p>
                                                            <p class="text-muted mb-2">{{ data.description }}</p>
                                                            {% if data.command is defined %}
                                                                <small class="text-muted">
                                                                    <i class="fa-icon me-1 fa-icon--nav-tools" aria-hidden="true"></i>
                                                                    {{ 'setup.step10.command'|trans({}, 'setup') }}: <code>{{ data.command }}</code>
                                                                </small>
                                                            {% elseif data.file is defined %}
                                                                <small class="text-muted">
                                                                    <i class="fa-icon me-1 fa-icon--nav-document" aria-hidden="true"></i>
                                                                    {{ 'setup.step10.file'|trans({}, 'setup') }}: <code>{{ data.file }}</code>
                                                                </small>
                                                            {% endif %}
                                                            {% if data.required_modules is defined %}
                                                                <div class="mt-2">
                                                                    <small class="text-muted">
                                                                        <i class="fa-icon me-1 fa-icon--nav-puzzle" aria-hidden="true"></i>
                                                                        {{ 'setup.step10.modules'|trans({}, 'setup') }}:
                                                                        {% for module in data.required_modules %}
                                                                            {% include '_components/_badge.html.twig' with { variant: 'secondary', content: module } %}
                                                                        {% endfor %}
                                                                    </small>
                                                                </div>
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                                </label>
                                            </div>
                                        {% endblock %}
                                    {% endembed %}
                                {% endfor %}

                                <div class="d-flex justify-content-between mt-4">
                                    <a href="{{ path('setup_step9_base_data') }}" class="btn btn-outline-secondary">
                                        <i class="fa-icon me-2 fa-icon--util-arrow-left" aria-hidden="true"></i>
                                        {{ 'setup.navigation.back'|trans({}, 'setup') }}
                                    </a>
                                    <div>
                                        <button type="button" class="btn btn-outline-secondary me-2"
                                                data-controller="ui-actions"
                                                data-action="click->ui-actions#submitForm"
                                                data-ui-actions-form-id-param="skip-form">
                                            <i class="fa-icon me-2 fa-icon--util-arrow-right" aria-hidden="true"></i>
                                            {{ 'setup.common.skip'|trans({}, 'setup') }}
                                        </button>
                                        <button type="submit" class="btn btn-primary">
                                            <i class="fa-icon me-2 fa-icon--nav-download" aria-hidden="true"></i>
                                            {{ 'setup.sample_data.import_selected'|trans({}, 'setup') }}
                                        </button>
                                    </div>
                                </div>
                            </form>

                            <form id="skip-form" method="post" action="{{ path('setup_step10_sample_data_skip') }}" class="d-none">
                                <input type="hidden" name="_token" value="{{ csrf_token('setup_sample_skip') }}">
                            </form>
                        {% else %}
                            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', icon: 'status-info' } } %}
                                {% block alert_body %}{{ 'setup.sample_data.no_data_available'|trans({}, 'setup') }}{% endblock %}
                            {% endembed %}

                            <div class="d-flex justify-content-between mt-4">
                                <a href="{{ path('setup_step9_base_data') }}" class="btn btn-outline-secondary">
                                    <i class="fa-icon me-2 fa-icon--util-arrow-left" aria-hidden="true"></i>
                                    {{ 'setup.navigation.back'|trans({}, 'setup') }}
                                </a>
                                <a href="{{ path('setup_step11_complete') }}" class="btn btn-primary">
                                    {{ 'setup.step10.next_button'|trans({}, 'setup') }}
                                    <i class="fa-icon ms-2 fa-icon--util-arrow-right" aria-hidden="true"></i>
                                </a>
                            </div>
                        {% endif %}
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}
