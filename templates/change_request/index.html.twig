{% extends 'base.html.twig' %}
{% import '_components/_fa_alva_hint.html.twig' as _fa_alva_hint %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% trans_default_domain 'change_requests' %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}

{% block title %}{{ 'change_request.title'|trans({}, 'change_requests') }}{% endblock %}

{% block body %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
<div class="container">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.change_requests'|trans({}, 'nav') }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'util-refresh', label: 'change_request.title'|trans({}, 'change_requests') },
        title: 'change_request.page.title'|trans({}, 'change_requests'),
        actions: [
            { label: 'change_request.action.create'|trans({}, 'change_requests'), href: path('app_change_request_new'), variant: 'primary', icon: 'plus' }
        ]
    }) }}
{% set _global_hints = alva_hints('change_request_index') %}
{% if _global_hints|length > 0 %}
    {% for _h in _global_hints %}
        {{ _fa_alva_hint.render(_h) }}
    {% endfor %}
{% endif %}

    <main class="fa-aurora-surface">

    {% if statistics %}
    <div class="row mb-4">
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'change_request.statistics.total'|trans({}, 'change_requests'), value: statistics.total, variant: 'primary' }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'change_request.statistics.pending_approval'|trans({}, 'change_requests'), value: statistics.pending_approval, variant: 'warning' }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'change_request.statistics.in_implementation'|trans({}, 'change_requests'), value: statistics.in_implementation, variant: 'info' }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({ label: 'change_request.statistics.overdue'|trans({}, 'change_requests'), value: statistics.overdue, variant: 'danger' }) }}
        </div>
    </div>
    {% endif %}

    {% if pending_approval|length > 0 %}
    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
        {% block alert_body %}
        <div class="fw-bold mb-2"><i class="fa-icon fa-icon--nav-list-check" aria-hidden="true"></i> {{ 'change_request.alert.pending_approval'|trans({}, 'change_requests') }}</div>
        <ul>
            {% for change in pending_approval %}
            <li>
                <a href="{{ path('app_change_request_show', {id: change.id}) }}">{{ change.changeNumber }} - {{ change.title }}</a>
                ({{ ('change_request.priority.' ~ change.priority)|trans({}, 'change_requests') }})
            </li>
            {% endfor %}
        </ul>
        {% endblock %}
    {% endembed %}
    {% endif %}

    {% if overdue|length > 0 %}
    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'danger' } } %}
        {% block alert_body %}
        <div class="fw-bold mb-2">{{ 'change_request.alert.overdue'|trans({}, 'change_requests') }}</div>
        <ul>
            {% for change in overdue %}
            <li>
                <a href="{{ path('app_change_request_show', {id: change.id}) }}">{{ change.changeNumber }} - {{ change.title }}</a>
            </li>
            {% endfor %}
        </ul>
        {% endblock %}
    {% endembed %}
    {% endif %}

    {% embed '_components/_card.html.twig' %}
        {% block card_attrs %}data-controller="bulk-actions" data-bulk-actions-endpoint-value="/{{ app.request.locale }}/change-request"{% endblock %}
        {% block card_header %}
            <div class="card-header">
                {%- set requestCount = change_requests|length -%}
                <h2>{{ 'change_request.list.title'|trans({'count': requestCount}, 'change_requests') }}</h2>
            </div>
        {% endblock %}
        {% block card_body %}
            {% import '_components/_fa_empty_state.html.twig' as _fa_empty_state %}
            {% if change_requests|length > 0 %}
            {% embed '_components/_table.html.twig' with {
                'wrapperClass': 'card-body',
                'noMargin': true
            } %}
                {% block table_head %}
                    <tr>
                        <th scope="col" class="bulk-select-column">
                            <input type="checkbox"
                                   class="form-check-input"
                                   data-action="bulk-actions#selectAll"
                                   data-bulk-actions-target="selectAllCheckbox"
                                   aria-label="{{ 'bulk_actions.select_all'|trans({}, 'messages') }}">
                        </th>
                        <th scope="col">{{ 'change_request.table.change_number'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.title'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.type'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.priority'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.status'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.complexity'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.requested'|trans({}, 'change_requests') }}</th>
                        <th scope="col">{{ 'change_request.table.actions'|trans({}, 'change_requests') }}</th>
                    </tr>
                {% endblock %}
                {% block table_body %}
                    {% for change in change_requests %}
                    <tr>
                        <td class="bulk-select-column">
                            <input type="checkbox"
                                   class="form-check-input"
                                   data-bulk-actions-target="item"
                                   data-action="bulk-actions#selectItem"
                                   value="{{ change.id }}"
                                   aria-label="{{ 'common.select'|trans({}, 'messages') }} {{ change.changeNumber }}">
                        </td>
                        <td><a href="{{ path('app_change_request_show', {id: change.id}) }}">{{ change.changeNumber }}</a></td>
                        <td>{{ change.title|slice(0, 40) }}...</td>
                        <td><span class="badge badge-{{ change.changeType }}">{{ ('change_request.change_type.' ~ change.changeType)|trans({}, 'change_requests') }}</span></td>
                        <td><span class="badge badge-{{ change.priority }}">{{ ('change_request.priority.' ~ change.priority)|trans({}, 'change_requests') }}</span></td>
                        <td><span class="{{ badge_status(change.status) }}">{{ ('change_request.status.' ~ change.status)|trans({}, 'change_requests') }}</span></td>
                        <td>{{ change.getComplexityScore() }}</td>
                        <td>{{ change.requestedDate|date('Y-m-d') }}</td>
                        <td>
                            <a href="{{ path('app_change_request_show', {id: change.id}) }}" class="fa-cyber-btn fa-cyber-btn--sm fa-cyber-btn--ghost">{{ 'change_request.action.view'|trans({}, 'change_requests') }}</a>
                            <a href="{{ path('app_change_request_edit', {id: change.id}) }}" class="fa-cyber-btn fa-cyber-btn--sm fa-cyber-btn--primary">{{ 'change_request.action.edit'|trans({}, 'change_requests') }}</a>
                        </td>
                    </tr>
                    {% endfor %}
                {% endblock %}
            {% endembed %}

            {# Bulk Action Bar #}
            {% include '_components/_bulk_action_bar.html.twig' with {
                actions: ['export', 'delete']
            } %}
            {% else %}
            {{ _fa_empty_state.render({
                mood: 'sleeping',
                title: 'change_request.empty.message'|trans({}, 'change_requests'),
                actions: [
                    { label: 'change_request.empty.create_first'|trans({}, 'change_requests'), variant: 'primary', icon: 'plus', href: path('app_change_request_new') }
                ]
            }) }}
            {% endif %}
        {% endblock %}
    {% endembed %}
    </main>
</div>

<style>
.stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem; margin-top: 1rem; }
.stat-item { display: flex; flex-direction: column; padding: 1rem; background: var(--surface-2); border-radius: 4px; }
.stat-label { font-size: 0.875rem; color: var(--fg-3); }
.stat-value { font-size: 1.5rem; font-weight: bold; color: var(--fg); }
.stat-value.warning { color: var(--warning); }
.stat-value.danger { color: var(--danger); }
</style>
{% endblock %}
