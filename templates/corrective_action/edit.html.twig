{% extends 'base.html.twig' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'corrective_action.page.edit'|trans({}, 'audits') }}: {{ action.title }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    {{ _fa_page_header.render({
        title: ('corrective_action.page.edit'|trans({}, 'audits')) ~ ': ' ~ action.title,
        badge: { icon: 'ui-edit', label: '' }
    }) }}
    {% include 'corrective_action/_form.html.twig' with {form: form} %}
</div>
{% endblock %}
