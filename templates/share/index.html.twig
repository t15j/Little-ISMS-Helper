{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}{{ 'share.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{{ _fa_page_header.render({
    badge: { icon: 'ui-send', label: 'share.title'|trans({}, 'messages') },
    title: 'share.title'|trans({}, 'messages')
}) }}

<main class="fa-aurora-surface">
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="h5 mb-0">
                            <i class="fa-icon fa-icon--delegate me-2" aria-hidden="true"></i>
                            {{ 'share.title'|trans({}, 'messages') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    {# Shared Content Preview #}
                    <div class="mb-4">
                        <h2 class="h6 mb-3">{{ 'share.content_preview'|trans({}, 'messages') }}</h2>

                        {% if title %}
                        <div class="mb-2">
                            <strong>{{ 'share.field.title'|trans({}, 'messages') }}:</strong>
                            <p class="mb-0">{{ title }}</p>
                        </div>
                        {% endif %}

                        {% if text %}
                        <div class="mb-2">
                            <strong>{{ 'share.field.text'|trans({}, 'messages') }}:</strong>
                            <p class="mb-0 text-break">{{ text|nl2br }}</p>
                        </div>
                        {% endif %}

                        {% if url %}
                        <div class="mb-2">
                            <strong>{{ 'share.field.url'|trans({}, 'messages') }}:</strong>
                            <p class="mb-0">
                                <a href="{{ url }}" target="_blank" rel="noopener" class="text-break">
                                    {{ url }}
                                    <i class="fa-icon fa-icon--util-external-link ms-1" aria-hidden="true"></i>
                                </a>
                            </p>
                        </div>
                        {% endif %}

                        {% if files|length > 0 %}
                        <div class="mb-2">
                            <strong>{{ 'share.field.files'|trans({}, 'messages') }}:</strong>
                            <ul class="list-unstyled mb-0">
                                {% for file in files %}
                                <li>
                                    <i class="fa-icon me-1 fa-icon--nav-document" aria-hidden="true"></i>
                                    {{ file.name }}
                                    <small class="text-muted">({{ (file.size / 1024)|number_format(1) }} KB)</small>
                                </li>
                                {% endfor %}
                            </ul>
                        </div>
                        {% endif %}

                        {% if not title and not text and not url and files|length == 0 %}
                        {{ _fa_alert.render({ variant: 'info', body: 'share.no_content'|trans({}, 'messages') }) }}
                        {% endif %}
                    </div>

                    <hr>

                    {# Action Selection #}
                    <h2 class="h6 mb-3">{{ 'share.select_action'|trans({}, 'messages') }}</h2>

                    <form action="{{ path('app_share_process') }}" method="post">
                        <input type="hidden" name="_token" value="{{ csrf_token('share_process') }}">
                        <input type="hidden" name="title" value="{{ title }}">
                        <input type="hidden" name="text" value="{{ text }}">
                        <input type="hidden" name="url" value="{{ url }}">
                        {% for file in files %}
                        <input type="hidden" name="file_ids[]" value="{{ file.path }}">
                        {% endfor %}

                        <div class="d-grid gap-2">
                            {% for action in shareContext.actions %}
                            <button type="submit"
                                    name="action"
                                    value="{{ action.id }}"
                                    class="fa-cyber-btn fa-cyber-btn--lg {{ action.highlight|default(false) ? 'fa-cyber-btn--primary' : 'fa-cyber-btn--ghost' }} text-start">
                                <div class="d-flex align-items-center">
                                    {# Auto-strip legacy bi- prefix so DB/YAML values keep working. #}
                                    <i class="fa-icon fa-icon--{{ (action.icon starts with 'bi-') ? action.icon[3:] : action.icon }} fs-4 me-3" aria-hidden="true"></i>
                                    <div>
                                        <strong>{{ action.label|trans({}, 'messages') }}</strong>
                                        <br>
                                        <small class="text-muted">{{ action.description|trans({}, 'messages') }}</small>
                                    </div>
                                    {% if action.id == shareContext.suggestedAction %}
                                    {% include '_components/_badge.html.twig' with { variant: 'success', class: 'ms-auto', content: 'share.suggested'|trans({}, 'messages') } %}
                                    {% endif %}
                                </div>
                            </button>
                            {% endfor %}
                        </div>
                    </form>
                {% endblock %}
                {% block card_footer %}
                    <div class="card-footer">
                        <a href="{{ path('app_dashboard', {'_locale': app.request.locale}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--status-critical me-1" aria-hidden="true"></i>
                            {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
</main>
{% endblock %}
