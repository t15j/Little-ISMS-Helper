{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block admin_title %}{{ permission.name }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'permission.show'|trans({}, 'messages') }}</p>
{% endblock %}

{% block breadcrumb_items %}
    <li class="breadcrumb-item"><a href="{{ path('permission_index') }}">{{ 'admin.nav.permissions'|trans({}, 'admin') }}</a></li>
    <li class="breadcrumb-item active" aria-current="page">{{ permission.name }}</li>
{% endblock %}

{% block admin_content %}
    {{ _fa_page_header.render({
        badge: { icon: 'nav-shield-lock', label: 'admin.nav.permissions'|trans({}, 'admin') },
        title: permission.name,
        subtitle: 'permission.show'|trans({}, 'messages'),
        actions: [
            { label: 'common.back'|trans({}, 'messages'), variant: 'ghost', icon: 'util-arrow-left', href: path('permission_index') }
        ]
    }) }}

    {# Permission Details #}
    {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i> Berechtigungsdetails
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="row mb-3">
                <div class="col-md-3"><strong>Name:</strong></div>
                <div class="col-md-9"><code>{{ permission.name }}</code></div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>{{ 'permission.category'|trans({}, 'messages') }}:</strong></div>
                <div class="col-md-9">
                    {% include '_components/_badge.html.twig' with { variant: 'primary', content: permission.category|title } %}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>{{ 'permission.action'|trans({}, 'messages') }}:</strong></div>
                <div class="col-md-9">
                    {% include '_components/_badge.html.twig' with { variant: 'info', content: permission.action|title } %}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>{{ 'permission.description'|trans({}, 'messages') }}:</strong></div>
                <div class="col-md-9">{{ permission.description }}</div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>Typ:</strong></div>
                <div class="col-md-9">
                    {% if permission.isSystemPermission %}
                        {% include '_components/_badge.html.twig' with { variant: 'success', content: 'System-Berechtigung' } %}
                    {% else %}
                        {% include '_components/_badge.html.twig' with { variant: 'secondary', content: 'Benutzerdefiniert' } %}
                    {% endif %}
                </div>
            </div>
            <div class="row">
                <div class="col-md-3"><strong>Erstellt am:</strong></div>
                <div class="col-md-9">{{ permission.createdAt|date('d.m.Y H:i') }}</div>
            </div>
        {% endblock %}
    {% endembed %}

    {# Roles using this permission #}
    {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fa-icon fa-icon--nav-team" aria-hidden="true"></i> {{ 'permission.roles_using'|trans({}, 'messages') }}
                    {% include '_components/_badge.html.twig' with { variant: 'light', content: roles|length } %}
                </h2>
            </div>
        {% endblock %}
        {% block card_body %}
            {% import '_components/_fa_alert.html.twig' as _fa_alert %}
            {% if roles|length > 0 %}
                {% embed '_components/_table.html.twig' with {
                    'class': 'table-hover',
                    'noMargin': true,
                    'responsive': true
                } %}
                    {% block table_head %}
                        <tr>
                            <th scope="col">Rollenname</th>
                            <th scope="col">Beschreibung</th>
                            <th scope="col">Benutzer</th>
                            <th scope="col" class="text-end">Aktion</th>
                        </tr>
                    {% endblock %}
                    {% block table_body %}
                        {% for role in roles %}
                            <tr>
                                <td>
                                    <strong>{{ role.name }}</strong>
                                    {% if role.isSystemRole %}
                                        {% include '_components/_badge.html.twig' with { variant: 'warning', content: 'System' } %}
                                    {% endif %}
                                </td>
                                <td>{{ role.description }}</td>
                                <td>
                                    {% include '_components/_badge.html.twig' with { variant: 'secondary', content: role.users|length } %}
                                </td>
                                <td class="text-end">
                                    <a href="{{ path('role_management_show', {id: role.id}) }}"
                                       class="fa-cyber-btn fa-cyber-btn--sm fa-cyber-btn--ghost">
                                        <i class="fa-icon fa-icon--ui-eye" aria-hidden="true"></i> Details
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                    {% endblock %}
                {% endembed %}

                {{ _fa_alert.render({ variant: 'info', class: 'mt-3', body: '<strong>' ~ 'permission.affected_users'|trans({}, 'messages') ~ ':</strong> Diese Berechtigung betrifft insgesamt ' ~ affected_users_count ~ ' Benutzer über ' ~ roles|length ~ ' Rolle(n).' }) }}
            {% else %}
                {{ _fa_alert.render({ variant: 'warning', body: 'Diese Berechtigung wird derzeit von keiner Rolle verwendet.' }) }}
            {% endif %}
        {% endblock %}
    {% endembed %}
{% endblock %}
