{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% trans_default_domain 'audit' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'audit.edit'|trans({}, 'audit') }} - {{ audit.title }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-lg-10 col-xl-9 mx-auto">
            <nav aria-label="breadcrumb" class="mb-3">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_audit_index') }}" data-turbo-action="advance">
                            <i class="fa-icon fa-icon--sop" aria-hidden="true"></i> {{ 'audit_management.title'|trans({}, 'audit') }}
                        </a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_audit_show', {id: audit.id}) }}" data-turbo-action="advance">
                            {{ audit.title }}
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">{{ 'common.edit'|trans({}, 'messages') }}</li>
                </ol>
            </nav>

            {{ _fa_page_header.render({
    title: ('audit.edit'|trans({}, 'audit')) ~ ': ' ~ (audit.title),
    badge: { icon: 'ui-edit', label: '' },
    actions: [
        { label: 'common.view'|trans({}, 'messages'), href: path('app_audit_show', {id: audit.id}), variant: 'ghost', icon: 'util-arrow-left' }
    ]
}) }}

            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', icon: 'status-info', class: 'mb-4' } } %}
                {% block alert_body %}<div class="fw-bold mb-2"> <i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> {{ 'audit.iso_27001_requirement'|trans({}, 'audit') }} </div> <p class="mb-0 small"> {{ 'audit.iso_27001_clause_info'|trans({}, 'audit') }} </p>{% endblock %}
            {% endembed %}

            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0"><i class="fa-icon fa-icon--ui-edit" aria-hidden="true"></i> {{ 'audit.edit'|trans({}, 'audit') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    {# C5-04 — `conditional-fields` controller toggles the scoped-assets row
                       when scopeType=asset (ISO 19011 Cl. 5.5.2). #}
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'id': 'audit-form', 'data-controller': 'conditional-fields'}}) }}

                    {# Basic Information Section #}
                    <fieldset class="mb-4">
                        <legend class="h3 mb-3">
                            <i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i>
                            {{ 'audit.section.basic_info'|trans({}, 'audit') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.title,
                                    'label': 'audit.field.title'|trans({}, 'audit'),
                                    'required': true
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.scope,
                                    'label': 'audit.field.scope'|trans({}, 'audit')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.scopeType,
                                    'label': 'audit.field.scope_type'|trans({}, 'audit')
                                } %}
                            </div>
                            <div class="col-md-12 d-none" id="subsidiaries-container">
                                {{ _fa_alert.render({ variant: 'info', icon: 'status-info', body: '<strong>Konzernaudit:</strong> Wählen Sie die Tochtergesellschaften aus, die in dieses Audit einbezogen werden sollen. Bei "Konzernweites Audit" sollten alle relevanten Tochtergesellschaften ausgewählt werden.' }) }}
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditedSubsidiaries,
                                    'label': 'audit.field.audited_subsidiaries'|trans({}, 'audit')
                                } %}
                            </div>
                            {# C5-04 — scopedAssets row, toggled by conditional-fields controller when scopeType=asset (ISO 19011 Cl. 5.5.2). #}
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.scopedAssets,
                                    'label_key': form.scopedAssets.vars.label,
                                    'translation_domain': 'audit'
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {% include '_components/_auto_form.html.twig' with {
                        'form': form,
                        'sections': {
                            'schedule': {
                                'label': 'audit.section.schedule'|trans({}, 'audit'),
                                'fields': ['plannedDate', 'actualDate']
                            },
                            'team': {
                                'label': 'audit.section.team'|trans({}, 'audit'),
                                'fields': ['leadAuditor', 'auditTeam']
                            },
                            'status': {
                                'label': 'audit.section.status'|trans({}, 'audit'),
                                'fields': ['status']
                            },
                            'objectives': {
                                'label': 'audit.section.objectives'|trans({}, 'audit'),
                                'fields': ['objectives']
                            },
                            'findings': {
                                'label': 'audit.section.findings'|trans({}, 'audit'),
                                'fields': ['findings', 'recommendations', 'conclusion']
                            }
                        },
                        'col_widths': {
                            'plannedDate': 6,
                            'actualDate': 6,
                            'leadAuditor': 6,
                            'status': 6
                        },
                        'exclude': ['title', 'scope', 'scopeType', 'scopedFramework', 'auditedSubsidiaries', 'scopedAssets']
                    } %}

                    {# Form Actions #}
                    <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                        <div>
                            <a href="{{ path('app_audit_show', {id: audit.id}) }}"
                               class="fa-cyber-btn fa-cyber-btn--ghost"
                               data-turbo-action="advance">
                                <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i> {{ 'common.back'|trans({}, 'messages') }}
                            </a>
                            <a href="{{ path('app_audit_index') }}"
                               class="fa-cyber-btn fa-cyber-btn--ghost"
                               data-turbo-action="advance">
                                <i class="fa-icon fa-icon--ui-list" aria-hidden="true"></i> {{ 'common.to_list'|trans({}, 'messages') }}
                            </a>
                        </div>
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary fa-cyber-btn--lg">
                            <i class="fa-icon fa-icon--ui-save" aria-hidden="true"></i> {{ 'common.save_changes'|trans({}, 'messages') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                {% endblock %}
            {% endembed %}

            {# Metadata Card #}
            {% embed '_components/_card.html.twig' with {class: 'mt-4'} %}
                {% block card_header %}
                    <div class="card-header bg-body-secondary">
                        <h2 class="h6 mb-0"><i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> {{ 'common.metadata'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="row mb-0">
                        <dt class="col-sm-3 text-muted">{{ 'common.created_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-9">{{ audit.createdAt|date('d.m.Y H:i') }}</dd>

                        <dt class="col-sm-3 text-muted">{{ 'common.updated_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-9">{{ audit.updatedAt|date('d.m.Y H:i') }}</dd>

                        <dt class="col-sm-3 text-muted">{{ 'common.id'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-9"><code>{{ audit.id }}</code></dd>
                    </dl>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>

<style>
    .form-check-input {
        cursor: pointer;
    }
    .form-check-label {
        cursor: pointer;
    }
    select[multiple] {
        min-height: 150px;
    }
</style>

<script>
// IIFE: Turbo re-eval guard — prevents re-declaration errors on body-swap
(function () {
function initAuditEditSubsidiaries() {
    const scopeTypeSelect = document.querySelector('[data-corporate-scope]');
    const subsidiariesContainer = document.getElementById('subsidiaries-container');
    const subsidiariesSelect = document.querySelector('[data-corporate-subsidiaries]');

    if (!scopeTypeSelect) {
        return; // Exit if scope type field not found
    }

    function toggleSubsidiariesField() {
        const selectedScope = scopeTypeSelect.value;
        const isCorporateScope = selectedScope === 'corporate_wide' || selectedScope === 'corporate_subsidiaries';

        if (isCorporateScope) {
            subsidiariesContainer.style.display = 'block';

            // Auto-select all for corporate_wide
            if (selectedScope === 'corporate_wide' && subsidiariesSelect) {
                for (let option of subsidiariesSelect.options) {
                    option.selected = true;
                }
            }
        } else {
            subsidiariesContainer.style.display = 'none';
            // Deselect all when hiding
            if (subsidiariesSelect) {
                for (let option of subsidiariesSelect.options) {
                    option.selected = false;
                }
            }
        }
    }

    // Run on page load to show field if already corporate scope
    toggleSubsidiariesField();

    // Run on scope type change
    scopeTypeSelect.addEventListener('change', toggleSubsidiariesField);
}

document.addEventListener('DOMContentLoaded', initAuditEditSubsidiaries);
document.addEventListener('turbo:load', initAuditEditSubsidiaries);
})();
</script>
{% endblock %}
