{# Dashboard Customization Settings Modal — Aurora fa-modal shell (2026-05-21) #}

<div class="fa-modal fa-modal--settings"
     id="dashboardSettingsModal"
     data-controller="fa-modal"
     data-dashboard-customizer-target="settingsModal"
     data-fa-modal-mode-value="settings"
     role="dialog"
     aria-modal="true"
     aria-labelledby="dashboardSettingsModalLabel"
     inert>

    <div class="fa-modal__backdrop"
         data-fa-modal-target="backdrop"
         data-action="click->fa-modal#backdropClick"
         aria-hidden="true"></div>

    <div class="fa-modal__container" data-fa-modal-target="dialog">

        <header class="fa-modal__header">
            <span class="fa-modal__icon" aria-hidden="true">
                <i class="fa-icon fa-icon--nav-sliders"></i>
            </span>
            <div class="fa-modal__heading">
                <h2 class="fa-modal__title" id="dashboardSettingsModalLabel">{{ 'dashboard.settings.title'|trans({}, 'dashboard') }}</h2>
                <p class="fa-modal__sub">{{ 'dashboard.settings.subtitle'|trans({}, 'dashboard') }}</p>
            </div>
            <button type="button"
                    class="fa-modal__close"
                    aria-label="{{ 'action.close'|trans({}, 'messages') }}"
                    data-action="fa-modal#cancel">
                <i class="fa-icon fa-icon--ui-close" aria-hidden="true"></i>
            </button>
        </header>

        <div class="fa-modal__body">
            <p class="text-muted mb-4">
                {{ 'dashboard.settings.intro'|trans({}, 'dashboard') }}
            </p>

            <div class="widget-toggles">
                <h6 class="mb-3">{{ 'dashboard.settings.group.stats'|trans({}, 'dashboard') }}</h6>
                <div class="form-check form-switch mb-3" data-widget-toggle="stats-cards">
                    <input class="form-check-input" type="checkbox" id="widget-toggle-stats-cards"
                           data-action="change->dashboard-customizer#toggleWidget"
                           checked>
                    <label class="form-check-label" for="widget-toggle-stats-cards">
                        <i class="fa-icon fa-icon--nav-bar-chart me-2 text-primary" aria-hidden="true"></i>
                        {{ 'dashboard.settings.widget.stats_cards'|trans({}, 'dashboard') }}
                    </label>
                </div>

                <h6 class="mb-3 mt-4">{{ 'dashboard.settings.group.charts'|trans({}, 'dashboard') }}</h6>
                <div class="form-check form-switch mb-3" data-widget-toggle="risk-chart">
                    <input class="form-check-input" type="checkbox" id="widget-toggle-risk-chart"
                           data-action="change->dashboard-customizer#toggleWidget"
                           checked>
                    <label class="form-check-label" for="widget-toggle-risk-chart">
                        <i class="fa-icon fa-icon--nav-pie-chart me-2 text-warning" aria-hidden="true"></i>
                        {{ 'dashboard.settings.widget.risk_chart'|trans({}, 'dashboard') }}
                    </label>
                </div>

                <div class="form-check form-switch mb-3" data-widget-toggle="asset-chart">
                    <input class="form-check-input" type="checkbox" id="widget-toggle-asset-chart"
                           data-action="change->dashboard-customizer#toggleWidget"
                           checked>
                    <label class="form-check-label" for="widget-toggle-asset-chart">
                        <i class="fa-icon fa-icon--nav-bar-chart me-2 text-info" aria-hidden="true"></i>
                        {{ 'dashboard.settings.widget.asset_chart'|trans({}, 'dashboard') }}
                    </label>
                </div>

                <h6 class="mb-3 mt-4">{{ 'dashboard.settings.group.widgets'|trans({}, 'dashboard') }}</h6>
                <div class="form-check form-switch mb-3" data-widget-toggle="activity-feed">
                    <input class="form-check-input" type="checkbox" id="widget-toggle-activity-feed"
                           data-action="change->dashboard-customizer#toggleWidget"
                           checked>
                    <label class="form-check-label" for="widget-toggle-activity-feed">
                        <i class="fa-icon fa-icon--nav-activity me-2 text-success" aria-hidden="true"></i>
                        {{ 'dashboard.settings.widget.activity_feed'|trans({}, 'dashboard') }}
                    </label>
                </div>

                <div class="form-check form-switch mb-3" data-widget-toggle="quick-actions">
                    <input class="form-check-input" type="checkbox" id="widget-toggle-quick-actions"
                           data-action="change->dashboard-customizer#toggleWidget"
                           checked>
                    <label class="form-check-label" for="widget-toggle-quick-actions">
                        <i class="fa-icon me-2 text-warning fa-icon--threat" aria-hidden="true"></i>
                        {{ 'dashboard.settings.widget.quick_actions'|trans({}, 'dashboard') }}
                    </label>
                </div>
            </div>
        </div>

        <footer class="fa-modal__footer">
            <button type="button"
                    class="fa-cyber-btn fa-cyber-btn--danger me-auto"
                    data-action="click->dashboard-customizer#resetToDefaults">
                <i class="fa-icon fa-icon--ui-refresh" aria-hidden="true"></i> {{ 'common.reset'|trans({}, 'messages') }}
            </button>
            <button type="button"
                    class="fa-cyber-btn fa-cyber-btn--ghost"
                    data-action="fa-modal#cancel">
                {{ 'common.close'|trans({}, 'messages') }}
            </button>
        </footer>
    </div>
</div>

<style>
.widget-toggles .form-check-label {
    cursor: pointer;
    display: flex;
    align-items: center;
    font-weight: 500;
}

.widget-toggles .form-check-input {
    cursor: pointer;
}

.widget-toggles h6 {
    color: var(--color-text-muted);
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-weight: 600;
}
</style>
