{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}{{ 'mfa.backup_codes.title'|trans({}, 'mfa') }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>
            <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
            {{ 'mfa.backup_codes.title'|trans({}, 'mfa') }}
        </h1>
        <a href="{{ path('app_profile_mfa_show', {id: mfa_token.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i>
            {{ 'common.back'|trans({}, 'messages') }}
        </a>
    </div>

    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'danger' } } %}
                {% block alert_body %}
                <div class="fa-alert__title">
                    <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
                    {{ 'mfa.backup_codes.warning'|trans({}, 'mfa') }}
                </div>
                <p>{{ 'mfa.backup_codes.warning_text'|trans({}, 'mfa') }}</p>
                <hr>
                <p class="mb-0">
                    <strong>{{ 'mfa.backup_codes.onetime_display'|trans({}, 'mfa') }}</strong>
                </p>
                {% endblock %}
            {% endembed %}

            {% embed '_components/_card.html.twig' with { 'class': 'border-warning mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <div class="d-flex justify-content-between align-items-center">
                            <h2 class="mb-0 text-warning">{{ 'mfa.backup_codes.codes'|trans({}, 'mfa') }}</h2>
                            {% include '_components/_badge.html.twig' with { variant: 'dark', content: backup_codes|length ~ ' ' ~ 'mfa.backup_codes.codes_count'|trans({}, 'mfa') } %}
                        </div>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
                        {% block alert_body %}
                        <strong><i class="fa-icon fa-icon--ui-person" aria-hidden="true"></i> {{ 'user.field.email'|trans({}, 'user') }}:</strong> {{ mfa_token.user.email }}<br>
                        <strong><i class="fa-icon fa-icon--asset-endpoint" aria-hidden="true"></i> {{ 'mfa.field.device_name'|trans({}, 'mfa') }}:</strong> {{ mfa_token.deviceName }}<br>
                        <strong><i class="fa-icon fa-icon--ui-calendar" aria-hidden="true"></i> {{ 'common.generated'|trans({}, 'messages') }}:</strong> {{ 'now'|date('d.m.Y H:i:s') }}
                        {% endblock %}
                    {% endembed %}

                    <div data-controller="backup-codes"
                         data-backup-codes-codes-value="{{ backup_codes|json_encode }}">
                        <div class="row" data-backup-codes-target="codesContainer">
                            {% for code in backup_codes %}
                                <div class="col-md-6 mb-2">
                                    <div class="bg-body-secondary p-3 rounded border">
                                        {% include '_components/_badge.html.twig' with { variant: 'secondary', content: loop.index, class: 'me-2' } %}
                                        <span class="font-monospace fw-bold">{{ code }}</span>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <button type="button"
                                    class="fa-cyber-btn fa-cyber-btn--primary"
                                    data-action="backup-codes#copy">
                                <i class="fa-icon fa-icon--ui-copy" aria-hidden="true"></i>
                                {{ 'mfa.backup_codes.copy_all'|trans({}, 'mfa') }}
                            </button>
                            <button type="button"
                                    class="fa-cyber-btn fa-cyber-btn--ghost"
                                    data-action="backup-codes#print">
                                <i class="fa-icon fa-icon--ui-printer" aria-hidden="true"></i>
                                {{ 'mfa.backup_codes.print'|trans({}, 'mfa') }}
                            </button>
                            <button type="button"
                                    class="fa-cyber-btn fa-cyber-btn--ghost"
                                    data-action="backup-codes#download">
                                <i class="fa-icon fa-icon--nav-download" aria-hidden="true"></i>
                                {{ 'mfa.backup_codes.download'|trans({}, 'mfa') }}
                            </button>
                        </div>
                    </div>
                {% endblock %}
            {% endembed %}

            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    <p class="fw-bold">
                        <i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i>
                        {{ 'mfa.backup_codes.usage_title'|trans({}, 'mfa') }}
                    </p>
                    <ul>
                        <li>{{ 'mfa.backup_codes.usage_1'|trans({}, 'mfa') }}</li>
                        <li>{{ 'mfa.backup_codes.usage_2'|trans({}, 'mfa') }}</li>
                        <li>{{ 'mfa.backup_codes.usage_3'|trans({}, 'mfa') }}</li>
                        <li>{{ 'mfa.backup_codes.usage_4'|trans({}, 'mfa') }}</li>
                    </ul>

                    {{ _fa_alert.render({ variant: 'warning', body: 'mfa.backup_codes.regenerate_warning'|trans({}, 'mfa') }) }}
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
    // Turbo re-eval guard: IIFE prevents re-declare crash on body-swap navigation
    (function () {
    const backupCodes = {{ backup_codes|json_encode|raw }};
    const userEmail = '{{ mfa_token.user.email }}';

    function copyBackupCodes() {
        const text = backupCodes.join('\n');

        navigator.clipboard.writeText(text).then(() => {
            const btn = event.target.closest('button');
            const originalHTML = btn.innerHTML;
            btn.innerHTML = '<i class="fa-icon fa-icon--ui-check" aria-hidden="true"></i> {{ 'common.copied'|trans({}, 'messages') }}';
            btn.classList.remove('btn-primary');
            btn.classList.add('btn-success');

            setTimeout(() => {
                btn.innerHTML = originalHTML;
                btn.classList.remove('btn-success');
                btn.classList.add('btn-primary');
            }, 2000);
        }).catch(err => {
            window.faToast('{{ 'common.copy_failed'|trans({}, 'messages') }}: ' + err, 'danger');
        });
    }

    function printBackupCodes() {
        const printWindow = window.open('', '', 'width=600,height=800');

        printWindow.document.write(`
            <html>
            <head>
                <title>{{ 'mfa.backup_codes.title'|trans({}, 'mfa') }} - ${userEmail}</title>
                <style>
                    body {
                        font-family: Arial, sans-serif;
                        padding: 20px;
                        max-width: 800px;
                        margin: 0 auto;
                    }
                    h1 {
                        font-size: 24px;
                        border-bottom: 2px solid #333;
                        padding-bottom: 10px;
                    }
                    .info {
                        background: #f8f9fa;
                        padding: 15px;
                        border-left: 4px solid #007bff;
                        margin: 20px 0;
                    }
                    .warning {
                        background: #fff3cd;
                        padding: 15px;
                        border: 2px solid var(--warning);
                        margin: 20px 0;
                    }
                    .codes {
                        display: grid;
                        grid-template-columns: 1fr 1fr;
                        gap: 10px;
                        margin: 20px 0;
                    }
                    .code-item {
                        font-family: monospace;
                        font-size: 18px;
                        padding: 10px;
                        background: #f8f9fa;
                        border: 1px solid var(--border);
                        border-radius: 4px;
                    }
                    .code-number {
                        display: inline-block;
                        background: var(--fg-3);
                        color: white;
                        padding: 2px 8px;
                        border-radius: 3px;
                        font-size: 12px;
                        margin-right: 10px;
                    }
                    @media print {
                        body { padding: 0; }
                        .no-print { display: none; }
                    }
                </style>
            </head>
            <body>
                <h1>{{ 'mfa.backup_codes.title'|trans({}, 'mfa') }}</h1>

                <div class="info">
                    <p><strong>{{ 'user.field.email'|trans({}, 'user') }}:</strong> ${userEmail}</p>
                    <p><strong>{{ 'mfa.field.device_name'|trans({}, 'mfa') }}:</strong> {{ mfa_token.deviceName }}</p>
                    <p><strong>{{ 'common.generated'|trans({}, 'messages') }}:</strong> ${new Date().toLocaleString()}</p>
                </div>

                <div class="warning">
                    <strong>⚠️ {{ 'mfa.backup_codes.warning'|trans({}, 'mfa') }}</strong><br>
                    {{ 'mfa.backup_codes.warning_text'|trans({}, 'mfa') }}
                </div>

                <h2>{{ 'mfa.backup_codes.codes'|trans({}, 'mfa') }} (${backupCodes.length})</h2>
                <div class="codes">
                    ${backupCodes.map((code, i) =>
                        '<div class="code-item"><span class="code-number">' + (i+1) + '</span>' + code + '</div>'
                    ).join('')}
                </div>

                <div class="info">
                    <h2>{{ 'mfa.backup_codes.usage_title'|trans({}, 'mfa') }}</h2>
                    <ul>
                        <li>{{ 'mfa.backup_codes.usage_1'|trans({}, 'mfa') }}</li>
                        <li>{{ 'mfa.backup_codes.usage_2'|trans({}, 'mfa') }}</li>
                        <li>{{ 'mfa.backup_codes.usage_3'|trans({}, 'mfa') }}</li>
                        <li>{{ 'mfa.backup_codes.usage_4'|trans({}, 'mfa') }}</li>
                    </ul>
                </div>
            </body>
            </html>
        `);

        printWindow.document.close();
        printWindow.focus();
        setTimeout(() => printWindow.print(), 250);
    }

    function downloadBackupCodes() {
        const timestamp = new Date().toISOString().replace(/[:.]/g, '-').slice(0, -5);
        const filename = `mfa-backup-codes-${userEmail}-${timestamp}.txt`;

        let content = '='.repeat(60) + '\n';
        content += 'MFA BACKUP CODES\n';
        content += '='.repeat(60) + '\n\n';
        content += 'User: ' + userEmail + '\n';
        content += 'Device: {{ mfa_token.deviceName }}\n';
        content += 'Generated: ' + new Date().toLocaleString() + '\n\n';
        content += '⚠️  WARNING: Keep these codes secure!\n';
        content += 'These codes can be used to access your account if you lose\n';
        content += 'access to your authenticator device. Each code can only be\n';
        content += 'used once.\n\n';
        content += '='.repeat(60) + '\n';
        content += 'BACKUP CODES (' + backupCodes.length + ')\n';
        content += '='.repeat(60) + '\n\n';

        backupCodes.forEach((code, i) => {
            content += `${(i+1).toString().padStart(2, '0')}. ${code}\n`;
        });

        content += '\n' + '='.repeat(60) + '\n';
        content += 'USAGE INSTRUCTIONS\n';
        content += '='.repeat(60) + '\n';
        content += '1. Use backup codes when you cannot access your authenticator\n';
        content += '2. Each code can only be used once\n';
        content += '3. Generate new codes before running out\n';
        content += '4. Store these codes in a secure location\n';

        const blob = new Blob([content], { type: 'text/plain' });
        const url = window.URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        window.URL.revokeObjectURL(url);

        // Show success feedback
        const btn = event.target.closest('button');
        const originalHTML = btn.innerHTML;
        btn.innerHTML = '<i class="fa-icon fa-icon--ui-check" aria-hidden="true"></i> {{ 'common.downloaded'|trans({}, 'messages') }}';
        btn.classList.add('btn-success');
        btn.classList.remove('btn-outline-secondary');

        setTimeout(() => {
            btn.innerHTML = originalHTML;
            btn.classList.remove('btn-success');
            btn.classList.add('btn-outline-secondary');
        }, 2000);
    }
    })();
    </script>
{% endblock %}
