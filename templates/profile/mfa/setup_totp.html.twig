{% extends 'base.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}

{% block title %}{{ 'mfa.setup_totp.title'|trans({}, 'mfa') }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>
            <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
            {{ 'mfa.setup_totp.title'|trans({}, 'mfa') }}
        </h1>
        <a href="{{ path('app_profile_mfa_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
            <i class="fa-icon fa-icon--util-arrow-left" aria-hidden="true"></i>
            {{ 'common.back'|trans({}, 'messages') }}
        </a>
    </div>

    {{ _fa_alert.render({ variant: 'info', class: 'mb-4', body: 'mfa.setup_totp.subtitle'|trans({}, 'mfa') }) }}

    <div class="row">
        <div class="col-lg-8">
            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
                            {{ 'mfa.setup_totp.step1'|trans({}, 'mfa') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% import '_components/_fa_alert.html.twig' as _fa_alert %}
                    <p class="lead">{{ 'mfa.setup_totp.scan_instructions'|trans({}, 'mfa') }}</p>

                    <div class="text-center mb-4">
                        <img src="data:image/png;base64,{{ qr_code }}"
                             alt="{{ 'mfa.qr_code_alt'|trans({}, 'mfa') }}"
                             class="border rounded p-3 bg-body-secondary"
                             style="max-width: 300px;">
                    </div>

                    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
                        {% block alert_body %}
                        <div class="fa-alert__title">{{ 'mfa.setup_totp.manual_entry'|trans({}, 'mfa') }}</div>
                        <p class="mb-2">{{ 'mfa.setup_totp.manual_instructions'|trans({}, 'mfa') }}</p>
                        <label for="secret-key" class="form-label">{{ 'mfa.setup.secret_key'|trans({}, 'mfa') }}</label>
                        <div class="input-group" data-controller="mfa-setup">
                            <input type="text"
                                   class="form-control font-monospace fa-cyber-input__field"
                                   value="{{ secret }}"
                                   id="secret-key"
                                   readonly
                                   aria-label="{{ 'mfa.setup.secret_key'|trans({}, 'mfa') }}">
                            <button class="fa-cyber-btn fa-cyber-btn--ghost"
                                    type="button"
                                    data-action="mfa-setup#copySecret"
                                    data-mfa-setup-copy-target-param="secret-key"
                                    data-mfa-setup-copied-text-value="{{ 'common.copied'|trans({}, 'messages') }}">
                                <i class="fa-icon fa-icon--audit-trail" aria-hidden="true"></i>
                                {{ 'common.copy'|trans({}, 'messages') }}
                            </button>
                        </div>
                        {% endblock %}
                    {% endembed %}

                    {{ _fa_alert.render({ variant: 'info', body: '<strong><i class="fa-icon fa-icon--user" aria-hidden="true"></i> ' ~ 'user.field.email'|trans({}, 'user') ~ ':</strong> ' ~ user.email ~ '<br><strong><i class="fa-icon fa-icon--asset-endpoint" aria-hidden="true"></i> ' ~ 'mfa.field.device_name'|trans({}, 'mfa') ~ ':</strong> ' ~ mfa_token.deviceName }) }}
                {% endblock %}
            {% endembed %}

            {% embed '_components/_card.html.twig' with { 'class': 'mb-4' } %}
                {% block card_attrs %}
                    data-controller="mfa-setup"
                    data-mfa-setup-verify-url-value="{{ path('app_profile_mfa_verify', {id: mfa_token.id}) }}"
                    data-mfa-setup-success-redirect-value="{{ path('app_profile_mfa_show', {id: mfa_token.id}) }}"
                    data-mfa-setup-verifying-text-value="{{ 'common.verifying'|trans({}, 'messages') }}"
                    data-mfa-setup-verify-button-text-value="{{ 'mfa.setup_totp.verify_button'|trans({}, 'mfa') }}"
                    data-mfa-setup-error-text-value="{{ 'common.error'|trans({}, 'messages') }}"
                {% endblock %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                            {{ 'mfa.setup_totp.step2'|trans({}, 'mfa') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <p class="lead">{{ 'mfa.setup_totp.verify_instructions'|trans({}, 'mfa') }}</p>

                    <form data-action="submit->mfa-setup#verify">
                        <div class="mb-3">
                            <label for="verification-code" class="form-label">
                                {{ 'mfa.field.verification_code'|trans({}, 'mfa') }}
                            </label>
                            <input type="text"
                                   class="form-control form-control-lg text-center font-monospace fa-cyber-input__field"
                                   id="verification-code"
                                   name="code"
                                   maxlength="6"
                                   pattern="[0-9]{6}"
                                   placeholder="123456"
                                   required
                                   autocomplete="off"
                                   autofocus
                                   data-mfa-setup-target="codeInput"
                                   aria-label="{{ 'mfa.field.verification_code'|trans({}, 'mfa') }}">
                            <div class="form-text">{{ 'mfa.setup_totp.code_help'|trans({}, 'mfa') }}</div>
                        </div>

                        <div data-mfa-setup-target="feedback"></div>

                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--success fa-cyber-btn--lg w-100" data-mfa-setup-target="submitButton">
                            <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i>
                            {{ 'mfa.setup_totp.verify_button'|trans({}, 'mfa') }}
                        </button>
                    </form>
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-lg-4">
            {% embed '_components/_card.html.twig' with { 'class': 'border-warning' } %}
                {% block card_attrs %}
                    data-controller="backup-codes"
                    data-backup-codes-codes-value="{{ backup_codes|json_encode }}"
                    data-backup-codes-user-email-value="{{ user.email }}"
                    data-backup-codes-device-name-value="{{ mfa_token.deviceName }}"
                    data-backup-codes-title-text-value="{{ 'mfa.backup_codes.title'|trans({}, 'mfa') }}"
                    data-backup-codes-user-label-value="{{ 'user.field.email'|trans({}, 'user') }}"
                    data-backup-codes-device-label-value="{{ 'mfa.field.device_name'|trans({}, 'mfa') }}"
                    data-backup-codes-generated-label-value="{{ 'common.date'|trans({}, 'messages') }}"
                    data-backup-codes-warning-title-value="{{ 'mfa.backup_codes.warning'|trans({}, 'mfa') }}"
                    data-backup-codes-warning-text-value="{{ 'mfa.backup_codes.warning_text'|trans({}, 'mfa') }}"
                    data-backup-codes-codes-label-value="{{ 'mfa.backup_codes.codes'|trans({}, 'mfa') }}"
                    data-backup-codes-copied-text-value="{{ 'common.copied'|trans({}, 'messages') }}"
                {% endblock %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0">
                            <i class="fa-icon fa-icon--status-warning text-warning" aria-hidden="true"></i>
                            {{ 'mfa.backup_codes.title'|trans({}, 'mfa') }}
                        </h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'danger' } } %}
                        {% block alert_body %}
                        <div class="fa-alert__title">
                            <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i>
                            {{ 'mfa.backup_codes.warning'|trans({}, 'mfa') }}
                        </div>
                        <p class="mb-0 small">{{ 'mfa.backup_codes.warning_text'|trans({}, 'mfa') }}</p>
                        {% endblock %}
                    {% endembed %}

                    <p class="small">{{ 'mfa.backup_codes.description'|trans({}, 'mfa') }}</p>

                    <div class="bg-body-secondary p-3 rounded border font-monospace small" style="max-height: 300px; overflow-y: auto;" data-backup-codes-target="codesContainer">
                        {% for code in backup_codes %}
                            <div class="py-1">
                                <i class="fa-icon fa-icon--mfa" aria-hidden="true"></i> <span class="font-monospace">{{ code }}</span>
                            </div>
                        {% endfor %}
                    </div>

                    <div class="d-grid gap-2 mt-3">
                        <button type="button"
                                class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm"
                                data-action="backup-codes#copy">
                            <i class="fa-icon fa-icon--audit-trail" aria-hidden="true"></i>
                            {{ 'mfa.backup_codes.copy_all'|trans({}, 'mfa') }}
                        </button>
                        <button type="button"
                                class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm"
                                data-action="backup-codes#print">
                            <i class="fa-icon fa-icon--ui-printer" aria-hidden="true"></i>
                            {{ 'mfa.backup_codes.print'|trans({}, 'mfa') }}
                        </button>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}
