{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'admin.modules.dependency_graph'|trans({}, 'admin') }} - {{ 'admin.panel.title'|trans({}, 'admin') }}{% endblock %}

{% block admin_title %}{{ 'admin.modules.dependency_graph'|trans({}, 'admin') }}{% endblock %}

{% block breadcrumb_items %}
    <li class="breadcrumb-item"><a href="{{ path('admin_modules_index') }}">{{ 'admin.nav.module_management'|trans({}, 'admin') }}</a></li>
    <li class="breadcrumb-item active" aria-current="page">{{ 'admin.modules.dependency_graph'|trans({}, 'admin') }}</li>
{% endblock %}

{% block admin_content %}
    {{ _fa_page_header.render({
        badge: { icon: 'nav-workflow', label: 'Modules' },
        title: 'Modul-Abhängigkeiten',
        subtitle: 'Visualisierung der Modul-Abhängigkeiten',
        actions: [
            { label: 'common.back_to_overview'|trans({}, 'messages'), variant: 'ghost', icon: 'util-arrow-left', href: path('admin_modules_index') }
        ]
    }) }}

    <div class="row g-4">
        <div class="col-lg-8">
            <section class="fa-section">
                <div class="fa-section__header">
                    <h2 class="fa-section__title">Abhängigkeits-Graph</h2>
                </div>
                <div class="fa-section__body">
                    {% embed '_components/_fa_table.html.twig' %}
                        {% block table_head %}
                            <tr>
                                <th scope="col" class="fa-table__th">Modul</th>
                                <th scope="col" class="fa-table__th">Status</th>
                                <th scope="col" class="fa-table__th">Typ</th>
                                <th scope="col" class="fa-table__th">Benötigt</th>
                                <th scope="col" class="fa-table__th">Benötigt von</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for key, graph in dependency_graph %}
                                <tr class="fa-table__row">
                                    <td class="fa-table__cell">
                                        <strong>{{ graph.name }}</strong>
                                    </td>
                                    <td class="fa-table__cell">
                                        {% if key in active_modules %}
                                            <span class="fa-status-pill fa-status-pill--success">Aktiv</span>
                                        {% else %}
                                            <span class="fa-status-pill fa-status-pill--neutral">Inaktiv</span>
                                        {% endif %}
                                    </td>
                                    <td class="fa-table__cell">
                                        {% if graph.required %}
                                            <span class="fa-status-pill fa-status-pill--warning">Erforderlich</span>
                                        {% else %}
                                            <span class="fa-status-pill fa-status-pill--primary">Optional</span>
                                        {% endif %}
                                    </td>
                                    <td class="fa-table__cell">
                                        {% if graph.dependencies|length > 0 %}
                                            {% for dep in graph.dependencies %}
                                                <span class="fa-status-pill fa-status-pill--warning me-1">{{ dependency_graph[dep].name }}</span>
                                            {% endfor %}
                                        {% else %}
                                            <span class="text-muted">-</span>
                                        {% endif %}
                                    </td>
                                    <td class="fa-table__cell">
                                        {% if graph.dependents|length > 0 %}
                                            {% for dep in graph.dependents %}
                                                <span class="fa-status-pill fa-status-pill--primary me-1">{{ dependency_graph[dep].name }}</span>
                                            {% endfor %}
                                        {% else %}
                                            <span class="text-muted">-</span>
                                        {% endif %}
                                    </td>
                                </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                </div>
            </section>
        </div>

        <div class="col-lg-4">
            <section class="fa-section mb-3">
                <div class="fa-section__header">
                    <h2 class="fa-section__title">Legende</h2>
                </div>
                <div class="fa-section__body">
                    <dl class="mb-0">
                        <dt><span class="fa-status-pill fa-status-pill--success">Aktiv</span></dt>
                        <dd>Modul ist derzeit aktiviert</dd>

                        <dt><span class="fa-status-pill fa-status-pill--neutral">Inaktiv</span></dt>
                        <dd>Modul ist derzeit deaktiviert</dd>

                        <dt><span class="fa-status-pill fa-status-pill--warning">Erforderlich</span></dt>
                        <dd>Modul ist Pflicht und kann nicht deaktiviert werden</dd>

                        <dt><span class="fa-status-pill fa-status-pill--primary">Optional</span></dt>
                        <dd>Modul kann aktiviert/deaktiviert werden</dd>

                        <dt>Benötigt</dt>
                        <dd>Module, die vorher aktiviert sein müssen</dd>

                        <dt>Benötigt von</dt>
                        <dd>Module, die dieses Modul benötigen</dd>
                    </dl>
                </div>
            </section>

            <section class="fa-section">
                <div class="fa-section__header">
                    <h2 class="fa-section__title">
                        <i class="fa-icon fa-icon--ui-lightbulb" aria-hidden="true"></i>
                        Hinweise
                    </h2>
                </div>
                <div class="fa-section__body">
                    <ul class="mb-0 small">
                        <li>{{ 'admin.modules.hint.auto_dependencies'|trans({}, 'admin') }}</li>
                        <li>{{ 'admin.modules.hint.activate_required'|trans({}, 'admin') }}</li>
                        <li>{{ 'admin.modules.hint.deactivate_dependency'|trans({}, 'admin') }}</li>
                        <li>{{ 'admin.modules.hint.required_always_active'|trans({}, 'admin') }}</li>
                    </ul>
                </div>
            </section>
        </div>
    </div>
{% endblock admin_content %}
