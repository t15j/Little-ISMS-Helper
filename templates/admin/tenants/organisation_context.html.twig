{% extends 'admin/layout.html.twig' %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_cyber_field.html.twig' as _fa_cyber %}

{% block admin_title %}
    {{ 'tenant.organisation_context.title'|trans({}, 'tenant') }}
{% endblock %}

{% block breadcrumb_items %}
    <li class="breadcrumb-item"><a href="{{ path('tenant_management_index') }}">{{ 'tenant.title'|trans({}, 'tenant') }}</a></li>
    <li class="breadcrumb-item"><a href="{{ path('tenant_management_show', {id: tenant.id}) }}">{{ tenant.code }}</a></li>
    <li class="breadcrumb-item active">{{ 'tenant.organisation_context.title'|trans({}, 'tenant') }}</li>
{% endblock %}

{% block admin_content %}
    {{ _fa_page_header.render({
        badge: { icon: 'ui-globe', label: 'tenant.organisation_context.title'|trans({}, 'tenant') },
        title: 'tenant.organisation_context.title'|trans({}, 'tenant'),
        subtitle: tenant.name
    }) }}

    <div class="row">
        <div class="col-lg-8">
            <section class="fa-section">
                <div class="fa-section__header">
                    <h2 class="fa-section__title">
                        <i class="fa-icon fa-icon--nav-building text-primary" aria-hidden="true"></i>
                        {{ 'tenant.organisation_context.header'|trans({}, 'tenant') }}
                    </h2>
                </div>
                <div class="fa-section__body">
                    <p class="text-muted mb-4">
                        {{ 'tenant.organisation_context.description'|trans({}, 'tenant') }}
                    </p>

                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

                    {# Organisation Name (read-only, from tenant) #}
                    <div class="mb-3">
                        {{ _fa_cyber.text('tenant_name_display', {
                            id: 'tenant-name-display',
                            label: 'tenant.field.name'|trans({}, 'tenant'),
                            value: tenant.name,
                            disabled: true,
                            help: 'tenant.organisation_context.name_info'|trans({}, 'tenant')
                        }) }}
                    </div>

                    {# Industries Multi-Select #}
                    <div class="row">
                        <div class="col-md-12">
                            {% include '_components/_form_field.html.twig' with {
                                'field': form.industries,
                                'label': 'tenant.organisation_context.field.industries'|trans({}, 'tenant')
                            } %}
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            {% include '_components/_form_field.html.twig' with {
                                'field': form.employee_count,
                                'label': 'tenant.organisation_context.field.employee_count'|trans({}, 'tenant')
                            } %}
                        </div>
                        <div class="col-md-6">
                            {% include '_components/_form_field.html.twig' with {
                                'field': form.country,
                                'label': 'tenant.organisation_context.field.country'|trans({}, 'tenant')
                            } %}
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12">
                            {% include '_components/_form_field.html.twig' with {
                                'field': form.description,
                                'label': 'tenant.organisation_context.field.description'|trans({}, 'tenant')
                            } %}
                        </div>
                    </div>

                    {# Current Configuration Info #}
                    {% if current_settings %}
                    {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info' } } %}
                        {% block alert_body %}
                        <div class="fa-alert__title">{{ 'tenant.organisation_context.current_config'|trans({}, 'tenant') }}</div>
                        <ul class="mb-0">
                            {% if current_settings.selected_modules is defined and current_settings.selected_modules %}
                            <li>
                                <strong>{{ 'tenant.organisation_context.active_modules'|trans({}, 'tenant') }}:</strong>
                                {{ current_settings.selected_modules|join(', ') }}
                            </li>
                            {% endif %}
                            {% if current_settings.selected_frameworks is defined and current_settings.selected_frameworks %}
                            <li>
                                <strong>{{ 'tenant.organisation_context.active_frameworks'|trans({}, 'tenant') }}:</strong>
                                {{ current_settings.selected_frameworks|join(', ') }}
                            </li>
                            {% endif %}
                            {% if current_settings.setup_completed_at is defined %}
                            <li>
                                <strong>{{ 'tenant.organisation_context.setup_date'|trans({}, 'tenant') }}:</strong>
                                {{ current_settings.setup_completed_at|date('d.m.Y H:i') }}
                            </li>
                            {% endif %}
                            {% if current_settings.last_modified_at is defined %}
                            <li>
                                <strong>{{ 'tenant.organisation_context.last_modified'|trans({}, 'tenant') }}:</strong>
                                {{ current_settings.last_modified_at|date('d.m.Y H:i') }}
                            </li>
                            {% endif %}
                        </ul>
                        {% endblock %}
                    {% endembed %}
                    {% endif %}

                    {# Impact Warning #}
                    {{ _fa_alert.render({ variant: 'warning', title: 'tenant.organisation_context.impact_warning.title'|trans({}, 'tenant'), body: 'tenant.organisation_context.impact_warning.message'|trans({}, 'tenant') }) }}

                    {# Form Actions #}
                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('tenant_management_show', {id: tenant.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--util-arrow-left me-2" aria-hidden="true"></i>
                            {{ 'common.back'|trans({}, 'messages') }}
                        </a>

                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--ui-check me-2" aria-hidden="true"></i>
                            {{ 'common.save'|trans({}, 'messages') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </section>
        </div>

        {# Sidebar Info #}
        <div class="col-lg-4">
            <section class="fa-section">
                <div class="fa-section__header">
                    <h2 class="fa-section__title">
                        <i class="fa-icon fa-icon--ui-lightbulb text-info me-2" aria-hidden="true"></i>
                        {{ 'tenant.organisation_context.info.title'|trans({}, 'tenant') }}
                    </h2>
                </div>
                <div class="fa-section__body">
                    <p class="small text-muted">
                        {{ 'tenant.organisation_context.info.description'|trans({}, 'tenant') }}
                    </p>
                    <ul class="small text-muted mb-0">
                        <li>{{ 'tenant.organisation_context.info.industries'|trans({}, 'tenant') }}</li>
                        <li>{{ 'tenant.organisation_context.info.size'|trans({}, 'tenant') }}</li>
                        <li>{{ 'tenant.organisation_context.info.country'|trans({}, 'tenant') }}</li>
                        <li>{{ 'tenant.organisation_context.info.corporate'|trans({}, 'tenant') }}</li>
                    </ul>
                </div>
            </section>
        </div>
    </div>
{% endblock %}
