{% extends 'base.html.twig' %}
{% trans_default_domain 'management_reports' %}

{% block title %}{{ 'management_reports.executive.title'|trans({}, 'management_reports') }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}
{% import '_components/_fa_cyber_field.html.twig' as _fa_cyber %}
<div class="container-fluid py-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.reports'|trans({}, 'nav'), url: path('app_reports_index') },
            { label: 'management_reports.title'|trans({}, 'management_reports'), url: path('app_management_reports') },
            { label: 'management_reports.executive.title'|trans({}, 'management_reports') }
        ]
    } %}

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h2 mb-0">
            <i class="fa-icon fa-icon--nav-dashboard text-primary" aria-hidden="true"></i> {{ 'management_reports.executive.title'|trans({}, 'management_reports') }}
        </h1>
        <div>
            <div class="btn-group">
                <a href="{{ path('app_management_reports_executive_pdf') }}" class="fa-cyber-btn fa-cyber-btn--danger">
                    <i class="fa-icon fa-icon--nav-file-pdf" aria-hidden="true"></i> {{ 'management_reports.action.download_pdf'|trans({}, 'management_reports') }}
                </a>
            </div>
            {% include '_components/_fa_pdf_locale_switch.html.twig' with {route: 'app_management_reports_executive_pdf'} only %}
        </div>
    </div>

    {# Date Range Filter #}
    <form class="d-flex gap-2 mb-3 align-items-end" method="get">
        <div>
            {{ _fa_cyber.text('from', {
                id: 'filter-from',
                type: 'date',
                label: 'management_reports.filter.from'|trans({}, 'management_reports'),
                value: app.request.query.get('from')
            }) }}
        </div>
        <div>
            {{ _fa_cyber.text('to', {
                id: 'filter-to',
                type: 'date',
                label: 'management_reports.filter.to'|trans({}, 'management_reports'),
                value: app.request.query.get('to')
            }) }}
        </div>
        <button type="submit" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
            <i class="fa-icon fa-icon--ui-funnel" aria-hidden="true"></i> {{ 'management_reports.filter.apply'|trans({}, 'management_reports') }}
        </button>
        {% if app.request.query.get('from') or app.request.query.get('to') %}
        <a href="{{ path('app_management_reports_executive') }}" class="fa-cyber-btn fa-cyber-btn--ghost fa-cyber-btn--sm">
            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> {{ 'management_reports.filter.reset'|trans({}, 'management_reports') }}
        </a>
        {% endif %}
    </form>

    {# Key Metrics #}
    <div class="row mb-4">
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'asset-server',
                label: 'management_reports.executive.assets'|trans({}, 'management_reports'),
                value: summary.summary.total_assets,
                variant: 'primary'
            }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'status-warning',
                label: 'management_reports.executive.risks'|trans({}, 'management_reports'),
                value: summary.summary.total_risks,
                variant: 'danger'
            }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'shield-check',
                label: 'management_reports.executive.compliance'|trans({}, 'management_reports'),
                value: summary.summary.compliance_percentage,
                unit: '%',
                variant: 'success'
            }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'nav-shield',
                label: 'management_reports.executive.controls'|trans({}, 'management_reports'),
                value: summary.summary.total_controls,
                variant: 'primary'
            }) }}
        </div>
    </div>

    <div class="row">
        {# Risk Distribution #}
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i>
                            {{ 'management_reports.executive.risk_distribution'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'management_reports.risk_level.critical'|trans({}, 'management_reports') } %}
                            <span>{{ summary.risk_distribution.critical }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_risks > 0 ? (summary.risk_distribution.critical / summary.summary.total_risks * 100) : 0, tone: 'danger' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'warning', content: 'management_reports.risk_level.high'|trans({}, 'management_reports') } %}
                            <span>{{ summary.risk_distribution.high }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_risks > 0 ? (summary.risk_distribution.high / summary.summary.total_risks * 100) : 0, tone: 'warning' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'info', content: 'management_reports.risk_level.medium'|trans({}, 'management_reports') } %}
                            <span>{{ summary.risk_distribution.medium }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_risks > 0 ? (summary.risk_distribution.medium / summary.summary.total_risks * 100) : 0, tone: 'info' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'success', content: 'management_reports.risk_level.low'|trans({}, 'management_reports') } %}
                            <span>{{ summary.risk_distribution.low }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_risks > 0 ? (summary.risk_distribution.low / summary.summary.total_risks * 100) : 0, tone: 'success' }) }}
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        {# Control Implementation Status #}
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--compliance-shield" aria-hidden="true"></i>
                            {{ 'management_reports.executive.control_status'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'success', content: 'management_reports.control_status.implemented'|trans({}, 'management_reports') } %}
                            <span>{{ summary.control_status.implemented }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_controls > 0 ? (summary.control_status.implemented / summary.summary.total_controls * 100) : 0, tone: 'success' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'warning', content: 'management_reports.control_status.in_progress'|trans({}, 'management_reports') } %}
                            <span>{{ summary.control_status.in_progress }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_controls > 0 ? (summary.control_status.in_progress / summary.summary.total_controls * 100) : 0, tone: 'warning' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'secondary', content: 'management_reports.control_status.not_started'|trans({}, 'management_reports') } %}
                            <span>{{ summary.control_status.not_started }}</span>
                        </div>
                        {{ _fa_progress.render({ value: summary.summary.total_controls > 0 ? (summary.control_status.not_started / summary.summary.total_controls * 100) : 0, tone: 'neutral' }) }}
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    <div class="row">
        {# Incident Summary #}
        <div class="col-md-4 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-bell" aria-hidden="true"></i>
                            {{ 'management_reports.executive.incidents_12m'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="row text-center">
                        <div class="col-4">
                            <h3 class="text-primary">{{ summary.incident_summary.total_12_months }}</h3>
                            <small class="text-muted">{{ 'management_reports.incident_status.total'|trans({}, 'management_reports') }}</small>
                        </div>
                        <div class="col-4">
                            <h3 class="text-warning">{{ summary.incident_summary.open }}</h3>
                            <small class="text-muted">{{ 'management_reports.incident_status.open'|trans({}, 'management_reports') }}</small>
                        </div>
                        <div class="col-4">
                            <h3 class="text-success">{{ summary.incident_summary.resolved }}</h3>
                            <small class="text-muted">{{ 'management_reports.incident_status.resolved'|trans({}, 'management_reports') }}</small>
                        </div>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        {# Audit Summary #}
        <div class="col-md-4 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-search" aria-hidden="true"></i>
                            {{ 'management_reports.executive.audits'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="row text-center">
                        <div class="col-6">
                            <h3 class="text-primary">{{ summary.audit_summary.total_audits }}</h3>
                            <small class="text-muted">{{ 'management_reports.audit_status.total'|trans({}, 'management_reports') }}</small>
                        </div>
                        <div class="col-6">
                            <h3 class="text-success">{{ summary.audit_summary.completed_this_year }}</h3>
                            <small class="text-muted">{{ 'management_reports.audit_status.this_year'|trans({}, 'management_reports') }}</small>
                        </div>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        {# Training Summary #}
        <div class="col-md-4 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--nav-mortarboard" aria-hidden="true"></i>
                            {{ 'management_reports.executive.trainings'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="text-center">
                        <h3 class="text-primary">{{ summary.training_summary.total_trainings }}</h3>
                        <small class="text-muted">{{ 'management_reports.training_status.total'|trans({}, 'management_reports') }}</small>
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Trend Charts Placeholder #}
    <div class="row mt-4">
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                            {{ 'management_reports.executive.risk_trends'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_fa_table.html.twig' with { compact: true } %}
                        {% block table_head %}
                            <tr>
                                <th scope="col" class="fa-table__th">{{ 'management_reports.trend.month'|trans({}, 'management_reports') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--num">{{ 'management_reports.trend.total_risks'|trans({}, 'management_reports') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--num">{{ 'management_reports.trend.high_critical'|trans({}, 'management_reports') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--num">{{ 'management_reports.trend.new_risks'|trans({}, 'management_reports') }}</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for trend in risk_trends %}
                            <tr class="fa-table__row">
                                <td class="fa-table__cell">{{ trend.month_name }}</td>
                                <td class="fa-table__cell fa-table__cell--num"><strong>{{ trend.total }}</strong></td>
                                <td class="fa-table__cell fa-table__cell--num">
                                    {% if trend.high_critical > 0 %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: trend.high_critical } %}
                                    {% else %}
                                    {% include '_components/_badge.html.twig' with { variant: 'success', content: 0 } %}
                                    {% endif %}
                                </td>
                                <td class="fa-table__cell fa-table__cell--num">{{ trend.new_risks }}</td>
                            </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i>
                            {{ 'management_reports.executive.incident_trends'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    {% embed '_components/_fa_table.html.twig' with { compact: true } %}
                        {% block table_head %}
                            <tr>
                                <th scope="col" class="fa-table__th">{{ 'management_reports.trend.month'|trans({}, 'management_reports') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--num">{{ 'management_reports.trend.total_incidents'|trans({}, 'management_reports') }}</th>
                                <th scope="col" class="fa-table__th fa-table__th--num">{{ 'management_reports.trend.high_critical'|trans({}, 'management_reports') }}</th>
                            </tr>
                        {% endblock %}
                        {% block table_body %}
                            {% for trend in incident_trends %}
                            <tr class="fa-table__row">
                                <td class="fa-table__cell">{{ trend.month_name }}</td>
                                <td class="fa-table__cell fa-table__cell--num">{{ trend.total }}</td>
                                <td class="fa-table__cell fa-table__cell--num">
                                    {% if trend.high_critical > 0 %}
                                    {% include '_components/_badge.html.twig' with { variant: 'danger', content: trend.high_critical } %}
                                    {% else %}
                                    {% include '_components/_badge.html.twig' with { variant: 'success', content: 0 } %}
                                    {% endif %}
                                </td>
                            </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Report Generated Info #}
    <div class="row mt-3">
        <div class="col-12">
            <div class="text-muted small text-end">
                <i class="fa-icon fa-icon--ui-clock" aria-hidden="true"></i>
                {{ 'management_reports.generated_at'|trans({}, 'management_reports') }}: {{ summary.generated_at|date('Y-m-d H:i') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
