{% extends 'base.html.twig' %}
{% trans_default_domain 'management_reports' %}

{% block title %}{{ 'management_reports.audit.title'|trans({}, 'management_reports') }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
{% import '_components/_fa_feature_card.html.twig' as _fa_feature_card %}
<div class="container-fluid py-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.reports'|trans({}, 'nav'), url: path('app_reports_index') },
            { label: 'management_reports.title'|trans({}, 'management_reports'), url: path('app_management_reports') },
            { label: 'management_reports.audit.title'|trans({}, 'management_reports') }
        ]
    } %}

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h2 mb-0">
            <i class="fa-icon fa-icon--ui-search text-warning" aria-hidden="true"></i> {{ 'management_reports.audit.title'|trans({}, 'management_reports') }}
        </h1>
        <div>
            <div class="btn-group">
                <a href="{{ path('app_management_reports_audit_pdf') }}" class="fa-cyber-btn fa-cyber-btn--danger">
                    <i class="fa-icon fa-icon--nav-file-pdf" aria-hidden="true"></i> {{ 'management_reports.action.download_pdf'|trans({}, 'management_reports') }}
                </a>
                <a href="{{ path('app_management_reports_audit_excel') }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                    <i class="fa-icon fa-icon--nav-file-check" aria-hidden="true"></i> {{ 'management_reports.action.download_excel'|trans({}, 'management_reports') }}
                </a>
            </div>
            {% include '_components/_fa_pdf_locale_switch.html.twig' with {route: 'app_management_reports_audit_pdf'} only %}
        </div>
    </div>

    {# Audit Summary #}
    <div class="row mb-4">
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'nav-clipboard-check',
                label: 'management_reports.audit.total_audits'|trans({}, 'management_reports'),
                value: report.audits.total,
                variant: 'primary'
            }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'nav-calendar-check',
                label: 'management_reports.audit.this_year'|trans({}, 'management_reports'),
                value: report.audits.this_year,
                variant: 'success'
            }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'nav-people',
                label: 'management_reports.audit.total_reviews'|trans({}, 'management_reports'),
                value: report.management_reviews.total,
                variant: 'primary'
            }) }}
        </div>
        <div class="col-md-3">
            {{ _fa_feature_card.render({
                icon: 'ui-calendar-event',
                label: 'management_reports.audit.reviews_this_year'|trans({}, 'management_reports'),
                value: report.management_reviews.this_year,
                variant: 'warning'
            }) }}
        </div>
    </div>

    <div class="row">
        {# Audit Status Breakdown #}
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--nav-pie-chart" aria-hidden="true"></i>
                            {{ 'management_reports.audit.status_breakdown'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'secondary', content: 'management_reports.audit_status.planned'|trans({}, 'management_reports') } %}
                            <span>{{ report.audits.by_status.planned }}</span>
                        </div>
                        {{ _fa_progress.render({ value: report.audits.total > 0 ? (report.audits.by_status.planned / report.audits.total * 100) : 0, tone: 'neutral' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'primary', content: 'management_reports.audit_status.in_progress'|trans({}, 'management_reports') } %}
                            <span>{{ report.audits.by_status.in_progress }}</span>
                        </div>
                        {{ _fa_progress.render({ value: report.audits.total > 0 ? (report.audits.by_status.in_progress / report.audits.total * 100) : 0, tone: 'primary' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'success', content: 'management_reports.audit_status.completed'|trans({}, 'management_reports') } %}
                            <span>{{ report.audits.by_status.completed }}</span>
                        </div>
                        {{ _fa_progress.render({ value: report.audits.total > 0 ? (report.audits.by_status.completed / report.audits.total * 100) : 0, tone: 'success' }) }}
                    </div>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-1">
                            {% include '_components/_badge.html.twig' with { variant: 'danger', content: 'management_reports.audit_status.cancelled'|trans({}, 'management_reports') } %}
                            <span>{{ report.audits.by_status.cancelled }}</span>
                        </div>
                        {{ _fa_progress.render({ value: report.audits.total > 0 ? (report.audits.by_status.cancelled / report.audits.total * 100) : 0, tone: 'danger' }) }}
                    </div>
                {% endblock %}
            {% endembed %}
        </div>

        {# ISO 27001 Requirements #}
        <div class="col-md-6 mb-4">
            {% embed '_components/_card.html.twig' with { 'class': 'h-100 shadow-sm' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h3 class="h5 mb-0">
                            <i class="fa-icon fa-icon--ui-info" aria-hidden="true"></i>
                            {{ 'management_reports.audit.iso_requirements'|trans({}, 'management_reports') }}
                        </h3>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <div class="fa-alert fa-alert--info mb-3" role="alert">
                        <strong>{{ 'management_reports.audit.iso_clause_9'|trans({}, 'management_reports') }}</strong>
                    </div>
                    <ul class="list-group">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            {{ 'management_reports.audit.internal_audits'|trans({}, 'management_reports') }}
                            {% include '_components/_badge.html.twig' with { variant: report.audits.this_year >= 1 ? 'success' : 'warning', content: report.audits.this_year >= 1 ? 'management_reports.audit.compliant'|trans({}, 'management_reports') : 'management_reports.audit.pending'|trans({}, 'management_reports') } %}
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            {{ 'management_reports.audit.management_review'|trans({}, 'management_reports') }}
                            {% include '_components/_badge.html.twig' with { variant: report.management_reviews.this_year >= 1 ? 'success' : 'warning', content: report.management_reviews.this_year >= 1 ? 'management_reports.audit.compliant'|trans({}, 'management_reports') : 'management_reports.audit.pending'|trans({}, 'management_reports') } %}
                        </li>
                    </ul>
                {% endblock %}
            {% endembed %}
        </div>
    </div>

    {# Quick Links #}
    {% embed '_components/_card.html.twig' with { 'class': 'shadow-sm mb-4' } %}
        {% block card_header %}
            <div class="card-header">
                <h3 class="h5 mb-0">
                    <i class="fa-icon fa-icon--ui-link" aria-hidden="true"></i>
                    {{ 'management_reports.audit.quick_links'|trans({}, 'management_reports') }}
                </h3>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="row">
                <div class="col-md-6">
                    <a href="{{ path('app_audit_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost w-100 mb-2">
                        <i class="fa-icon fa-icon--sop" aria-hidden="true"></i> {{ 'management_reports.audit.view_audits'|trans({}, 'management_reports') }}
                    </a>
                </div>
                <div class="col-md-6">
                    <a href="{{ path('app_management_review_index') }}" class="fa-cyber-btn fa-cyber-btn--ghost w-100 mb-2">
                        <i class="fa-icon fa-icon--group" aria-hidden="true"></i> {{ 'management_reports.audit.view_reviews'|trans({}, 'management_reports') }}
                    </a>
                </div>
            </div>
        {% endblock %}
    {% endembed %}

    {# Report Generated Info #}
    <div class="text-muted small text-end">
        <i class="fa-icon fa-icon--ui-clock" aria-hidden="true"></i>
        {{ 'management_reports.generated_at'|trans({}, 'management_reports') }}: {{ report.generated_at|date('Y-m-d H:i') }}
    </div>
</div>
{% endblock %}
