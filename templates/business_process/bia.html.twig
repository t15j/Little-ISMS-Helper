{% extends 'base.html.twig' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}
{% import '_components/_fa_alert.html.twig' as _fa_alert %}
{% block title %}BIA - {{ business_process.name }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Home</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_business_process_index') }}">BCM</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_business_process_show', {'id': business_process.id}) }}">{{ business_process.name }}</a></li>
            <li class="breadcrumb-item active">Business Impact Analysis</li>
        </ol>
    </nav>

    {{ _fa_page_header.render({
        badge: { icon: 'nav-bar-chart', label: 'nav.bcm'|trans({}, 'nav') },
        title: 'business_process.bia.title'|trans({}, 'business_process'),
        subtitle: business_process.name,
        actions: [
            { label: 'common.back'|trans({}, 'messages'), variant: 'ghost', icon: 'util-arrow-left', href: path('app_business_process_show', {'id': business_process.id}) }
        ]
    }) }}

    {# Recovery Objectives #}
    {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0"><i class="fa-icon fa-icon--nav-calendar-check" aria-hidden="true"></i> Recovery Objectives</h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="row">
                <div class="col-md-4">
                    {% embed '_components/_card.html.twig' with {variant: 'light'} %}
                        {% block card_body %}
                            <div class="text-center">
                                <div class="small fw-semibold text-muted mb-1">RTO</div>
                                <p class="display-4 fw-bold mb-1">{{ bia_data.rto }}h</p>
                                <small class="text-muted">{{ 'business_process.bia.rto_long'|trans({}, 'bcm') }}</small>
                            </div>
                        {% endblock %}
                    {% endembed %}
                </div>
                <div class="col-md-4">
                    {% embed '_components/_card.html.twig' with {variant: 'light'} %}
                        {% block card_body %}
                            <div class="text-center">
                                <div class="small fw-semibold text-muted mb-1">RPO</div>
                                <p class="display-4 fw-bold mb-1">{{ bia_data.rpo }}h</p>
                                <small class="text-muted">{{ 'business_process.bia.rpo_long'|trans({}, 'bcm') }}</small>
                            </div>
                        {% endblock %}
                    {% endembed %}
                </div>
                <div class="col-md-4">
                    {% embed '_components/_card.html.twig' with {variant: 'light'} %}
                        {% block card_body %}
                            <div class="text-center">
                                <div class="small fw-semibold text-muted mb-1">MTPD</div>
                                <p class="display-4 fw-bold mb-1">{{ bia_data.mtpd }}h</p>
                                <small class="text-muted">{{ 'business_process.bia.mtpd_long'|trans({}, 'bcm') }}</small>
                            </div>
                        {% endblock %}
                    {% endembed %}
                </div>
            </div>
        {% endblock %}
    {% endembed %}

    {# Financial Impact #}
    {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0"><i class="fa-icon fa-icon--impact" aria-hidden="true"></i> {{ 'business_process.bia.section.financial_impact'|trans({}, 'business_process') }}</h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="row">
                <div class="col-md-6">
                    <div class="small text-muted text-uppercase fw-semibold mb-1">{{ 'business_process.bia.field.damage_per_hour'|trans({}, 'business_process') }}</div>
                    <p class="h3">{{ bia_data.financial_impact_hour ? (bia_data.financial_impact_hour|number_format(2, ',', '.') ~ ' €') : 'business_process.bia.field.not_specified'|trans({}, 'business_process') }}</p>
                </div>
                <div class="col-md-6">
                    <div class="small text-muted text-uppercase fw-semibold mb-1">{{ 'business_process.bia.field.damage_per_day'|trans({}, 'business_process') }}</div>
                    <p class="h3">{{ bia_data.financial_impact_day ? (bia_data.financial_impact_day|number_format(2, ',', '.') ~ ' €') : 'business_process.bia.field.not_specified'|trans({}, 'business_process') }}</p>
                </div>
            </div>
            {% if bia_data.financial_impact_day %}
                {% embed '_components/_fa_alert.html.twig' with { props: { variant: 'info', class: 'mt-3' } } %}
                    {% block alert_body %}
                    <ul class="mb-0">
                        <li>{{ 'business_process.bia.field.projection_week'|trans({'%amount%': (bia_data.financial_impact_day * 7)|number_format(2, ',', '.')}, 'business_process') }}</li>
                        <li>{{ 'business_process.bia.field.projection_month'|trans({'%amount%': (bia_data.financial_impact_day * 30)|number_format(2, ',', '.')}, 'business_process') }}</li>
                    </ul>
                    {% endblock %}
                {% endembed %}
            {% endif %}
        {% endblock %}
    {% endembed %}

    {# Impact Assessment #}
    {% embed '_components/_card.html.twig' with {class: 'mb-4'} %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0"><i class="fa-icon fa-icon--nav-bar-chart" aria-hidden="true"></i> {{ 'business_process.bia.section.impact_assessment'|trans({}, 'business_process') }}</h2>
            </div>
        {% endblock %}
        {% block card_body %}
    {% import '_components/_fa_progress.html.twig' as _fa_progress %}
            <div class="row">
                <div class="col-md-4">
                    <div class="small text-muted text-uppercase fw-semibold mb-2">{{ 'business_process.bia.field.reputational_impact'|trans({}, 'business_process') }}</div>
                    {{ _fa_progress.render({ value: bia_data.reputational_impact, max: 5, tone: 'danger', showText: true }) }}
                </div>
                <div class="col-md-4">
                    <div class="small text-muted text-uppercase fw-semibold mb-2">{{ 'business_process.bia.field.regulatory_impact'|trans({}, 'business_process') }}</div>
                    {{ _fa_progress.render({ value: bia_data.regulatory_impact, max: 5, tone: 'warning', showText: true }) }}
                </div>
                <div class="col-md-4">
                    <div class="small text-muted text-uppercase fw-semibold mb-2">{{ 'business_process.bia.field.operational_impact'|trans({}, 'business_process') }}</div>
                    {{ _fa_progress.render({ value: bia_data.operational_impact, max: 5, tone: 'info', showText: true }) }}
                </div>
            </div>
            <div class="mt-3">
                <div class="small text-muted text-uppercase fw-semibold mb-2">{{ 'business_process.bia.field.business_impact_score'|trans({}, 'business_process') }}</div>
                {{ _fa_progress.render({ value: bia_data.business_impact_score, max: 5, tone: bia_data.business_impact_score >= 4 ? 'danger' : (bia_data.business_impact_score >= 3 ? 'warning' : 'success'), showText: true }) }}
            </div>
        {% endblock %}
    {% endembed %}

    {# Data Reuse Recommendations #}
    {% embed '_components/_card.html.twig' %}
        {% block card_header %}
            <div class="card-header">
                <h2 class="mb-0"><i class="fa-icon fa-icon--ui-lightbulb" aria-hidden="true"></i> {{ 'business_process.bia.section.data_reuse_recommendations'|trans({}, 'business_process') }}</h2>
            </div>
        {% endblock %}
        {% block card_body %}
            <div class="small text-muted text-uppercase fw-semibold mb-2">{{ 'business_process.bia.field.suggested_availability'|trans({}, 'business_process') }}</div>
            <p class="lead">
                {{ 'business_process.bia.field.rto_recommendation_intro'|trans({'%rto%': bia_data.rto}, 'business_process') }}
                {% include '_components/_badge.html.twig' with { variant: 'primary', class: 'fa-status-pill--lg', content: bia_data.suggested_availability ~ '/5' } %}
            </p>
            <small class="text-muted">
                {{ 'business_process.bia.field.rto_consistency_hint'|trans({}, 'business_process') }}
            </small>
        {% endblock %}
    {% endembed %}
</div>
{% endblock %}
