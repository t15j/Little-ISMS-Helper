{% extends 'base.html.twig' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'compliance.mapping.edit.title'|trans({}, 'compliance') }}{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    {# Breadcrumb Navigation #}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.compliance'|trans({}, 'nav'), url: path('app_compliance_index'), icon: 'shield-check' },
            { label: 'compliance.mapping.list.title'|trans({}, 'compliance'), url: path('app_compliance_mapping_index') },
            { label: mapping.sourceRequirement.requirementId ~ ' → ' ~ mapping.targetRequirement.requirementId, url: path('app_compliance_mapping_show', {id: mapping.id}) },
            { label: 'common.edit'|trans({}, 'messages') }
        ]
    } %}

    {# Page Header #}
    {{ _fa_page_header.render({
        title: 'compliance.mapping.edit.title'|trans({}, 'compliance'),
        badge: { icon: 'ui-edit', label: '' },
        actions: [
            { label: 'common.view'|trans({}, 'messages'), href: path('app_compliance_mapping_show', {id: mapping.id}), variant: 'outline-primary', icon: 'ui-eye' },
            { label: 'common.back'|trans({}, 'messages'), href: path('app_compliance_mapping_index'), variant: 'secondary', icon: 'util-arrow-left' }
        ]
    }) }}

    <div class="row">
        <div class="col-md-8">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    {{ form_start(form) }}

                    {% include '_components/_auto_form.html.twig' with {
                        'form': form,
                        'sections': {
                            'requirements': {
                                'label': 'compliance.mapping.section.requirements'|trans({}, 'compliance'),
                                'fields': ['sourceRequirement', 'targetRequirement']
                            },
                            'strength': {
                                'label': 'compliance.mapping.section.strength'|trans({}, 'compliance'),
                                'fields': ['mappingPercentage', 'mappingType', 'confidence']
                            },
                            'rationale': {
                                'label': 'compliance.mapping.section.rationale'|trans({}, 'compliance'),
                                'fields': ['mappingRationale', 'bidirectional']
                            }
                        },
                        'col_widths': {
                            'sourceRequirement': 6,
                            'targetRequirement': 6,
                            'mappingPercentage': 4,
                            'mappingType': 4,
                            'confidence': 4,
                            'mappingRationale': 12,
                            'bidirectional': 6
                        }
                    } %}

                    {# Form Actions #}
                    <div class="form-actions d-flex gap-2 justify-content-between">
                        <a href="{{ path('app_compliance_mapping_show', {id: mapping.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--ui-save" aria-hidden="true"></i> {{ 'common.save'|trans({}, 'messages') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-md-4">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header"><h2 class="mb-0"><i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> {{ 'compliance.mapping.info.current'|trans({}, 'compliance') }}</h2></div>
                {% endblock %}
                {% block card_body %}
                    <p class="small">
                        <strong>{{ 'compliance.mapping.field.source'|trans({}, 'compliance') }}:</strong><br>
                        {{ mapping.sourceRequirement.framework.code }} - {{ mapping.sourceRequirement.requirementId }}
                    </p>
                    <p class="small">
                        <strong>{{ 'compliance.mapping.field.target'|trans({}, 'compliance') }}:</strong><br>
                        {{ mapping.targetRequirement.framework.code }} - {{ mapping.targetRequirement.requirementId }}
                    </p>
                    <p class="small">
                        <strong>{{ 'compliance.mapping.field.type'|trans({}, 'compliance') }}:</strong>
                        {% include '_components/_badge.html.twig' with { variant: mapping.mappingBadgeClass, content: mapping.mappingType } %}
                    </p>
                    {% if mapping.isAnalyzed %}
                        <p class="small">
                            <strong>{{ 'compliance.mapping.field.calculated'|trans({}, 'compliance') }}:</strong> {{ mapping.calculatedPercentage }}%<br>
                            <strong>{{ 'compliance.mapping.field.analysis_confidence'|trans({}, 'compliance') }}:</strong> {{ mapping.analysisConfidence }}%
                        </p>
                    {% endif %}
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}
