{% extends 'base.html.twig' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'compliance.framework.edit.title'|trans({}, 'compliance') }} - {{ framework.name }}{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.compliance'|trans({}, 'nav'), url: path('app_compliance_index') },
            { label: framework.name, url: path('app_compliance_framework_show', {id: framework.id}) },
            { label: 'common.edit'|trans({}, 'messages') }
        ]
    } %}

    {{ _fa_page_header.render({
        title: ('compliance.framework.edit.heading'|trans({}, 'compliance')) ~ ': ' ~ framework.name,
        badge: { icon: 'ui-edit', label: '' },
        actions: [
            { label: 'common.back'|trans({}, 'messages'), href: path('app_compliance_framework_show', {id: framework.id}), variant: 'ghost', icon: 'util-arrow-left' }
        ]
    }) }}

    <div class="row">
        <div class="col-lg-8">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    {{ form_start(form) }}

                    {% include '_components/_auto_form.html.twig' with {
                        'form': form,
                        'sections': {
                            'identification': {
                                'label': 'compliance.framework.section.identification'|trans({}, 'compliance'),
                                'fields': ['code', 'version', 'name']
                            },
                            'details': {
                                'label': 'compliance.framework.section.details'|trans({}, 'compliance'),
                                'fields': ['description', 'applicableIndustry', 'regulatoryBody', 'scopeDescription']
                            },
                            'configuration': {
                                'label': 'compliance.framework.section.configuration'|trans({}, 'compliance'),
                                'fields': ['mandatory', 'active']
                            }
                        },
                        'col_widths': {
                            'code': 6,
                            'version': 6,
                            'name': 12,
                            'description': 12,
                            'applicableIndustry': 6,
                            'regulatoryBody': 6,
                            'scopeDescription': 12,
                            'mandatory': 4,
                            'active': 4
                        }
                    } %}

                    <div class="d-flex gap-2 pt-3 border-top">
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--warning">
                            <i class="fa-icon fa-icon--status-ok" aria-hidden="true"></i> {{ 'common.save'|trans({}, 'messages') }}
                        </button>
                        <a href="{{ path('app_compliance_framework_show', {id: framework.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                    </div>

                    {{ form_end(form) }}
                {% endblock %}
            {% endembed %}
        </div>
        <div class="col-lg-4">
            {% embed '_components/_card.html.twig' with { 'class': 'mb-3' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0"><i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> {{ 'common.help_label'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <h3>{{ 'compliance.framework.help.code.title'|trans({}, 'compliance') }}</h3>
                    <p class="small text-muted">{{ 'compliance.framework.help.code.text'|trans({}, 'compliance') }}</p>

                    <h3>{{ 'compliance.framework.help.mandatory.title'|trans({}, 'compliance') }}</h3>
                    <p class="small text-muted">{{ 'compliance.framework.help.mandatory.text'|trans({}, 'compliance') }}</p>

                    <h3>{{ 'compliance.framework.help.active.title'|trans({}, 'compliance') }}</h3>
                    <p class="small text-muted">{{ 'compliance.framework.help.active.text'|trans({}, 'compliance') }}</p>
                {% endblock %}
            {% endembed %}

            {% embed '_components/_card.html.twig' with { 'class': 'mb-3' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0"><i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> {{ 'compliance.framework.info'|trans({}, 'compliance') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <dl class="row mb-0">
                        <dt class="col-sm-6">{{ 'common.created_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-6">{{ framework.createdAt|date('d.m.Y H:i') }}</dd>

                        <dt class="col-sm-6">{{ 'common.updated_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-6">{{ framework.updatedAt ? framework.updatedAt|date('d.m.Y H:i') : '-' }}</dd>

                        <dt class="col-sm-6">{{ 'compliance.framework.field.requirements'|trans({}, 'compliance') }}</dt>
                        <dd class="col-sm-6">{% include '_components/_badge.html.twig' with { variant: 'info', content: framework.requirements|length } %}</dd>
                    </dl>
                {% endblock %}
            {% endembed %}

            {% if is_granted('ROLE_ADMIN') %}
            {% embed '_components/_card.html.twig' with { 'class': 'border-danger' } %}
                {% block card_header %}
                    <div class="card-header">
                        <h2 class="mb-0"><i class="fa-icon fa-icon--status-warning" aria-hidden="true"></i> {{ 'common.danger_zone'|trans({}, 'messages') }}</h2>
                    </div>
                {% endblock %}
                {% block card_body %}
                    <form method="post" action="{{ path('app_compliance_framework_delete', {id: framework.id}) }}"
                          data-controller="ui-actions"
                          data-action="submit->ui-actions#confirm"
                          data-ui-actions-message-param="{{ 'compliance.framework.confirm.delete'|trans({}, 'compliance') }}">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ framework.id) }}">
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--danger w-100">
                            <i class="fa-icon fa-icon--ui-trash" aria-hidden="true"></i> {{ 'common.delete'|trans({}, 'messages') }}
                        </button>
                    </form>
                {% endblock %}
            {% endembed %}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
