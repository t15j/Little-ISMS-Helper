{% extends 'base.html.twig' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'compliance.requirement.edit.title'|trans({}, 'compliance') }} {{ requirement.requirementId }}{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    {# Breadcrumb Navigation #}
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.compliance'|trans({}, 'nav'), url: path('app_compliance_index'), icon: 'shield-check' },
            { label: 'compliance.requirement.list.title'|trans({}, 'compliance'), url: path('app_compliance_requirement_index') },
            { label: requirement.requirementId, url: path('app_compliance_requirement_show', {id: requirement.id}) },
            { label: 'common.edit'|trans({}, 'messages') }
        ]
    } %}

    {# Page Header #}
    {{ _fa_page_header.render({
        title: ('compliance.requirement.edit.title'|trans({}, 'compliance')) ~ ' ' ~ requirement.requirementId,
        badge: { icon: 'ui-edit', label: '' },
        actions: [
            { label: 'common.view'|trans({}, 'messages'), href: path('app_compliance_requirement_show', {id: requirement.id}), variant: 'outline-primary', icon: 'ui-eye' },
            { label: 'common.back'|trans({}, 'messages'), href: path('app_compliance_requirement_index'), variant: 'secondary', icon: 'util-arrow-left' }
        ]
    }) }}

    <div class="row">
        <div class="col-md-8">
            {% embed '_components/_card.html.twig' %}
                {% block card_body %}
                    {{ form_start(form) }}

                    {% include '_components/_auto_form.html.twig' with {
                        'form': form,
                        'sections': {
                            'identification': {
                                'label': 'compliance.requirement.section.identification'|trans({}, 'compliance'),
                                'fields': ['framework', 'requirementId']
                            },
                            'basic': {
                                'label': 'compliance.requirement.section.basic'|trans({}, 'compliance'),
                                'fields': ['title', 'description', 'category', 'priority']
                            },
                            'structure': {
                                'label': 'compliance.requirement.section.structure'|trans({}, 'compliance'),
                                'fields': ['requirementType', 'parentRequirement']
                            }
                        },
                        'col_widths': {
                            'framework': 6,
                            'requirementId': 6,
                            'title': 12,
                            'description': 12,
                            'category': 6,
                            'priority': 6,
                            'requirementType': 6,
                            'parentRequirement': 6
                        }
                    } %}

                    {# Note: Applicability and Fulfillment are now tenant-specific #}
                    {# Use the "Quick Update" form on the show page to manage fulfillment data #}

                    {# Form Actions #}
                    <div class="form-actions d-flex gap-2 justify-content-between">
                        <a href="{{ path('app_compliance_requirement_show', {id: requirement.id}) }}" class="fa-cyber-btn fa-cyber-btn--ghost">
                            <i class="fa-icon fa-icon--status-critical" aria-hidden="true"></i> {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                        <button type="submit" class="fa-cyber-btn fa-cyber-btn--primary">
                            <i class="fa-icon fa-icon--ui-save" aria-hidden="true"></i> {{ 'common.save'|trans({}, 'messages') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                {% endblock %}
            {% endembed %}
        </div>

        <div class="col-md-4">
            {% embed '_components/_card.html.twig' %}
                {% block card_header %}
                    <div class="card-header"><h2 class="mb-0"><i class="fa-icon fa-icon--status-info" aria-hidden="true"></i> {{ 'compliance.requirement.info.title'|trans({}, 'compliance') }}</h2></div>
                {% endblock %}
                {% block card_body %}
                    <ul class="list-unstyled small">
                        <li><i class="fa-icon fa-icon--ui-link" aria-hidden="true"></i> {{ requirement.mappedControls|length }} {{ 'compliance.requirement.info.mapped_controls'|trans({}, 'compliance') }}</li>
                        <li><i class="fa-icon fa-icon--nav-process" aria-hidden="true"></i> {{ requirement.detailedRequirements|length }} {{ 'compliance.requirement.info.sub_requirements'|trans({}, 'compliance') }}</li>
                        <li><i class="fa-icon fa-icon--ui-calendar" aria-hidden="true"></i> {{ 'common.created'|trans({}, 'messages') }} {{ requirement.createdAt|date('Y-m-d') }}</li>
                    </ul>
                {% endblock %}
            {% endembed %}

            {% embed '_components/_card.html.twig' with { 'class': 'mt-3 bg-body-secondary' } %}
                {% block card_header %}
                    <div class="card-header"><h2 class="mb-0"><i class="fa-icon fa-icon--ui-lightbulb" aria-hidden="true"></i> {{ 'common.note'|trans({}, 'messages') }}</h2></div>
                {% endblock %}
                {% block card_body %}
                    <p class="small mb-0">
                        {{ 'compliance.requirement.edit.note'|trans({}, 'compliance') }} <a href="{{ path('app_compliance_requirement_show', {id: requirement.id}) }}">{{ 'compliance.requirement.view_page_note'|trans({}, 'compliance') }}</a>.
                    </p>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
</div>
{% endblock %}
