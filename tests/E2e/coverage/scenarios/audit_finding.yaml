# Audit-Finding scenarios (ISO 27001 Cl. 9.2).
#
# AuditFindingType requires an existing InternalAudit (`audit` EntityType,
# required=true). The connected-demo-data seeder does NOT create any
# InternalAudit rows yet, so the L2 create scenario is deferred until a
# seeded audit-id is available.

scenarios: []
