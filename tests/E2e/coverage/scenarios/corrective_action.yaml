# CAPA scenarios (ISO 27001 Cl. 10.1).
#
# CorrectiveActionType requires `finding` (EntityType, AuditFinding-id).
# Demo-data seeder yields 0 AuditFindings → 0 CorrectiveActions creatable.
# Scenario deferred until the seeder emits a baseline finding.

scenarios: []
